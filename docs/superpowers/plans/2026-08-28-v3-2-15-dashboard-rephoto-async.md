# V3.2.15 看板异常口径与翻拍图片异步加载发布计划

## 目标

- 项目进度“异常与缺照”不统计唯一异常原因是 `missing_collector_photo` 的资料组。
- 如果资料组同时存在其他异常，继续按异常资料组统计并在数据中台下钻展示。
- 审阅与翻拍工作台先加载终端文字资料，再异步加载照片。
- 保留失败降级、跨终端防串图、审计、权限和现有翻拍流程。

## 实现边界

- PostgreSQL 看板聚合、JSON 本地聚合和数据中台下钻使用同一口径。
- 终端打开接口不解析图片 URL；完整终端详情接口保持默认返回图片 URL，供前端异步补图。
- 前端异步请求只合并同一终端、同一打开序列的结果，失败时保留文字资料。
- 不改变照片、采集器、翻拍完成状态和生产业务数据。
- 不新增数据库迁移，Alembic 继续保持 `20260824_0016`。

## 验证

- 后端全量测试与相关 PostgreSQL/JSON/工作台回归。
- 前端工作台组件测试、相关测试、TypeScript 类型检查和 Vite 生产构建。
- V3.2.15 源码、发布包、SOP 和历史版本兼容门禁。
- 生产发布前备份 `.env`、共享数据、uploads、PostgreSQL dump 和当前 release。
- 切换后等待 `127.0.0.1:8000` 实际监听，再检查 `/health`、`/project-board`、`/review-workbench` 与未登录接口权限。
- retention 先 dry-run，只在认证 SSH 稳定且目标安全时保留最近 5 个 release。

## 回滚

- 应用回滚到生产 V3.2.14 immutable release。
- 数据库保持 `20260824_0016`，不运行 downgrade。
