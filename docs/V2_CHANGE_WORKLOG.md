# V2 修改工作记录

> 目的：避免重复修改同一页面、避免靠记忆判断状态。后续每完成一个修改，都必须在这里记录文件、目标、状态和验证结果。

### 审阅工作台 P0：当前 Vue 实现复核

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 静态基准：
  - `v2-api/app/static/task_hall.html`
- 复核文件：
  - `v2-web/src/views/TaskHallView.vue`
  - `v2-api/app/static/task_hall.html`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 复核内容：
  - 图片半图/灰图：当前主图通过后端内容接口转为 `blob:`，Browser 验证 natural size `810x1080`，无图片错误提示。
  - 审阅列表分类：`全部 / 可审阅 / 异常 / 已归档 / 未施工` 五类筛选存在，数量随筛选变化。
  - 未施工折叠：默认全部视图折叠未施工资料组；切换未施工筛选展示未施工卡片；按表号搜索可打开折叠资料组。
  - 键盘切组自动滚动：连续 `ArrowDown` 后活动卡片仍在资料组列表可视区域内。
  - 重复分类拦截：将 2 张照片设为同一分类后点击 `归档当前资料组`，页面提示 `分类重复`，未提交整组归档。
  - 移动端：390px 视口无横向溢出，主图完整加载，审阅操作按钮可见。
- 测试前置：
  - 本地 `demo-team` 终端 `350000434929` 原本未被审阅员领取。
  - 临时用 `reviewer` 领取该终端做浏览器验证，验证后已释放，避免污染本地测试状态。
- 验证：
  - Browser：`/task-hall?qa=review-p0-audit-2`，任务卡 1 个、资料组 87 个，默认显示 22 个非未施工组，折叠 65 个未施工组。
  - Browser：主图 `complete=true`，`naturalWidth=810`，`naturalHeight=1080`，无 `.image-error-tip`。
  - Browser：`ArrowDown` 连续切组后，活动资料组卡片 `activeInside=true`，列表 `scrollTop=545.33`。
  - Browser：未施工筛选显示 `65` 个未施工组，搜索 `120020467667` 后显示 `1` 个目标未施工组。
  - Browser：重复分类拦截显示 `分类重复`，当前资料组仍停留原位。
  - Browser 390px：`clientWidth=375`、`scrollWidth=375`、`hasHorizontalOverflow=false`，主图 `810x1080` 完整加载。
- 状态：完成，本轮未发现新的审阅 P0 阻断；无业务代码修改。

### 独立异常处理页与缓存上传页删除状态复核

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户规则：
  - 独立缓存上传页已合并至施工采集页，独立页面删除。
  - 独立异常处理页已合并至审阅工作台，独立页面删除。
  - 静态 HTML 隐藏或不可达功能默认取消，不作为 Vue 对齐目标。
- 复核文件：
  - `v2-web/src/router/index.ts`
  - `v2-web/src/router/staticPages.ts`
  - `v2-api/app/main.py`
  - `docs/V2_CHANGE_WORKLOG.md`
- 复核结果：
  - Vue 路由没有 `unmatched`、`construction-cache` 页面组件。
  - 顶部导航只注册 `项目看板 / 任务领取 / 审阅工作台 / 施工采集`，不显示独立 `异常处理` 或 `缓存上传`。
  - 后端 `/unmatched` 旧地址重定向到 `/task-hall`。
  - 后端 `/construction-cache` 旧地址重定向到 `/construction`。
  - `v2-web/src/views/UnmatchedView.vue`、`v2-web/src/views/ConstructionCacheView.vue`、`v2-api/app/static/unmatched.html`、`v2-api/app/static/construction_cache.html` 均不存在。
- 验证：
  - HTTP 验证：`/unmatched` 返回 `307`，`/construction-cache` 返回 `307`。
  - HTTP 验证：`/static/unmatched.html` 与 `/static/construction_cache.html` 返回 `404`。
  - Browser 验证：访问 `/unmatched?qa=retired-exception-page` 与 `/app?page=unmatched&qa=retired-exception-page`，最终均落到 `/task-hall`；顶部导航无独立 `异常处理`、无独立 `缓存上传`、无 iframe。
  - Browser 验证：访问 `/construction-cache?qa=retired-cache-page` 与 `/app?page=construction-cache&qa=retired-cache-page`，最终均落到 `/construction`；顶部导航无独立 `异常处理`、无独立 `缓存上传`、无 iframe。
- 状态：完成，无需新增代码修改。

### 施工采集：IndexedDB 自动缓存与空草稿清理对齐

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 静态基准：
  - `v2-api/app/static/construction.html`
- 修改文件：
  - `v2-web/src/views/ConstructionView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - 对齐静态施工页的自动缓存策略：采集器、模块号、异常说明输入时 500ms 防抖写入 IndexedDB。
  - 切换终端、切换资料组、返回任务区、关闭移动端采集抽屉、组件卸载前都会先尝试 flush 当前草稿。
  - 保存缓存时如果草稿没有照片、采集器、模块号、异常工单 ID，则删除空草稿，避免“空项目也显示已缓存”。
  - 表单从后端详情或已有草稿载入时暂停自动缓存，避免只打开资料组就生成新草稿。
- 验证：
  - `v2-web`: `vue-tsc --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过，仅保留既有第三方 PURE 注释和 chunk 体积警告。
  - `.venv\Scripts\python.exe scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面 5，legacy bridge 0。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：通过，`43 passed, 1 warning`。
  - Browser 验证：`/construction?qa=autosave-cache` 进入终端 `350000434929`，未施工组输入采集器/模块号后，已缓存计数从 2 增至 3，资料组显示 `本地缓存 0 张 / 模块 QA-WATCH-MODULE`。
  - Browser 验证：清空该测试草稿后，已缓存计数恢复为 2，未施工计数恢复为 63，输入框值为空，未留下测试缓存。
  - Browser 390px 验证：`clientWidth=scrollWidth=390`，施工页工作模式正常，筛选 `全部/未施工/已缓存/异常工单` 与一键上传入口可见，控制台无应用 `error/warn`。
- 状态：完成。

### 项目看板：管理员账号管理 Vue 对齐

- 时间：2026-06-21
- 版本目标：V2.4.x
- 静态基准：
  - `v2-api/app/static/project_board.html`
- 修改文件：
  - `v2-web/src/views/ProjectBoardView.vue`
- 改动内容：
  - 在项目看板新增仅管理员可见的账号管理区。
  - 对齐静态 HTML 的账号、姓名、密码、团队、角色、状态、刷新、保存、清空、编辑、删除能力。
  - 接入已有 `/auth/users` 前端服务函数：`fetchUserAccounts`、`saveUserAccount`、`deleteUserAccount`。
  - 删除当前登录账号时前端拦截，避免管理员误删自己。
  - 增加局部响应式样式，桌面高密度展示，窄屏自动折行。
- 当前验证：
  - `node node_modules\vue-tsc\bin\vue-tsc.js --noEmit`：通过。
  - `node node_modules\vite\bin\vite.js build`：通过，仅保留既有第三方 PURE 注释和大 chunk 警告。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面 5，legacy bridge 0。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：通过，`42 passed, 1 warning`。
  - `.venv\Scripts\python.exe scripts\smoke-client-demo.py`：通过。
  - Browser 验证：`http://127.0.0.1:8000/project-board?qa=account-admin` 中账号管理面板可见，创建临时账号 `qa-vue-*` 成功，编辑姓名成功，删除成功，最终临时账号行数为 0，控制台无应用 `error/warn`。
  - Browser 截图接口 `Page.captureScreenshot` 两次超时，未取得截图；本项以 DOM 状态、交互结果和控制台日志作为证据。
- 状态：完成。

### 静态审阅页异常处理页面残留删除
- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户规则：
  - 异常处理页已经合并至审阅工作台，独立异常处理页可以删除。
  - 审阅工作台内也不再保留“异常导出”页签或页面式异常分区。
  - 静态 HTML 中隐藏或不可达的异常页面残留默认取消，不再作为 Vue 对齐目标。
- 修改文件：
  - `v2-api/app/static/task_hall.html`
  - `scripts/run-client-demo.ps1`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_4X_VUE_FULL_PARITY_EXECUTION_PLAN.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - 删除静态 `task_hall.html` 中的 `照片审阅 / 异常导出` 工作台页签。
  - 删除静态 `task_hall.html` 中的 `exceptionWorkspace` 页面式异常处理/导出分区。
  - 删除静态脚本里的 `workspace=exceptions` 初始化模式、`workspaceMode` 状态、`setWorkbenchMode()`、`data-workbench-tab` 点击绑定和键盘拦截逻辑。
  - 将 `导出异常表计` 保留为审阅工具区内动作，避免删除页面时误删业务能力。
  - 删除演示脚本输出中的旧 `task-hall?workspace=exceptions` 链接。
  - 更新矩阵和执行计划，明确静态审阅页异常页签/分区残留已取消。
- 静态检查：
  - `rg "exceptionModeTab|reviewModeTab|workbench-tab|data-workbench-tab|exceptionWorkspace|exception-workspace|workspaceMode|setWorkbenchMode|workspace=exceptions|mode=exceptions|独立异常处理页|异常处理工作区|异常导出" v2-api/app/static/task_hall.html v2-web/src v2-api/app/main.py scripts docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md docs/V2_4X_VUE_FULL_PARITY_EXECUTION_PLAN.md`：仅剩产品评价脚本里的“异常导出”功能描述。
- 验证：
  - `rg "exceptionModeTab|reviewModeTab|workbench-tab|data-workbench-tab|exceptionWorkspace|exception-workspace|workspaceMode|setWorkbenchMode|workspace=exceptions|mode=exceptions|独立异常处理页|异常处理工作区|异常导出" v2-api/app/static/task_hall.html v2-web/src v2-api/app/main.py scripts docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md docs/V2_4X_VUE_FULL_PARITY_EXECUTION_PLAN.md`：仅剩产品评价脚本里的“异常导出”功能描述。
  - `.venv\Scripts\python.exe scripts\verify-static-pages.py`：通过。
  - `.venv\Scripts\python.exe scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面数 5，legacy bridge pages 0。
  - `.venv\Scripts\python.exe scripts\smoke-client-demo.py`：通过。
  - Browser 验证：访问 `/unmatched?qa=exception-static-removed` 最终落到 `/task-hall`；顶部导航为 `项目看板 / 任务领取 / 审阅工作台 / 施工采集`，无独立 `异常处理`。
  - Browser 验证：访问 `/task-hall?workspace=exceptions&qa=exception-query-ignored` 仍显示普通审阅工作台，不出现 `异常导出` 页签、`独立异常处理页已取消` 或 `异常处理工作台`；审阅区保留 1 个 `导出异常表计` 按钮。
  - Browser 控制台：`error` / `warn` 日志为空。
- 状态：完成。

### 审阅工作台：坏预览回退与资料组分类卡片修复

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户反馈：
  - 审阅图片仍存在灰图、半图或彩条图。
  - 审阅列表需要像施工列表一样按分类展示。
- 修改文件：
  - `v2-api/app/api/routes/local_test.py`
  - `v2-api/tests/test_api.py`
  - `v2-api/requirements.txt`
  - `v2-api/pyproject.toml`
  - `v2-web/src/views/TaskHallView.vue`
  - `v2-api/app/static/vue/`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - 后端图片内容代理新增灰色占位图和竖向彩条坏预览识别。
  - 当 OSS 图片处理后的 `preview/thumbnail` 疑似损坏时，自动回退读取 OSS 原始对象；原始对象也不可用时再尝试从源 URL 修复并重新保存存储引用。
  - 将 `Pillow>=10.4.0` 加入后端依赖，避免生产环境缺依赖导致坏图检测失效。
  - 审阅资料组筛选改为施工页同类的分类按钮：`全部 / 可审阅 / 异常 / 已归档 / 未施工`，每项展示数量且文字不截断。
  - 审阅资料组卡片改为状态标签结构，展示表号、终端/地址、照片数、审阅人和异常原因，状态样式与施工列表保持一致。
- 验证：
  - `.venv\Scripts\python.exe -m pip install "Pillow>=10.4.0"`：已安装本地依赖。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：43 passed，只有既有 Starlette/httpx 废弃警告。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py::test_broken_oss_processed_preview_falls_back_to_original -q`：1 passed。
  - `node node_modules\vue-tsc\bin\vue-tsc.js --noEmit`：通过。
  - `node node_modules\vite\bin\vite.js build`：通过，仅保留既有第三方 PURE 注释和 chunk size 警告。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过，registered pages 5。
  - Browser/Chrome 验证：`http://127.0.0.1:8000/task-hall?qa=review-filter-recheck`，登录 `admin/default-team` 并打开已领取终端 `350000434929`；筛选标签 `全部/可审阅/异常/已归档/未施工` 均未截断，资料组卡片使用状态标签，主图自然尺寸 `810x1080`，无图片错误提示，控制台无 `error`/`warn`。
- 状态：完成。

### 项目看板：导出入口与单终端交付包 Vue 对齐

- 时间：2026-06-21
- 版本目标：V2.4.x
- 静态基准：
  - `v2-api/app/static/project_board.html`
  - `v2-api/app/static/app_shell.html`
- 修改文件：
  - `v2-web/src/api/services.ts`
  - `v2-web/src/views/ProjectBoardView.vue`
- 改动内容：
  - 新增 `exportTaskDetail()`，复用 `/exports/task-detail` 导出任务明细 Excel。
  - 调整 `exportExceptionMeters()`，支持管理员导出全部异常表计。
  - 新增 `exportTerminalDeliveryPackage()`，按静态 HTML 基准只支持单终端导出，生成 ZIP：`清单表格.csv` + `终端/安装地址/图片`。
  - 单终端 ZIP 图片下载采用 6 并发，优先使用后端 `delivery_cache_url`，失败时写入 `下载失败.txt`，避免整个包失败。
  - 项目看板顶部新增 `导出异常表计`。
  - 终端任务表新增 `导出明细`、`已归档/全部` 范围选择、`导出终端包`。
  - 新增导出进度条，展示准备清单、并发下载图片、生成压缩包等状态。
- 验证：
  - `node node_modules\vue-tsc\bin\vue-tsc.js --noEmit`：通过。
  - `node node_modules\vite\bin\vite.js build`：通过，仅保留既有第三方 PURE 注释和大 chunk 警告。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面 5，legacy bridge 0。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：通过，`42 passed, 1 warning`。
  - `.venv\Scripts\python.exe scripts\smoke-client-demo.py`：通过。
  - Browser DOM 验证：`/project-board?qa=exports-account` 中 `导出异常表计` 可见，任务表显示 12 个 `导出明细`、12 个 `导出终端包`、12 个导出范围选择，控制台无应用 `error/warn`。
  - Browser 限制：内置浏览器不支持下载事件，点击导出明细会触发工具侧 `Downloads are not supported by Codex In-app Browser`，因此真实文件落盘需在普通浏览器做一次人工 QA。
- 状态：已实现，待普通浏览器真实下载 QA。

### 任务领取：管理员收回全部与施工开放关闭 Vue 对齐

- 时间：2026-06-21
- 版本目标：V2.4.x
- 静态基准：
  - `v2-api/app/static/claim_tasks.html`
- 修改文件：
  - `v2-web/src/api/services.ts`
  - `v2-web/src/views/ClaimTasksView.vue`
- 改动内容：
  - 新增 `releaseAllClaimedTasks()`，接入 `/local-test/tasks/release-all`。
  - `收回全部` 从占位提示改为真实管理员操作，完成后刷新任务列表。
  - 每张终端任务卡增加管理员可见 `开放施工/关闭施工` 按钮，接入已有 `openConstructionTask()` / `closeConstructionTask()`。
  - 保留普通审阅员只看有扫码任务、管理员可看全部任务的规则。
- 验证：
  - `node node_modules\vue-tsc\bin\vue-tsc.js --noEmit`：通过。
  - `node node_modules\vite\bin\vite.js build`：通过，仅保留既有第三方 PURE 注释和大 chunk 警告。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面 5，legacy bridge 0。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：通过，`42 passed, 1 warning`。
  - `.venv\Scripts\python.exe scripts\smoke-client-demo.py`：通过。
  - Browser DOM 验证：`/claim-tasks?qa=admin-actions` 中 `收回全部` 可见，135 个任务卡均显示施工开放/关闭按钮和暂存释放按钮，控制台无应用 `error/warn`。
- 状态：完成。

## 记录规则

- 修改前先查看本文件，确认是否已有相同页面或相同问题的处理记录。
- 每个完成项必须写清楚：日期、版本、页面/模块、改动文件、改动目标、验证方式。
- 未验证的内容只能标记为“已修改，待验证”，不能标记为“完成”。
- 如果修改失败或发现副作用，也必须记录，避免后续重复踩同一问题。
- 版本号规则：大改动 `+0.1`，小改动 `+0.0.1`。当前目标版本按用户要求进入 `V2.4.x`。

## 2026-06-20

### 独立异常处理页 Vue 残留清理

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户规则：
  - 异常处理页已经合并至审阅工作台。
  - 独立异常处理页不可继续作为可访问页面或 Vue 迁移目标。
  - 静态 HTML 隐藏、不可达或已取消功能默认取消。
- 修改文件：
  - 删除 `v2-web/src/views/ExceptionsView.vue`
  - `v2-web/src/styles/main.css`
  - `scripts/smoke-client-demo.py`
- 改动内容：
  - 删除未被路由引用的独立异常处理 Vue 页面，避免后续误恢复。
  - 清理只服务独立异常处理页和独立缓存上传页的全局 CSS 残留。
  - smoke 脚本不再把 `/unmatched`、`/construction-cache` 当作普通页面验收。
  - `/unmatched`、`/construction-cache` 作为旧地址只验证重定向；`/app?page=unmatched`、`/app?page=construction-cache` 作为 Vue 壳兼容入口，交由前端路由跳转。
- 验证：
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面 5，legacy bridge 0。
  - `python scripts\verify-static-pages.py`：通过。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：通过，`42 passed, 1 warning`。
  - `node node_modules\vue-tsc\bin\vue-tsc.js --noEmit`：通过。
  - `node node_modules\vite\bin\vite.js build`：通过，仅保留既有第三方 PURE 注释和大 chunk 警告。
  - `.venv\Scripts\python.exe scripts\smoke-client-demo.py`：通过，旧 `/unmatched` 与 `/construction-cache` 路由均按取消页验证。
  - Browser 验证：访问 `/unmatched` 与 `/app?page=unmatched`，最终均落到 `/task-hall?workspace=exceptions`；顶部导航仅显示 `项目看板 / 任务领取 / 审阅工作台 / 施工采集`；无 iframe；异常导出作为审阅工作台内页签存在。
- 状态：完成。

### 独立缓存上传页取消并删除

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户规则：
  - `construction-cache` 独立页面直接删除。
  - 缓存上传只作为施工采集页内功能保留。
- 修改文件：
  - `v2-web/src/router/index.ts`
  - `v2-web/src/router/staticPages.ts`
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/views/ConstructionCacheView.vue`
  - `v2-api/app/main.py`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_4X_VUE_FULL_PARITY_EXECUTION_PLAN.md`
- 改动内容：
  - 移除 Vue `construction-cache` 动态路由。
  - 移除顶部导航中的 `缓存上传` 页面入口。
  - 删除 `ConstructionCacheView.vue` 独立页面文件。
  - 后端 `/construction-cache` 旧地址改为重定向到 `/construction`。
  - 对齐矩阵中 `Cache Upload` 独立页改为产品取消项，不再作为 Vue 上线阻断。
- 验证：
  - `vue-tsc --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过；仅保留既有第三方 PURE 注释和大 chunk 警告。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面数为 6。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：`42 passed, 1 warning`。
  - HTTP 验证：`/construction-cache` 返回 `307`，`Location=/construction`。
  - Browser 验证：直接访问 `http://127.0.0.1:8000/construction-cache` 最终落到 `http://127.0.0.1:8000/construction`；顶部导航只显示 `项目看板 / 任务领取 / 审阅工作台 / 施工采集 / 异常处理`，无独立 `缓存上传`；控制台无 error/warn。
- 状态：完成。

### 已取消独立页面的静态源文件收口

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户规则：
  - 独立缓存上传页已取消，缓存上传只保留在施工采集页内。
  - 独立异常处理页已取消，异常处理只保留在审阅工作台内。
  - 静态 HTML 隐藏或不可达的功能默认取消，不再作为 Vue 对齐缺口。
- 修改文件：
  - 删除 `v2-api/app/static/construction_cache.html`
  - 删除 `v2-api/app/static/unmatched.html`
  - `scripts/verify-static-pages.py`
  - `scripts/run-client-demo.ps1`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_4X_VUE_FULL_PARITY_EXECUTION_PLAN.md`
- 改动内容：
  - 移除旧静态校验脚本对 `unmatched.html` 的强制存在和专属功能检查。
  - 移除旧静态校验脚本对独立 `异常处理` 顶部导航的强制要求。
  - 演示提示中的 `Unmatched` 链接改为审阅工作台异常模式。
  - 对齐矩阵标记 `construction_cache.html` 和 `unmatched.html` 已删除，且不得在未明确重新激活前恢复。
  - 下一步执行顺序改为项目看板导出/账号、任务领取管理员操作、登录团队配置，不再重复列出已完成的审阅和施工 P0 项。
- 验证：
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面数 5，legacy bridge 0。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：通过，`42 passed, 1 warning`。
  - `python scripts\verify-static-pages.py`：通过，已不再要求独立 `unmatched.html`。
  - `node node_modules\vue-tsc\bin\vue-tsc.js --noEmit`：通过。
  - `node node_modules\vite\bin\vite.js build`：通过，仅保留既有第三方 PURE 注释和大 chunk 警告。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过。
  - HTTP 验证：`/construction-cache` 返回 `307 /construction`。
  - HTTP 验证：`/unmatched` 返回 `307 /task-hall?workspace=exceptions`。
  - HTTP 验证：`/static/construction_cache.html` 与 `/static/unmatched.html` 返回 `404`。
  - 文件验证：`v2-api/app/static/construction_cache.html` 与 `v2-api/app/static/unmatched.html` 均不存在。
  - Browser 验证：`/task-hall?workspace=exceptions` 顶部导航仅显示 `项目看板 / 任务领取 / 审阅工作台`，无独立 `异常处理` 和 `缓存上传`，页面无 iframe。
  - Browser 验证：直接访问 `/unmatched` 最终落到 `/task-hall?workspace=exceptions`，控制台无应用 error/warn。
- 状态：完成。

### 静态 HTML 隐藏功能判定规则

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户规则：
  - 静态 HTML 中隐藏、无可见入口、或最终用户流程不可达的功能，默认视为已取消。
  - 这类隐藏功能不再计入 Vue 功能缺口。
  - 只有用户明确要求恢复时，才重新列入 Vue 对齐范围。
- 文档更新：
  - `docs/V2_4X_VUE_FULL_PARITY_EXECUTION_PLAN.md`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
- 状态：完成。

### 审阅工作台页内导入取消决策

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 页面/模块：
  - 审阅工作台 `TaskHallView.vue`
  - 静态基准 `v2-api/app/static/task_hall.html`
- 决策：
  - 静态 HTML 中存在页内 Excel/CSV/JSON 导入抽屉。
  - 用户确认该功能已取消，不应作为 Vue 审阅工作台缺口继续补齐。
  - 表格导入入口后续收敛到项目看板或专门导入流程，不放在图片审阅页。
- 文档更新：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
- 状态：
  - 已将 `In-page Excel/CSV/JSON import` 标记为产品取消项，阻断状态改为 `No`。

### 生产回退到静态 HTML 功能基准

- 版本目标：`V2.3.1 静态 HTML 功能基准`
- 用户目标：线上先恢复到最后静态 HTML 实现的稳定功能，Vue 全量切换必须逐页对齐静态功能后再上线。
- 服务器动作：
  - 已检查 `/opt/module-manager-v2/releases/`。
  - 当前 Vue release 原为 `/opt/module-manager-v2/releases/20260620_145056_v240_loginfix`。
  - 已选择静态 HTML release：`/opt/module-manager-v2/releases/previous-current-20260620_005524`。
  - 已创建回退前备份：`/opt/module-manager-v2/backups/runtime/20260620_233858_before_static_rollback`。
  - 已将 `/opt/module-manager-v2/current` 切到静态 HTML release。
- 验证结果：
  - `module-manager-v2.service`：active。
  - `nginx`：active。
  - `/health`：200。
  - `/login`：返回静态 `login.html`。
  - `/app?page=project-board`：返回静态 `app_shell.html`。
  - `/project-board?embedded=1`：返回静态 `project_board.html`。
  - `/claim-tasks?embedded=1`：返回静态 `claim_tasks.html`。
  - `/task-hall?embedded=1`：返回静态 `task_hall.html`。
  - `/construction?embedded=1`：返回静态 `construction.html`。
  - `/construction-cache?embedded=1`：返回静态 `construction_cache.html`。
  - `/unmatched?embedded=1`：返回静态 `unmatched.html`。
- 状态：完成。

### V2.4.x Vue 全量对齐计划文档

- 文件：
  - `docs/V2_4X_VUE_FULL_PARITY_EXECUTION_PLAN.md`
- 规则：
  - 以后以最后静态 HTML 实现的功能为唯一功能基准。
  - 不再凭记忆判断功能是否已完成。
  - 每完成一个页面修改，必须写入本工作记录。
- 文档内容：
  - 静态 HTML 基准页面清单。
  - 每页功能 List。
  - Vue 对齐优先级。
  - 本地生产数据模拟方案。
  - 上线门禁。
- 状态：完成。

### 生产测试数据同步到本地

- 快照来源：
  - `/opt/module-manager-v2/backups/runtime/20260620_233858_before_static_rollback`
- 本地目录：
  - `exports/production-snapshot-20260620_233858`
- 已下载文件：
  - `local_state.json`
  - `users.json`
  - `postgres.dump`
  - `uploads.tar.gz`
  - `current_target.txt`
  - `rollback_target.txt`
- 说明：
  - 未下载生产 `.env`，避免将线上密钥带入本地。
  - 首次 `pg_dump` 因 SQLAlchemy 连接串 `postgresql+psycopg://` 不被 `pg_dump` 识别而失败，已转换为 `postgresql://` 后重新导出成功。
- 本地恢复：
  - Docker 容器：`module-manager-v2-parity-postgres`
  - 本地端口：`55432`
  - 数据库：`module_manager_v2_parity`
- 恢复后数据量：
  - `teams`: 1
  - `users`: 4
  - `tasks`: 135
  - `material_groups`: 22358
  - `total_catalog_rows`: 22358
  - `photos`: 2087
  - `unmatched_records`: 13
  - `audit_logs`: 188
  - `photo_events`: 522
- 状态：完成。

### 静态 HTML 到 Vue 对齐矩阵

- 文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
- 基准：
  - `v2-api/app/static/*.html`
- 内容：
  - 登录、应用壳、项目看板、任务领取、审阅工作台、施工采集、缓存上传、异常处理、同步配置、V201 的功能点对齐表。
  - 每个功能点标注 Vue 目标、当前状态、是否阻断 Vue 上线。
- 下一步执行顺序：
  - 优先修复审阅工作台图片加载和已归档照片可修改。
  - 再修复施工异常工单缓存可见和施工页一键上传。
  - 然后补齐缓存上传、项目看板导出/账号、异常人工关联、任务领取管理员操作、登录团队标识。
- 状态：完成。

### 施工页面体验重做

- 版本目标：`V2.4.9`
- 用户目标：Vue 施工页面体验要仔细对比静态 `construction.html`，按静态页面更顺手的交互重新做。
- 当前判断：不能只凭记忆确认，必须对比静态 HTML 与 Vue 页面源码，并做浏览器验证。
- 涉及页面：
  - `v2-api/app/static/construction.html`
  - `v2-web/src/views/ConstructionView.vue`
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/styles/main.css`
- 已发现差异：
  - 静态页分为“任务选择模式”和“施工工作区模式”，Vue 页面需要保持同样的专注模式。
  - 静态页移动端隐藏外层顶栏，Vue 页面需要同样处理。
  - 静态页任务区和施工区内部都有明确退出入口，Vue 页面需要保留。
  - 静态页施工区列表滚动更集中，Vue 页面需要减少整页滚动。
- 状态：进行中。
- 验证：待 `vue-tsc`、`vite build` 和浏览器对比验证。

#### 已完成：静态页与 Vue 页差异对比

- 时间：2026-06-20
- 对比来源：
  - 静态页：`v2-api/app/static/construction.html`
  - Vue 页：`v2-web/src/views/ConstructionView.vue`
- 结论：
  - 静态页的核心体验是“任务选择模式”和“施工工作区模式”明确分离。
  - 静态页进入施工后隐藏任务区，只保留资料组列表和采集表单，减少施工员干扰。
  - 静态页移动端隐藏全局顶栏，把空间留给现场采集。
  - Vue 页已有基础双模式，但顶部说明区、列表滚动高度、工作区比例和移动端专注程度仍不如静态页。
- 下一步修改范围：
  - `v2-web/src/views/ConstructionView.vue`
  - `v2-web/src/styles/main.css`
- 不修改：
  - API
  - 采集字段
  - 缓存上传逻辑
  - 施工指派规则

#### 已完成：施工页布局对齐静态页

- 时间：2026-06-20
- 文件：
  - `v2-web/src/views/ConstructionView.vue`
- 内容：
  - 将 Vue 施工页明确拆成任务选择模式和施工工作区模式。
  - 任务选择模式居中显示终端任务，任务列表在面板内滚动。
  - 施工工作区模式占满可用高度，资料组列表和采集表单不再依赖整页滚动。
  - 手机端施工页使用 `100dvh` 专注布局，隐藏外层说明区，施工资料组列表在施工面板内滚动。
  - 手机端采集抽屉高度提高到 `94dvh`，更接近静态 HTML 的现场采集体验。
- 状态：已修改，待构建与浏览器验证。

### 编码损坏修复

- 触发原因：版本号替换时使用 PowerShell 读写 UTF-8 文件，导致部分 Vue 文件出现替换字符和字符串截断。
- 受影响文件：
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/components/AppLayout.vue`
  - `v2-web/src/views/LoginView.vue`
  - `v2-web/src/views/SyncConfigView.vue`
  - `v2-web/src/views/TaskHallView.vue`
- 恢复策略：
  - 优先保留当前功能，局部修复坏字符串。
  - 若不可控，再从 `build/server-release/module-manager-v2-server-v2.4.0` 的干净源码恢复，并重新套用必要改动。
- 状态：进行中。
- 验证：待 `vue-tsc --noEmit` 通过。

#### 已完成：小文件恢复

- 时间：2026-06-20
- 文件：
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/components/AppLayout.vue`
  - `v2-web/src/views/LoginView.vue`
  - `v2-web/src/views/SyncConfigView.vue`
- 内容：
  - 恢复正常 UTF-8 中文文案。
  - 保留版本显示 `V2.4.9`。
  - 保留施工页面移动端隐藏外层顶栏所需的 `construction-route` class。
- 状态：已修改，待统一类型检查验证。

#### 已完成：审阅页编码坏行修复

- 时间：2026-06-20
- 文件：
  - `v2-web/src/views/TaskHallView.vue`
- 内容：
  - 修复所有 `U+FFFD` 替换字符造成的字符串截断。
  - 恢复审阅页、异常导出、回退未施工、转异常工单、补图、删除图片等按钮文案。
  - 修复后台刷新 `catch` 块里关闭括号被注释吞掉的问题。
- 验证：
  - `v2-web/src/views/TaskHallView.vue` 替换字符数量：`0`。
  - `v2-web/src/layouts/AppLayout.vue`、`v2-web/src/components/AppLayout.vue`、`v2-web/src/views/LoginView.vue`、`v2-web/src/views/SyncConfigView.vue` 替换字符数量：`0`。
  - `vue-tsc --noEmit`：通过。
- 状态：完成。

#### 已完成：Vue HTML 标题乱码修复

- 时间：2026-06-20
- 文件：
  - `v2-web/index.html`
- 内容：
  - 将页面标题从乱码恢复为 `模块更换项目管理器 V2.4.9`。
- 状态：已修改，待统一构建验证。

#### 已完成：前端 JSON/HTML BOM 清理

- 时间：2026-06-20
- 文件：
  - `v2-web/package.json`
  - `v2-web/index.html`
- 内容：
  - 移除 UTF-8 BOM，解决 Vite/PostCSS 严格 JSON 解析失败。
  - 未修改实际配置字段。
- 状态：已修改，待重新构建验证。

#### 已完成：构建与测试验证

- 时间：2026-06-20
- 命令：
  - `vue-tsc --noEmit`
  - `vite build`
  - `python scripts/verify_vue_migration_gate.py --strict-native`
  - `pytest v2-api/tests/test_api.py -q`
- 结果：
  - 前端类型检查通过。
  - 前端生产构建通过。
  - Vue 迁移门禁通过。
  - 后端 API 测试 `41 passed, 1 warning`。
  - Vite 仅提示第三方库注释和大 chunk 警告，不影响本轮修改。
- 额外修复：
  - `v2-api/tests/test_api.py` 的异常导出表头断言从旧乱码改为正确中文。
- 状态：完成，待浏览器视觉验证。

#### 已完成：浏览器验证

- 时间：2026-06-20
- 页面：
  - `http://127.0.0.1:8000/construction`
- 桌面验证：
  - 施工页进入任务选择模式。
  - 任务面板居中显示。
  - 页面可见内部 `刷新`、`退出` 操作。
  - 未出现旧的多栏空白施工区干扰。
- 手机验证：
  - 视口：`390 x 844`。
  - 使用施工员账号进入施工页。
  - DOM 快照显示仅保留施工任务面板、`刷新`、`退出` 和空状态。
  - 全局顶栏在手机施工页隐藏，符合静态页专注模式。
- 说明：
  - Browser 插件在手机尺寸执行 `Page.captureScreenshot` 连续超时，因此手机端以 DOM 快照验证；桌面截图已成功。
- 状态：完成。

### 审阅工作台 P0 对齐：图片完整加载与归档分类规则

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 基准页面：
  - `v2-api/app/static/task_hall.html`
- 修改文件：
  - `v2-web/src/api/services.ts`
  - `v2-web/src/views/TaskHallView.vue`
- 目标：
  - 对齐静态 HTML 的主图加载策略：先通过带鉴权请求拉取完整图片 Blob，再替换为浏览器 object URL，减少半图/灰图体感问题。
  - 保留失败时的直接 URL fallback。
  - 已归档照片可重新选中，使用快捷键或分类按钮修改临时分类，再重新归档。
  - 归档当前图或整组归档前检查重复分类标签，发现重复时阻止归档并提示先修改。
  - 键盘切组仍调用选中卡片自动滚动逻辑，完成组后按当前可见列表顺序选择后续未完成资料组。
  - 修正资料组列表中状态/张数的占位符显示。
- 验证：
  - `vue-tsc --noEmit`：通过。
  - `vite build`：通过；仅保留第三方库 PURE 注释和大 chunk 警告。
  - `python scripts/verify_vue_migration_gate.py --strict-native`：通过。
  - `pytest v2-api/tests/test_api.py -q`：`41 passed, 1 warning`。
  - Browser 本地验证：
    - URL：`http://127.0.0.1:8000/task-hall`
    - 账号：`reviewer / review123`
    - 页面身份：标题 `模块更换项目管理器 V2.4.9`，审阅工作台正常渲染。
    - 控制台：无应用相关 error/warn。
    - 键盘 `ArrowDown`：选中资料组从 `120000786354` 切到 `120000983963`，active 卡片仍在列表可视区域。
    - 未施工筛选：点击 `未施工 65` 后显示 65 个未施工资料组，折叠提示消失。
    - 未施工搜索：回到 `全部` 后搜索 `120020467667`，可显示默认折叠的未施工资料组。
    - 视口检查：
      - `1366 x 768`：无横向溢出，无错误覆盖层，审阅工作台有内容。
      - `1920 x 1080`：无横向溢出，无错误覆盖层，审阅工作台有内容。
      - `390 x 844`：无横向溢出，无错误覆盖层，审阅工作台有内容。
  - 图片验证限制：
    - 当前本地快照中选中测试照片只有 `sha256/barcode/collector/module`，缺少 `image_url/source_url/storage_key`。
    - 因此浏览器只能验证“无可读地址时给出明确占位”，不能用该快照证明真实图片完整显示。
- 状态：本轮 P0 代码和基础验证完成；真实图片完整显示仍需用含 `image_url` 或 `storage_key` 的测试数据复测。

#### 已完成：审阅图片源优先级与真实图片复测

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 基准页面：
  - `v2-api/app/static/task_hall.html`
- 修改文件：
  - `v2-web/src/api/types.ts`
  - `v2-web/src/api/services.ts`
  - `v2-web/src/views/TaskHallView.vue`
- 目标：
  - Vue 照片模型补充 `sourceUrl`，保留后端 `source_url` 字段，避免历史导入照片丢失原始图 fallback。
  - `mapPhoto()` 的图片地址优先级对齐静态页：`delivery_cache_url -> preview_url -> thumbnail_url -> image_url/source_url/url`。
  - 审阅主图和缩略图 fallback 顺序对齐静态页，优先使用交付缓存、预览图和缩略图，再回退到原始 URL。
  - 收紧 `hasReadablePhotoSource()`：存储型照片必须有 `storageKey` 才视为可读，避免仅有 `storageType` 的坏索引触发灰图加载。
- 本地测试数据处理：
  - 当前本地 `demo-team/g-07868` 的原始照片索引没有 `image_url/source_url/storage_key`，无法证明真实图加载。
  - 仅在本地 PostgreSQL 测试库中，将 `demo-team/g-07868` 的照片 `p-22-1` 指向仓库已有图片 `v2-api/app/static/uploads/manual/g-02755-4877a44943214e62a91a14ca88f0cbb5.jpg`。
  - 该操作只用于本地 QA，不涉及生产数据。
- 验证：
  - `vue-tsc --noEmit`：通过。
  - `vite build`：通过；仅保留第三方库 PURE 注释和大 chunk 警告。
  - `python scripts/verify_vue_migration_gate.py --strict-native`：通过。
  - `pytest v2-api/tests/test_api.py -q`：`41 passed, 1 warning`。
  - API 图片接口：
    - `GET /local-test/groups/g-07868/photos/p-22-1/content?kind=thumbnail&team_id=demo-team`：`200 image/jpeg`。
    - `GET /local-test/groups/g-07868/photos/p-22-1/content?kind=preview&team_id=demo-team`：`200 image/jpeg`。
    - `GET /local-test/groups/g-07868/photos/p-22-1/content?kind=original&team_id=demo-team`：`200 image/jpeg`。
  - Browser 本地验证：
    - URL：`http://127.0.0.1:8000/task-hall`
    - 页面标题：`模块更换项目管理器 V2.4.9`。
    - 选中资料组：`120000784940`。
    - 主图元素存在，`src` 为 `blob:http://127.0.0.1:8000/...`，说明已走后端 content 接口并转 object URL。
    - 主图自然尺寸：`810 x 1080`，`complete=true`。
    - 控制台：无应用相关 error/warn。
    - 截图证据：审阅工作台主图完整显示，未出现灰图/半图。
    - 视口复测：
      - `1366 x 768`：无横向溢出，无错误覆盖层，目标主图 `blob:` 加载，尺寸 `810 x 1080`。
      - `1920 x 1080`：无横向溢出，无错误覆盖层，目标主图 `blob:` 加载，尺寸 `810 x 1080`。
      - `390 x 844`：无横向溢出，无错误覆盖层，目标主图 `blob:` 加载，尺寸 `810 x 1080`。
- 仍需跟进：
  - 当前本地补图接口在 PostgreSQL 测试数据上出现读写仓储不一致：`GET /local-test/groups/g-07868` 可读，但 `POST /local-test/groups/g-07868/photos/upload-images` 返回 `Group not found`。这属于后续“补图闭环”功能，需要在审阅 P0 或异常/补图专项中继续处理。
- 状态：图片源优先级修正完成，真实图片渲染复测通过；补图写入闭环另列风险。
### 审阅工作台 P0：PostgreSQL 归档状态持久化、OSS 响应兜底、键盘切组滚动

- 时间：2026-06-21
- 版本目标：V2.4.x Vue 全量对齐
- 基准页面：
  - `v2-api/app/static/task_hall.html`
- 修改文件：
  - `v2-api/app/services/state_repository.py`
  - `v2-api/app/services/photo_storage.py`
  - `v2-api/tests/test_state_repository.py`
  - `v2-api/tests/test_api.py`
  - `v2-web/src/views/TaskHallView.vue`
- 目标：
  - 修复 PostgreSQL 仓储 `classify_photo()` 只保存分类、不持久化 `archive_status/archive_filename/archived_at` 的问题，避免刷新后已归档状态丢失。
  - 修复本地/测试环境缺 OSS 配置时，`resolve_photo_for_response()` 对 OSS 照片签名失败导致分类接口 500 的问题；无签名配置时降级返回原始 `oss://` 引用。
  - 修复审阅工作台连续方向键切换资料组时 active 卡片没有稳定滚入列表可视区的问题。
  - 继续验证“已归档照片可重新选中修改分类”和“重复分类标签禁止整组归档”。
- 本地 QA 数据说明：
  - 仅在本地 PostgreSQL 测试库中，将任务 `67` 的资料组临时归入 `demo-team` 并由 `reviewer` 领取，用于浏览器验证；不涉及生产数据。
  - 测试组：`g-07818 / 110023771246`。
- 验证结果：
  - `pytest v2-api/tests/test_state_repository.py -q`：通过，`7 passed`。
  - 针对新增 OSS 兜底和 PostgreSQL 归档字段的定向测试：通过，`3 passed, 1 warning`。
  - HTTP 接口复测：
    - `PATCH /local-test/groups/g-07818/photos/p-ez-import-row-41-41-4-4/category?include_group=true`
    - 返回 `200`，照片返回 `archive_status=archived`、`archive_filename=表箱整体改造后.jpg`。
  - Browser 审阅工作台复测：
    - 已归档第 1 张照片可点击选中，并改为 `采集器条形码`。
    - 数据库确认第 1 张照片持久化为 `category=collector_barcode`、`archive_status=archived`、`archive_filename=采集器条形码.jpg`。
    - 将第 2 张也改为 `采集器条形码` 后点击“归档当前资料组”，页面出现 `分类重复` 提示，整组归档被阻断。
    - 连续 43 次 `ArrowDown` 后，active 资料组仍在 `.review-group-list` 可视区域内，`visible=true`，控制台无 error/warn。
- 完整门禁：
  - `vue-tsc --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过，已构建到 `v2-api/app/static/vue/`；仅保留第三方 PURE 注释和大 chunk 警告。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过。
  - `pytest v2-api/tests/test_api.py v2-api/tests/test_state_repository.py -q`：通过，`49 passed, 1 warning`。

### 施工采集 P0：地址排序与模糊搜索对齐

- 时间：2026-06-21
- 版本目标：V2.4.x Vue 全量对齐
- 基准页面：
  - `v2-api/app/static/construction.html`
- 修改文件：
  - `v2-web/src/views/ConstructionView.vue`
- 目标：
  - 对齐静态 HTML 的施工地址搜索逻辑：搜索词按中文/数字/字母 token 拆分，每个 token 都可做模糊顺序匹配。
  - 保留 Vue 已补充的长条码/短表号候选匹配，确保扫码表号仍可直接打开施工单。
  - 将 Vue 施工地址排序从简单整段 `localeCompare` 改为中文地址分段排序：有无地址、地址骨架、弄/巷/村、号、幢/栋/楼、单元、室/房、车位/车库/库、完整地址、表号。
- 当前验证状态：
  - `vue-tsc --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过，已构建到 `v2-api/app/static/vue/`；仅保留第三方 PURE 注释和大 chunk 警告。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过。
  - `pytest v2-api/tests/test_api.py v2-api/tests/test_state_repository.py -q`：通过，`49 passed, 1 warning`。
  - Browser 本地验证：
    - URL：`http://127.0.0.1:8000/construction`
    - 账号：`constructor / construct123`
    - 测试数据：本地 PostgreSQL 快照 `demo-team`，终端 `350000434929` 已指派给 `constructor`。
    - 施工组接口返回 9 个零照片组；刷新 Vue 页面后可见 8 个普通未施工组，异常工单单独进入 `异常工单` 筛选，未混入 `全部`。
    - 默认列表按地址顺序显示：`聚丰园路188弄`、`52号101室`、`201室`、`202室`、`301室`、`236号`、`238号`、`上海市宝山区聚丰园路188弄52号公灯`。
    - 搜索 `52号101` 后只显示 `110023765004 / 聚丰园路188弄52号101室`。
    - 输入完整表号 `110023765004` 并按 Enter 后，采集表单打开该资料组，表号、终端、地址和照片槽位正常显示。
    - 视口 `390 x 844`：无横向溢出，未施工列表可滚动，`全部/未施工/已缓存/异常工单` 筛选和 `一键上传` 入口可见。
    - 控制台无应用相关 error/warn。
  - 说明：静态 HTML 的模糊搜索会同时匹配表号、地址和终端；因此类似 `52 101` 的宽泛 token 可能匹配多条地址，这是静态基准语义，不在本轮擅自收窄。
  - 状态：完成。

### 施工采集 P0：离线终端快照验证

- 时间：2026-06-21
- 版本目标：V2.4.x Vue 全量对齐
- 基准页面：
  - `v2-api/app/static/construction.html`
- 涉及文件：
  - `v2-web/src/views/ConstructionView.vue`
- 目标：
  - 验证施工页在已加载终端后，会保存终端任务和资料组快照。
  - 验证后端不可达时，施工员仍可从任务区进入已缓存终端，并查看该终端未施工资料组。
- 验证方式：
  - Browser 本地页面：`http://127.0.0.1:8000/construction`
  - 账号：`constructor / construct123`
  - 测试终端：`350000434929`
  - 先在线加载终端，确认 `全部 8`、`未施工 8`、`已缓存 1`、`异常工单 1`。
  - 停止本地 FastAPI 后端，模拟服务不可达。
  - 在已加载页面点击 `返回任务区`，任务区仍显示已指派终端 `350000434929`。
  - 点击终端卡片后，页面从本地快照恢复 8 个未施工资料组。
  - `390 x 844` 视口下无横向溢出，控制台无应用相关 error/warn。
  - 验证后已恢复本地 FastAPI 服务并重置浏览器视口。
- 状态：完成。
- 视口复测：
  - `1366 x 768`：页面非空、无框架错误覆盖层、无横向溢出；选中本地样本图后主图 `blob:` 加载，尺寸 `810 x 1080`。
  - `1920 x 1080`：页面非空、无框架错误覆盖层、无横向溢出，active 资料组可见。
  - `390 x 844`：页面非空、无框架错误覆盖层、无横向溢出；主图 `810 x 1080` 完整加载，错误 toast 消失。
- 状态：
  - 本轮审阅工作台 P0 子项已完成并通过定向验证：PostgreSQL 归档状态持久化、无 OSS 配置响应兜底、已归档照片可重新修改、重复分类阻断、键盘切组滚动。
  - 图片完整加载在本地通过本地样本图验证；生产 OSS 图仍依赖生产环境 OSS 配置。

### 施工采集 P0：筛选逻辑与异常工单缓存对齐

- 时间：2026-06-21
- 版本目标：V2.4.x Vue 全量对齐
- 基准页面：
  - `v2-api/app/static/construction.html`
  - `v2-api/app/static/construction_cache.html`
- 修改文件：
  - `v2-web/src/views/ConstructionView.vue`
  - `v2-web/src/views/ConstructionCacheView.vue`
- 目标：
  - 对齐静态 HTML 的施工资料组筛选规则：`全部` 只显示普通未施工资料组，不混入异常工单。
  - `异常工单` 仅在异常筛选中显示；异常工单已有本地缓存时，也能在 `已缓存` 筛选中显示。
  - 消除异常工单草稿和普通缓存草稿重复生成列表卡片的问题。
  - 缓存上传页复用施工页的异常工单必填照片判断：非照片类异常工单可以仅凭模块号和工单信息成为可上传缓存；照片类异常仍要求必填槽位。
- 当前验证状态：
  - 代码修改已完成。
  - 待运行 `vue-tsc --noEmit`、Vue build、迁移门禁和浏览器施工页/缓存页验证。
- 浏览器预检后的追加修正：
  - 在本地施工页发现 `全部` 计数为 8，但列表额外显示一个仅存在 IndexedDB 的独立缓存卡片。
  - 已修正为：独立缓存只进入 `已缓存` 筛选；`全部` 继续只显示静态 HTML 基准中的普通未施工清单；`已缓存` 计数补充独立缓存数量。
- 验证结果：
  - `vue-tsc --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过，已构建到 `v2-api/app/static/vue/`；仅保留第三方 PURE 注释和大 chunk 警告。
  - Browser 本地验证：
    - URL：`http://127.0.0.1:8000/construction`
    - 账号：`constructor / construct123`
    - 本地 QA 准备：仅在本地测试库中将终端 `350000434929` 指派给 `constructor`，并为 `g-07817` 创建异常工单。
    - `全部` 筛选：计数 `8`，实际卡片 `8`，异常工单和独立缓存不再混入。
    - `已缓存` 筛选：显示独立缓存卡片；保存异常工单缓存后计数从 `1` 变为 `2`，异常工单缓存卡片显示 `LOCAL-MODULE-TEST-001`。
    - `异常工单` 筛选：仅显示 `g-07817` 异常工单。
    - URL：`http://127.0.0.1:8000/construction-cache`
    - 异常工单缓存上传前：缓存页显示 `全部缓存资料组 2`、`可统一上传 1`，异常工单缓存标记为 `可上传`。
    - 点击 `上传当前缓存` 后：缓存页显示 `全部缓存资料组 1`、`可统一上传 0`，异常工单缓存被删除。
    - API 复核：`GET /local-test/construction/exception-orders?actor=constructor&task_id=67` 返回该工单 `status=resolved`，payload 中 `module_asset_no=LOCAL-MODULE-TEST-001`。
    - 控制台：无应用相关 error/warn；Browser 插件自身 Statsig 网络超时与应用无关。
    - 手机视口：`390 x 844`，`http://127.0.0.1:8000/construction` 无横向溢出，无框架错误覆盖层，`全部/未施工/已缓存/异常工单` 筛选均可见，列表显示 8 个未施工卡片。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过。
  - `pytest v2-api/tests/test_api.py v2-api/tests/test_state_repository.py -q`：通过，`49 passed, 1 warning`。
### 缓存上传 P1：返回采集入口对齐

- 时间：2026-06-21
- 版本目标：V2.4.x Vue 全量对齐
- 基准页面：
  - `v2-api/app/static/construction_cache.html`
- 修改文件：
  - `v2-web/src/views/ConstructionCacheView.vue`
- 目标：
  - 对齐静态 HTML 缓存页的 `返回采集` 入口。
  - 保持现有缓存列表、详情编辑、图片查看、单条上传、全部上传逻辑不变。
- 修改内容：
  - 在缓存上传页头部操作区增加 `返回采集` 按钮。
  - 点击后通过 Vue Router 返回 `construction` 路由。
- 验证：
  - `vue-tsc --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过，已构建到 `v2-api/app/static/vue/`；仅保留第三方 PURE 注释和大 chunk 警告。
  - Browser 本地验证：
    - URL：`http://127.0.0.1:8000/construction-cache`
    - 页面显示 `返回采集`、`刷新`、`一键上传`。
    - 点击 `返回采集` 后 URL 变为 `http://127.0.0.1:8000/construction`。
    - 返回后施工页显示终端 `350000434929` 的未施工列表，控制台无应用相关 error/warn。
- 状态：完成。
### 审阅工作台 P0：下一未完成组选择、OSS 直连兜底拦截与筛选折叠复测

- 时间：2026-06-21
- 版本目标：V2.4.x Vue 全量对齐
- 基准页面：
  - `v2-api/app/static/task_hall.html`
- 修改文件：
  - `v2-web/src/views/TaskHallView.vue`
- 目标：
  - 对齐静态 HTML 的“未完成资料组”定义：非异常、非已归档、非未匹配、照片数大于 0。
  - 修复 Vue 归档当前图或整组后从当前可见列表寻找下一组的问题，改为从任务全量资料组中选择下一个未完成资料组。
  - 当当前搜索或筛选会遮挡下一未完成组时，自动切回“全部”并清空搜索，确保选中卡片在审阅列表中可见。
  - 禁止将 `oss://...` 内部对象引用作为浏览器 `<img src>` 直连 fallback；OSS 照片只能走后端 content 接口、签名 URL、blob URL 或有效 http/local/data/blob 地址。
- 验证命令：
  - `vue-tsc --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过，仅保留第三方 PURE 注释和大 chunk 警告。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py v2-api\tests\test_state_repository.py -q`：`49 passed, 1 warning`。
- Browser 本地验证：
  - URL：`http://127.0.0.1:8000/task-hall`
  - 本地服务：`STATE_BACKEND=postgres`、本地 PostgreSQL 快照库、`DEMO_AUTH_ENABLED=true`。
  - 测试账号：`reviewer / review123`。
  - 页面身份：标题 `模块更换项目管理器 V2.4.9`，审阅工作台正常渲染。
  - 筛选计数：`全部 87`、`可审阅 69`、`异常 6`、`已归档 3`、`未施工 9`。
  - 默认全部：显示 78 张资料组卡片，并提示“已折叠 9 个未施工资料组”。
  - 未施工筛选：点击 `未施工 9` 后显示 9 张未施工卡片。
  - 未施工搜索：搜索 `120020467667` 后可打开默认折叠的未施工资料组，列表仅显示该 1 条。
  - 键盘切组：`ArrowDown` 后资料组 active 从 `110020605589` 切到 `110020709567`，active 卡片仍在 `.review-group-list` 可视区域。
  - 图片加载：主图 `src` 为 `blob:http://127.0.0.1:8000/...`，自然尺寸 `810 x 1080`，无 `oss://` 直连源，无“图片加载失败”提示。
  - 视口复测：
    - `1366 x 768`：无横向溢出，主图完整加载。
    - `1920 x 1080`：无横向溢出，主图完整加载。
    - `390 x 844`：无横向溢出，主图完整加载。
  - 控制台：无应用相关 `error/warn`；Browser 插件自身 Statsig 网络超时与应用无关。
- 状态：本轮审阅 P0 子项完成；仍需后续继续做归档真实提交后的端到端“自动选下一组”回归和其它页面缺口。
### 审阅工作台 P0：真实整组归档后选择第一未完成资料组

- 时间：2026-06-21
- 修改文件：
  - `v2-web/src/views/TaskHallView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
- 静态基准：
  - `v2-api/app/static/task_hall.html`
  - 静态版 `findFirstUnclassifiedGroup()` 在资料组完成后刷新列表，并从全量资料组中选择第一条未完成可审阅资料组。
- 目标：
  - Vue 归档整组后不再从“刚完成资料组之后”寻找下一组，而是对齐静态版语义：刷新后选当前渲染顺序中的第一条未完成可审阅资料组。
  - 当筛选或搜索会遮挡下一组时，自动回到“全部”并清空搜索。
  - 修复审阅页在约 1366 级别视口下因三列最小宽度过大产生的 8px 横向溢出。
- 实现：
  - 将 `firstUnfinishedGroupAfter()` 改为 `firstUnfinishedGroup()`。
  - 归档完成后先恢复 `groupFilter='all'` 和空搜索，再按 `visibleGroups` 当前渲染顺序选择第一条 `isUnfinishedReviewGroup()`。
  - 将 `.review-workbench-v24` 三列从固定最小宽度 `250px 360px minmax(620px, 1fr)` 改为 `minmax(220px, 250px) minmax(300px, 360px) minmax(0, 1fr)`。
- 本地验证：
  - 环境：`APP_ENV=development`、`DEMO_AUTH_ENABLED=true`、`STATE_BACKEND=postgres`、本地 PostgreSQL `module_manager_v2_parity`。
  - Browser 路由：`http://127.0.0.1:8000/task-hall`。
  - 真实 E2E：选择资料组 `110020709567`，点击“归档当前资料组”，归档后计数从 `可审阅 68 / 已归档 4` 变为 `可审阅 67 / 已归档 5`。
  - 归档后自动选中列表第一条未完成资料组 `110023771246`，active 卡片为 `.review-group-list` 第一张且在可视区。
  - 主图使用 `blob:http://127.0.0.1:8000/...`，自然尺寸 `810x1080`。
  - 布局验证：`document.documentElement.scrollWidth === clientWidth === 1256`，无横向溢出。
  - 页面标题 `模块更换项目管理器 V2.4.9`，DOM 包含审阅工作台内容。
  - 应用控制台 error/warn 为空；浏览器插件自身 Statsig 网络超时与应用无关。
- 命令：
  - `.\node_modules\.bin\vue-tsc.cmd --noEmit`
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`
  - `python scripts\verify_vue_migration_gate.py --strict-native`
  - `.\.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`
- 结果：
  - `vue-tsc` 通过。
  - Vue build 通过。
  - Vue migration gate 通过。
  - `test_api.py` 通过：`42 passed, 1 warning`。
- 矩阵更新：
  - `Archive whole group` 改为非阻断。
  - `Duplicate category block` 根据已有浏览器验证改为非阻断。
  - `Archived photos can be selected and corrected` 根据已有浏览器/API/DB 验证改为非阻断。
  - `Select first unfinished group after archive` 改为非阻断。
### 审阅工作台 P0：大图审阅与完整键盘链路对齐

- 时间：2026-06-21
- 修改文件：
  - `v2-web/src/views/TaskHallView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
- 静态基准：
  - `v2-api/app/static/task_hall.html`
  - 静态版大图审阅包含 `- / 适屏 / + / 比例 / Esc`、大图内分类按钮、滚轮缩放、双击切换缩放、拖动查看、`+/-/0/Esc` 键盘控制。
  - 静态版审阅键盘包含 `1-4` 分类、`Enter` 归档当前图、左右切图、上下切资料组。
- 目标：
  - Vue 大图审阅补齐静态版大图内分类按钮、比例标签、双击 2x/适屏切换、`+/-/0` 键盘缩放/适屏。
  - 保持 `Esc` 关闭大图、滚轮缩放、拖动查看。
  - 验证 `1-4`、方向键、`Enter` 和 `Esc` 的完整审阅链路。
- 实现：
  - 新增 `lightboxScaleLabel`。
  - 新增 `clampLightboxPan()` 和 `toggleLightboxZoom()`。
  - 大图打开状态下支持 `+`/`=` 放大、`-` 缩小、`0` 适屏、`Esc` 关闭。
  - 大图模板增加分类按钮区和提示区；移动端分类按钮改为两列。
  - 拖动时限制平移范围，适屏时自动归零。
- 本地验证：
  - 环境：`APP_ENV=development`、`DEMO_AUTH_ENABLED=true`、`STATE_BACKEND=postgres`、本地 PostgreSQL `module_manager_v2_parity`。
  - Browser 路由：`http://127.0.0.1:8000/task-hall`。
  - 大图打开：双击主图后 `.review-lightbox` 出现，图片自然尺寸 `810x1080`，初始 transform 为 `translate(0px, 0px) scale(1)`。
  - 大图缩放：按 `+` 后比例为 `125%`，transform 为 `scale(1.25)`。
  - 大图拖动：放大后拖动，transform 变为 `translate(90px, 50px) scale(1.25)`。
  - 适屏：按 `0` 后比例回到 `适屏`，transform 回到 `translate(0px, 0px) scale(1)`。
  - 大图分类：按 `4` 后第 2 张照片分类改为 `表箱整体改造后`，大图分类按钮同步 active。
  - 当前图归档：按 `Enter` 后第 2 张照片保持 `表箱整体改造后`，选中照片自动移动到第 3 张未分类图。
  - 关闭大图：按 `Esc` 后 `.review-lightbox` 消失。
  - 方向键：`ArrowRight` 选中第 4 张，`ArrowLeft` 回到第 3 张，`ArrowDown` 切到下一资料组，`ArrowUp` 回到上一资料组，active 卡片均在可视区域。
  - 移动端：临时视口 `390 x 844`，大图分类按钮宽度约 `175px` 两列排列，无横向溢出，图片自然尺寸 `810x1080`。
  - 应用控制台：error/warn 为空。
- 命令：
  - `.\node_modules\.bin\vue-tsc.cmd --noEmit`
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`
  - `python scripts\verify_vue_migration_gate.py --strict-native`
  - `.\.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`
- 结果：
  - `vue-tsc` 通过。
  - Vue build 通过。
  - Vue migration gate 通过。
  - `test_api.py` 通过：`42 passed, 1 warning`。
- 矩阵更新：
  - `Lightbox zoom/pan/fit/close` 改为非阻断。
  - `Keyboard 1-4, arrows, Enter, Esc` 改为非阻断。
  - `Archive current photo` 改为非阻断。

### 审阅工作台 P0：补图压缩上传对齐静态 HTML

- 时间：2026-06-21
- 版本目标：V2.4.x Vue 全量对齐
- 静态基准：
  - `v2-api/app/static/task_hall.html`
  - 静态页 `uploadInlinePhotos()` 会先通过 `ModuleImageUpload.compressImageFiles()` 压缩补图，再提交 `/local-test/groups/{group_id}/photos/upload-images`。
- 修改文件：
  - `v2-web/src/views/TaskHallView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
- 目标：
  - Vue 审阅页“补图”上传前先在浏览器本地压缩图片，降低移动端卡顿、上传耗时和大图失败概率。
  - 不改后端 API，不改资料组字段，不改变静态 HTML 已有业务规则。
- 实现：
  - 新增 `compressSupplementImage(file)`。
  - 非图片和 GIF 保持原文件。
  - 普通图片按最长边 `1600px`、JPEG `0.78` 质量压缩。
  - 如果压缩后不比原 JPEG 小，则保留原文件。
  - `uploadPhotos()` 改为上传压缩后的文件数组。
- 本地验证：
  - `.\node_modules\.bin\vue-tsc.cmd --noEmit`：通过。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：通过，`42 passed, 1 warning`。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过，仅保留第三方 PURE 注释和大 chunk 警告。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/task-hall`
  - 页面身份：标题 `模块更换项目管理器 V2.4.9`，DOM 为 Vue 审阅工作台，非静态 HTML。
  - 本地测试团队：`demo-team`。
  - 测试资料组：`g-07829` / 表号 `110020602797`。
  - 使用同一补图 API 上传临时测试图后，资料组从 `4 张` 增至 `5 张`。
  - 浏览器搜索 `110020602797` 并点击目标组后，照片条显示 `5` 张，新增本地上传图可加载。
  - 桌面端：页面非空、无框架错误覆盖层、无横向溢出、控制台 `error/warn` 为空。
  - 手机端 `390 x 844`：目标组显示 `5 张`，照片条 `5` 张，主图 `810 x 1080` 完整加载，无横向溢出。
- 矩阵更新：
  - `Supplement photos` 改为非阻断。
- 说明：
  - Browser 插件当前未暴露文件选择器上传能力，本轮浏览器无法直接模拟点击 `<input type="file">` 选择本地文件；已通过同一后端补图 API 写入本地测试库，再用 Vue 页面验证刷新展示。
  - 旧 OSS 缩略图在本地未配置完整 OSS 访问时可能显示为 0 尺寸；本轮新增的本地补图可以正常显示，OSS 图完整性另属图片代理/OSS 配置专项。

### 审阅工作台 P0：保存表号、采集器、模块号对齐静态 HTML

- 时间：2026-06-21
- 版本目标：V2.4.10
- 静态基准：
  - `v2-api/app/static/task_hall.html`
  - 静态页 `saveInlineMetadata()` 通过 `/local-test/groups/{group_id}/metadata` 保存表号、采集器和模块号。
- 修改文件：
  - `v2-api/tests/test_api.py`
  - `v2-web/index.html`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 目标：
  - 修复版本号门禁残留：代码版本已升到 `V2.4.10`，测试和 HTML 标题同步更新。
  - 验证 Vue 审阅页“保存资料组”与静态 HTML 元数据保存能力一致。
- 验证数据：
  - 本地测试库：`demo-team`
  - 任务：`67 / 350000434929`
  - 资料组：`g-07818 / 110023771246`
  - 原值：采集器 `3130009381930000109822`，模块 `3130054512250026163713`
  - 临时值：采集器 `QA-COL-228131`，模块 `QA-MOD-228131`
- Browser 验证：
  - URL：`http://127.0.0.1:8000/task-hall?qa=metadata-v2410`
  - 页面标题：`模块更换项目管理器 V2.4.10`
  - 页面为 Vue 审阅工作台，非空、无框架错误覆盖层。
  - 选择资料组 `110023771246` 后，三个元数据输入框正常显示。
  - 修改采集器和模块号后点击 `保存资料组`，UI 输入框保持新值。
  - API 读回 `/local-test/groups/g-07818`，`collector` 和 `module_asset_no` 以及照片索引字段均写入新值。
  - 再通过 Vue 保存按钮恢复原值，并用 API 确认服务端恢复原值。
  - 控制台 `error/warn` 为空。
- 门禁命令：
  - `vue-tsc --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过，保留第三方 PURE 注释和大 chunk 警告。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：通过，`42 passed, 1 warning`。
- 矩阵更新：
  - `Save meter/collector/module` 改为非阻断。
- 状态：完成。
### 审阅工作台 P0：删除当前图对齐静态 HTML

- 时间：2026-06-21
- 版本目标：V2.4.10
- 静态基准：
  - `v2-api/app/static/task_hall.html`
  - 静态页 `deletePhoto()` 删除当前照片后，会重新加载当前资料组、优先选中相邻照片、后台刷新任务统计，并提示删除成功。
- 修改文件：
  - `v2-web/src/views/TaskHallView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 修改内容：
  - Vue 删除当前图后保留 `loadGroup(groupId, preferredPhotoId)` 的相邻照片选中逻辑。
  - 将删除后的完整 `loadTasks() + loadGroups()` 改为 `refreshTasksSilently() + refreshGroupsSilently()`，避免后台刷新重新选择资料组并打断审阅焦点。
  - 保持后端 DELETE `/local-test/groups/{group_id}/photos/{photo_id}` 协议不变。
- 本地 QA 数据：
  - 环境：本地 `demo-team`，未触碰生产库。
  - 资料组：`g-07818 / 110023771246`。
  - 先通过补图 API 加入两张 QA 临时图，照片数从 4 变为 6。
  - 第一次浏览器删除第 6 张后，API 确认为 5 张；发现 Vue 后台完整刷新会把选中图重置到第 3 张。
  - 修复后重新构建，第二次浏览器删除剩余第 5 张，UI 即时变为 4 张，并选中相邻第 4 张。
  - API 复核 `GET /local-test/groups/g-07818`：`photo_count=4`，QA 临时照片已清理回原始状态。
- 浏览器验证：
  - URL：`http://127.0.0.1:8000/task-hall?qa=delete-v2410`
  - 页面标题：`模块更换项目管理器 V2.4.10`
  - 第 6 张删除前：照片条 6 张，目标图 `p-g-07818-37300a13b781`。
  - 第 6 张删除后：API 变为 5 张。
  - 第 5 张删除后：UI 变为 4 张，资料组卡片显示 `待审阅 / 4 张`，选中照片为 `#4 未分类`，toast 显示 `已删除当前图`。
  - 控制台：无应用相关 `error/warn`。
- 验证命令：
  - `v2-web\node_modules\.bin\vue-tsc.CMD --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过，仅保留第三方 PURE 注释和大 chunk 警告。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：通过，`42 passed, 1 warning`。
- 状态：完成；`Delete photo` 矩阵项改为非阻断。
### 审阅工作台 P0：恢复待审对齐静态 HTML

- 时间：2026-06-21
- 版本目标：V2.4.10
- 静态基准：
  - `v2-api/app/static/task_hall.html`
  - 静态页 `restoreInlinePending()` 会按照片数量恢复为 `pending/incomplete`，发送 `note=恢复待审` 和空 `exception_note`，刷新当前资料组、任务统计并提示恢复成功。
- 修改文件：
  - `v2-web/src/api/services.ts`
  - `v2-web/src/views/TaskHallView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 实现：
  - `saveReview()` 增加可选 `note` 和 `exceptionNote` 参数，保留原调用兼容。
  - Vue `restorePending()` 发送 `{ note: '恢复待审', exception_note: '' }`，按照片数恢复 `pending/incomplete`。
  - 恢复后同步 `activeGroup`、资料组列表、异常表单草稿，并静默刷新任务和资料组统计。
- 本地 QA 数据：
  - 团队：`demo-team`
  - 任务：`67 / 350000434929`
  - 资料组：`g-07818 / 110023771246`
  - 先通过 API 将资料组置为 `exception`，并写入 `exception_note=QA restore pending setup`。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/task-hall?qa=restore-pending-v2410`
  - 页面标题：`模块更换项目管理器 V2.4.10`
  - 搜索 `110023771246` 后，目标卡片显示 `异常 / 4 张`。
  - 点击 `恢复待审` 后，目标卡片变为 `待审阅 / 4 张`。
  - 内联异常说明输入从 `QA restore pending setup` 清空为空。
  - 页面 toast 显示 `已恢复为待审`。
  - 控制台无应用相关 `error/warn`。
- API 验证：
  - `GET /local-test/groups/g-07818` 返回 `status=pending`、`photo_count=4`、`exception_note=""`。
- 命令验证：
  - `v2-web/.\\node_modules\\.bin\\vue-tsc.cmd --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\\build-vue-shell.ps1`：通过，保留第三方 PURE 注释和大 chunk 警告。
  - `python scripts\\verify_vue_migration_gate.py --strict-native`：通过。
  - `.venv\\Scripts\\python.exe -m pytest v2-api\\tests\\test_api.py -q`：通过，`42 passed, 1 warning`。
- 矩阵更新：
  - `Restore pending` 改为非阻断。
- 状态：完成。
### 审阅工作台 P0：回退未施工对齐静态 HTML

- 时间：2026-06-21
- 版本目标：V2.4.10
- 静态基准：
  - `v2-api/app/static/task_hall.html`
  - 静态页 `resetInlineUnconstructed()` 会确认后调用 `/local-test/groups/{group_id}/reset-unconstructed`，软清空当前资料组照片、采集器和模块号，保留当前无图资料组审阅面板，并后台刷新任务和资料组统计。
- 修改文件：
  - `v2-web/src/views/TaskHallView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 实现：
  - Vue `resetUnconstructed()` 不再调用完整 `loadGroups()` 跳到第一个可审阅组。
  - 回退成功后保留当前资料组为 active，清空 `photos` 和 `selectedPhotoId`。
  - 同步元数据/异常表单草稿和资料组列表条目。
  - 通过 `refreshTasksSilently()` 和 `refreshGroupsSilently()` 后台刷新统计，避免审阅焦点被重置。
- 本地 QA 数据：
  - 团队：`demo-team`
  - 任务：`67 / 350000434929`
  - 资料组：`g-07817 / 110020602801`
  - 先通过补图 API 为本地未施工组补入样本照片，形成 `待审阅 / 3 张` 的可回退状态。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/task-hall?qa=reset-unconstructed-v2410`
  - 页面标题：`模块更换项目管理器 V2.4.10`
  - 搜索 `110020602801` 后，目标卡片显示 `待审阅 / 3 张`。
  - 点击 `回退未施工` 并确认后，目标卡片显示 `未施工 / 0 张`。
  - 审阅主图消失，审阅区显示 `未施工资料组` 说明。
  - 控制台无应用相关 `error/warn`。
- API 验证：
  - `GET /local-test/groups/g-07817` 返回 `status=pending`、`photo_count=0`、`collector=null`、`module_asset_no=null`、`construction_collector=null`、`construction_module_asset_no=null`、`photos=[]`。
- 命令验证：
  - `v2-web/.\\node_modules\\.bin\\vue-tsc.cmd --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\\build-vue-shell.ps1`：通过，保留第三方 PURE 注释和大 chunk 警告。
  - `python scripts\\verify_vue_migration_gate.py --strict-native`：通过。
  - `.venv\\Scripts\\python.exe -m pytest v2-api\\tests\\test_api.py -q`：通过，`42 passed, 1 warning`。
- 矩阵更新：
  - `Reset to unconstructed` 改为非阻断。
- 状态：完成。
### 审阅工作台 P0：转异常工单对齐静态 HTML

- 时间：2026-06-21
- 版本目标：V2.4.10
- 静态基准：
  - `v2-api/app/static/task_hall.html`
  - 静态页 `markInlineException()` 调用 `/local-test/groups/{group_id}/return-exception`，使用异常说明或已有问题原因作为备注，生成施工异常工单，保留当前资料组，并后台刷新任务和资料组统计。
- 修改文件：
  - `v2-web/src/api/types.ts`
  - `v2-web/src/api/services.ts`
  - `v2-web/src/views/TaskHallView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 实现：
  - `MaterialGroup` 增加 `exceptionReasons`。
  - `BackendGroup` 映射 `exception_reasons`，保留后端已有异常原因。
  - Vue `markException()` 的备注兜底改为：输入说明 -> 资料组已有异常原因/异常说明 -> `人工标记异常`。
  - 生成异常工单后同步当前资料组、异常表单草稿和资料组列表条目，并静默刷新任务/资料组统计。
- 本地 QA 数据：
  - 团队：`demo-team`
  - 任务：`67 / 350000434929`
  - 资料组：`g-07817 / 110020602801`
  - 使用刚完成回退未施工后的无图资料组执行转异常工单验证。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/task-hall?qa=return-exception-v2410`
  - 页面标题：`模块更换项目管理器 V2.4.10`
  - 搜索 `110020602801` 后，目标卡片显示 `未施工 / 0 张`。
  - 点击 `转异常工单` 后，目标卡片显示 `异常 / 0 张`。
  - 审阅面板保留当前组，异常说明显示 `人工标记异常`。
  - 页面 toast 显示 `已生成施工异常工单`。
  - 控制台无应用相关 `error/warn`。
- API 验证：
  - `GET /local-test/groups/g-07817` 返回 `status=exception`、`photo_count=0`、`reviewer=reviewer`、`exception_note=人工标记异常`。
  - `GET /local-test/construction/exception-orders?actor=constructor&task_id=67` 返回该组新增 `open` 状态工单，`category=photo_error`、`note=人工标记异常`。
- 命令验证：
  - `v2-web/.\\node_modules\\.bin\\vue-tsc.cmd --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\\build-vue-shell.ps1`：通过，保留第三方 PURE 注释和大 chunk 警告。
  - `python scripts\\verify_vue_migration_gate.py --strict-native`：通过。
  - `.venv\\Scripts\\python.exe -m pytest v2-api\\tests\\test_api.py -q`：通过，`42 passed, 1 warning`。
- 矩阵更新：
  - `Return to exception work order` 改为非阻断。
- 状态：完成。
### 审阅工作台 P0：异常原因/说明编辑对齐静态 HTML

- 时间：2026-06-21
- 版本目标：V2.4.10
- 静态基准：
  - `v2-api/app/static/task_hall.html`
  - 静态页 `groupIssueReasons()` 会按 `exception_reasons`、`exception_note`、异常状态下的 `review_note` 合并异常原因，并去重。
  - 静态页 `renderInlineExceptionTools()` 会把上述异常原因作为 `inlineExceptionNote` 默认值，异常类型下拉默认第一项 `meter_error / 表号错误`。
  - 静态页 `markInlineException()` 使用手动输入说明，若为空再回退到已有异常原因，最后回退到 `人工标记异常`。
- 修改文件：
  - `v2-web/src/views/TaskHallView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 实现：
  - Vue 新增 `groupIssueReasons()`，按静态 HTML 的顺序合并并去重异常原因。
  - `groupIssueReasonText()` 改为复用去重后的异常原因。
  - `syncDraftsFromGroup()` 在切换资料组时把异常类型重置为静态默认 `meter_error`，异常说明预填为静态同款异常原因文本。
  - 保留已有 `markException()` 手动说明优先逻辑，编辑后的说明继续通过 `/local-test/groups/{group_id}/return-exception` 提交。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/task-hall?qa=exception-edit-v2410-after-build`
  - 页面标题：`模块更换项目管理器 V2.4.10`
  - 流程：进入审阅工作台 -> 切换 `异常 7` -> 打开 `110020602801 / 异常 / 0 张` -> 检查异常面板。
  - 异常类型显示为 `表号错误`，与静态页默认第一项一致。
  - 异常说明预填为 `photo_error；人工标记异常`，来自已有异常原因和异常说明。
  - 将异常说明改为 `QA exception note edited V2410` 后点击 `转异常工单`，页面提示 `已生成施工异常工单`，异常说明回写为 `meter_error；QA exception note edited V2410`。
  - 控制台无应用相关 `error/warn`。
- 命令验证：
  - `v2-web/.\\node_modules\\.bin\\vue-tsc.cmd --noEmit`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\\build-vue-shell.ps1`：通过，保留第三方 PURE 注释和大 chunk 警告。
  - `python scripts\\verify_vue_migration_gate.py --strict-native`：通过。
  - `.venv\\Scripts\\python.exe -m pytest v2-api\\tests\\test_api.py -q`：通过，`42 passed, 1 warning`。
- 矩阵更新：
  - `Exception reason/note editing` 改为非阻断。
- 状态：完成。
### 审阅工作台 P0：已领取任务看板、资料组列表、搜索闭环验证

- 时间：2026-06-21
- 版本目标：V2.4.10
- 静态基准：
  - `v2-api/app/static/task_hall.html`
  - 静态页 `filteredTasks()` 只显示 `has_scan_info && claimed_by === reviewer` 的终端任务。
  - 静态页 `renderGroups()` 渲染当前终端资料组，默认折叠未施工资料组，显示 `可审阅/异常/已归档/未施工` 分类和计数。
  - 静态页 `matchesGroupQuery()` 支持表号、终端、地址、审阅人、采集器、模块号搜索，且搜索时可打开默认折叠的未施工资料组。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 实现状态：
  - 本次未改 Vue 代码；只对已有 Vue 实现做静态基准对照和浏览器验证。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/task-hall?qa=exception-edit-v2410-after-build`
  - 页面标题：`模块更换项目管理器 V2.4.10`
  - 已领取任务看板：左侧只显示 `终端 350000434929`，文本为 `已由 reviewer 领取`、`改造 87`、`未审 67`。
  - 默认资料组列表：全部视图显示 `79/87`，提示 `已折叠 8 个未施工资料组`，前 5 条资料组卡片显示表号、终端/地址、状态/照片数、审阅人。
  - 表号搜索：输入 `110023766221` 后显示 `1/87`，唯一结果为 `110023766221 / 异常 / 2 张`。
  - 地址搜索：输入 `71号501` 后显示 `1/87`，唯一结果为 `110020709964 / 聚丰园路188弄71号501室 / 异常 / 2 张`。
  - 终端搜索：输入 `350000434929` 后显示 `87/87`，折叠的未施工组也进入可搜索范围。
  - 控制台无应用相关 `error/warn`。
- 命令验证：
  - 复用上一项门禁结果：`vue-tsc --noEmit`、`build-vue-shell.ps1`、`verify_vue_migration_gate.py --strict-native`、`pytest v2-api/tests/test_api.py -q` 均通过。
- 矩阵更新：
  - `Claimed task board` 改为非阻断。
  - `Group list` 改为非阻断。
  - `Search meter/address/terminal` 改为非阻断。
- 状态：完成。
### 审阅工作台 P0：异常表计导出闭环验证

- 时间：2026-06-21
- 版本目标：V2.4.10
- 静态基准：
  - `v2-api/app/static/task_hall.html`
  - 静态页 `exportExceptionMeters()` 调用 `/exports/exception-meters`，请求体包含当前审阅员，并下载 `exception-meters-*.xlsx`。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 实现状态：
  - 本次未改 Vue 代码；Vue `exportExceptions()` 已调用 `exportExceptionMeters()`，服务层与静态页使用同一后端接口。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/task-hall?qa=exception-edit-v2410-after-build`
  - 点击审阅面板内 `导出异常表计`。
  - 页面显示 `异常表计已导出`。
  - 控制台无应用相关 `error/warn`。
- API 验证：
  - `POST /exports/exception-meters`，请求体 `{"reviewer":"reviewer","team_id":"default-team"}`。
  - 响应状态：`200`。
  - `content-type=application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`。
  - `content-disposition=attachment; filename="exception-meters-*.xlsx"`。
  - 文件头：`PK\x03\x04`，确认为 xlsx zip 容器。
- 矩阵更新：
  - `Export exception meters` 改为非阻断。
- 状态：完成。
### 审阅工作台 P0：后台刷新保持当前组验证

- 时间：2026-06-21
- 版本目标：V2.4.10
- 静态基准：
  - `v2-api/app/static/task_hall.html`
  - 静态页 `shouldDeferBackgroundRefresh()` 会在忙碌、加载、归档或用户刚交互时延后后台刷新。
  - 静态页 `refreshCurrentGroups(preferredGroupId, { background: true })` 会刷新组列表摘要，同时保留当前组照片并避免重置审阅焦点。
  - 静态页每 10 秒调度一次外部刷新。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 实现状态：
  - 本次未改 Vue 代码；Vue `externalRefresh()`、`refreshTasksSilently()`、`refreshGroupsSilently()` 已按静态思路保留当前任务、当前组和当前照片。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/task-hall?qa=exception-edit-v2410-after-build`
  - 设置资料组搜索为 `110023766221`，结果计数为 `1/87`。
  - 打开唯一结果 `110023766221 / 聚丰园路188弄69号501室 / 异常 / 2 张`。
  - 刷新前：搜索词 `110023766221`，活动组为该异常组，图片位置 `1/2`。
  - 等待 11.5 秒，覆盖 10 秒后台刷新周期。
  - 刷新后：搜索词仍为 `110023766221`，计数仍为 `1/87`，活动组仍为该异常组，图片位置仍为 `1/2`。
  - 页面控制台无应用相关 `error/warn`。浏览器插件自身 Statsig 网络超时为工具侧日志，不属于应用日志。
- 矩阵更新：
  - `Background refresh preserving active group` 改为非阻断。
- 状态：完成。
### 施工采集 P0：任务模式、工作区、缓存筛选和本地缓存验证

- 时间：2026-06-21
- 版本目标：V2.4.x
- 静态基准：
  - `v2-api/app/static/construction.html`
  - 静态页具备任务选择模式、施工工作区模式、返回任务区、`全部/未施工/已缓存/异常工单` 筛选、施工页一键上传完整缓存、IndexedDB 本地草稿缓存。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 本次说明：
  - 本轮未改 Vue 业务代码；对已存在的 `ConstructionView.vue` 实现做静态基准对照和浏览器闭环验证。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/construction?qa=construction-p0-verify`
  - 页面标题：`模块更换项目管理器 V2.4.10`
  - 管理员任务选择模式：显示终端 `350000434929`，显示已指派 `constructor`、改造数、已上传、未施工、异常、进度和管理员操作按钮。
  - 点击终端任务后进入施工工作区：任务面板隐藏，左侧资料组列表和右侧采集表单显示；筛选计数为 `全部8 / 未施工8 / 已缓存0 / 异常工单0`。
  - 选中资料组 `120020467667` 后，采集表单显示采集器、模块号、照片槽位、`保存缓存` 和 `上传当前组`。
  - 点击 `保存缓存` 后，筛选计数变为 `全部8 / 未施工7 / 已缓存1 / 异常工单0`。
  - 点击 `已缓存1` 后，只显示已缓存资料组 `120020467667`，并显示 `1 个本地缓存 / 一键上传`。
  - 当前草稿照片和必填项不完整，`一键上传` 按钮保持禁用，符合静态页只上传完整缓存的规则。
  - 点击 `返回任务区` 后，资料组和采集表单隐藏，终端任务卡片重新显示。
  - 刷新后重新登录本地管理员账号，再进入施工页同一终端，仍显示 `已缓存1`，证明缓存已进入本地持久化缓存而不是临时内存。
  - 控制台无应用相关 `error/warn`。
  - 桌面视口无横向溢出。
- 矩阵更新：
  - `Task selection mode` 改为非阻断。
  - `Construction work mode` 改为非阻断。
  - `Return to task view` 改为非阻断。
  - `Filters: all/unconstructed/cached/exception` 改为非阻断。
  - `Upload all cached drafts on construction page` 改为非阻断。
  - `IndexedDB cache` 改为非阻断。
- 状态：完成。
### 施工采集 P0：必填模块号与必填照片校验

- 时间：2026-06-21
- 版本目标：V2.4.x
- 静态基准：
  - `v2-api/app/static/construction.html`
  - 静态页要求模块资产编号必填，且 `改造前照片`、`模块与电表照片`、`改造后照片` 为必填照片；未满足时不能上传当前资料组。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 本次说明：
  - 本轮未改 Vue 业务代码；对已存在的施工采集校验做浏览器闭环验证。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/construction?qa=cache-persist-after-login`
  - 页面标题：`模块更换项目管理器 V2.4.10`
  - 选中未施工资料组 `110023765004 / 聚丰园路188弄52号101室`。
  - 空表单状态下，采集器可选、模块资产编号为空、四个照片槽位均为空。
  - 页面显示 `模块资产编号必填。缺少：改造前照片、模块与电表照片、改造后照片`。
  - `上传当前组` 按钮处于 disabled 状态，无法提交空资料组。
  - `保存缓存`、采集器/模块扫码按钮和照片 `拍照/相册` 操作仍可见。
  - 控制台无应用相关 `error/warn`。
- 矩阵更新：
  - `Required module/before/module-after/after validation` 改为非阻断。
- 状态：完成。
### 施工采集 P0：扫码手动输入兜底验证

- 时间：2026-06-21
- 版本目标：V2.4.x
- 静态基准：
  - `v2-api/app/static/construction.html`
  - 静态页扫码失败后需要提供拍照识别和手动输入兜底，避免现场无法使用实时摄像头识别时卡死。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 本次说明：
  - 本轮未改 Vue 业务代码；只验证已存在的扫码兜底路径。
  - 未接受任何摄像头权限，也未做真实移动设备扫码；实时摄像头扫码仍保留为移动端设备 QA 项。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/construction?qa=cache-persist-after-login`
  - 当前资料组：`110023765004 / 聚丰园路188弄52号101室`
  - 点击模块号旁边的 `扫码` 后，扫码层显示 `扫描模块号`。
  - 当前桌面环境实时扫码不可用时，扫码层显示 `拍照识别` 和 `手动输入` 两个兜底按钮。
  - 点击 `手动输入` 后出现 `扫描模块号 / 请输入编号` 弹窗。
  - 输入 `QA-MODULE-SCAN` 并按 Enter 后，弹窗关闭，模块号输入框回填为 `QA-MODULE-SCAN`，页面提示 `已填入 QA-MODULE-SCAN`。
  - QuaggaJS 输出了两条视频流初始化 warn，属于第三方库调试日志；页面无应用相关 `error`。
- 矩阵更新：
  - `Manual scan input fallback` 改为非阻断。
- 状态：完成。
### 施工采集 P0：异常工单提交矩阵状态复核

- 时间：2026-06-21
- 版本目标：V2.4.x
- 静态基准：
  - `v2-api/app/static/construction.html`
  - 异常工单需在施工采集端可处理，补充或修正后进入缓存/上传流程，上传成功后工单可进入 resolved。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 本次说明：
  - 本轮未改 Vue 业务代码；根据已有工作记录和当前 API 状态复核矩阵阻断状态。
- 证据：
  - `docs/V2_CHANGE_WORKLOG.md` 已有 `施工采集 P0：筛选逻辑与异常工单缓存对齐` 记录：异常工单只在异常筛选中显示，保存异常工单缓存后进入已缓存筛选，缓存上传页可上传并删除异常工单缓存。
  - 当前本地 API `GET /local-test/construction/exception-orders?actor=constructor&task_id=67` 返回 3 条工单，其中包含 open/resolved 状态工单。
  - 当前本地 API `GET /local-test/construction/tasks/67/groups?actor=constructor` 返回该终端普通未施工组，说明异常工单没有混入普通未施工资料组。
- 矩阵更新：
  - `Exception work order submit` 改为非阻断。
- 状态：完成。
### 施工采集 P0：图片压缩链路源码对齐

- 时间：2026-06-21
- 版本目标：V2.4.x
- 静态基准：
  - `v2-api/app/static/construction.html`
  - `v2-api/app/static/image_upload_utils.js`
  - 静态页选择照片后通过 `ModuleImageUpload.compressImageFile()` 压缩，再写入本地草稿，上传时使用草稿中的图片。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 本次说明：
  - 本轮未改 Vue 业务代码；由于当前 Browser API 不支持直接给隐藏 file input 注入本地图片，本项用源码路径对齐作为证据。
- 源码证据：
  - Vue `ConstructionView.vue` 的 `pickFile(slotKey, event)` 在读取原始文件后立即调用 `compressImageFile(rawFile)`。
  - Vue `compressImageFile()` 使用 canvas 将图片最长边限制为 `1600`，输出 `image/jpeg`，质量 `0.78`，并返回新的 `.jpg` File。
  - Vue `pickFile()` 将压缩后的 File 写入 `selectedFiles`，生成预览 URL，并调用 `saveCurrentDraft({ silent: true })`。
  - Vue `buildCurrentDraft()` 从 `selectedFiles` 构造草稿照片；`uploadDraft()` 再通过 `fileFromDraftPhoto()` 使用草稿中的 File 上传。
  - 静态 HTML 同样在 `handlePhotoFile()` 中调用压缩工具后写入本地草稿，再由 `uploadCurrentDraft()` 上传草稿图片。
- 验证边界：
  - 已完成源码链路对齐；真实移动端相机/相册选择仍需设备 QA。
- 矩阵更新：
  - `Image compression before cache/upload` 改为非阻断。
- 状态：完成。
### 施工采集 P0：异常工单已有照片按槽位显示

- 时间：2026-06-21
- 版本目标：V2.4.x
- 静态基准：
  - `v2-api/app/static/construction.html`
  - 异常工单进入施工采集后，如果资料组已有已分类照片，应在对应照片槽位显示，避免施工员重复上传。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 本地 QA 数据：
  - 仅在本地测试库中，用管理员 JWT 将 `demo-team` 的资料组 `g-07818 / 110023771246` 转为异常工单。
  - 该资料组已有 4 张测试照片，其中 `collector_barcode` 和 `after_box` 有明确分类。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/construction?qa=existing-slot-constructor`
  - 账号：`constructor / construct123`
  - 施工员进入终端 `350000434929` 后，`异常工单` 筛选计数为 `4`。
  - 打开异常工单 `g-07818 / photo_error / QA existing photo slot parity`。
  - 采集表单显示 `异常工单处理`，并显示地址 `聚丰园路188弄52号402室`。
  - `采集器照片` 槽位显示 `系统已有`，图片加载完成，natural size 为 `810 x 1080`。
  - `改造后照片` 槽位显示 `系统已有`，图片加载完成，natural size 为 `810 x 1080`。
  - `改造前照片` 和 `模块与电表照片` 仍为空，页面保留缺失提示：`模块资产编号必填。缺少：改造前照片、模块与电表照片`。
  - 页面无应用相关 `error`；QuaggaJS 视频流 warn 属第三方扫码库调试日志。
- 矩阵更新：
  - `Existing photos shown by slot` 改为非阻断。
- 状态：完成。
### 施工采集 P0：扫码、拍照、相册设备能力对齐复核

- 时间：2026-06-21
- 版本目标：V2.4.x
- 静态基准：
  - `v2-api/app/static/construction.html`
  - 静态页施工采集支持表号/采集器/模块号扫码，扫码失败后有拍照识别和手动输入兜底；照片槽位同时提供拍照和相册选择。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 本次说明：
  - 本轮未改 Vue 业务代码；按源码路径和已完成的浏览器兜底验证复核设备能力对齐状态。
  - 当前桌面 Browser 不能完整验证真实手机摄像头、系统相册选择器和现场条码识别，所以这些仍保留为发布前真实移动设备 QA 项，但不再作为 Vue 功能迁移阻断项。
- 证据：
  - 表号扫码直接打开施工单此前已在矩阵中记录为浏览器验证通过。
  - 模块号扫码点击后，桌面环境实时扫码不可用时显示 `拍照识别` 和 `手动输入`；手动输入 `QA-MODULE-SCAN` 后可回填模块号。
  - `ConstructionView.vue` 中采集器、模块号、表号均通过 `startScanner()` 进入同一扫码流程，并由 `applyScanValue()` 按目标字段回填。
  - Vue 扫码流程包含 QuaggaJS 加载、BarcodeDetector 原生识别、拍照识别和手动输入 fallback。
  - 每个照片槽位均显示 `拍照` 和 `相册`；源码中 `拍照` input 使用 `capture="environment"`，`相册` input 不带 capture，二者都进入 `pickFile()`，再进入压缩与缓存链路。
- 矩阵更新：
  - `Collector/module/meter scan` 改为非阻断。
  - `QuaggaJS + BarcodeDetector fallback` 改为非阻断。
  - `Camera and album photo selection` 改为非阻断。
- 风险保留：
  - 发布前仍需用真实手机浏览器或小程序体验版确认摄像头权限、对焦、条形码识别率、系统相册选择和弱网缓存体验。
- 状态：完成。

### 独立异常处理页取消并删除

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户规则：
  - 异常处理页已合并至审阅工作台。
  - 独立 `/unmatched` 页面直接删除。
  - 异常资料组处理、补图、回退未施工、转异常工单、保存资料组等操作统一在审阅工作台完成。
- 修改文件：
  - `v2-web/src/router/index.ts`
  - `v2-web/src/router/staticPages.ts`
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/views/TaskHallView.vue`
  - `v2-web/src/views/UnmatchedView.vue`
  - `v2-api/app/main.py`
  - `v2-api/tests/test_api.py`
  - `scripts/verify_vue_migration_gate.py`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_4X_VUE_FULL_PARITY_EXECUTION_PLAN.md`
- 改动内容：
  - 移除 Vue `unmatched` 动态路由。
  - 移除顶部导航中的独立 `异常处理` 入口。
  - 删除 `UnmatchedView.vue` 独立页面文件。
  - 后端 `/unmatched` 旧地址改为重定向到 `/task-hall?workspace=exceptions`。
  - 前端 `/app?page=unmatched` 旧入口改为重定向到 `/task-hall?workspace=exceptions`。
  - 审阅工作台删除嵌入 `/unmatched` 的 iframe，改为内置异常导出面板。
  - Vue 迁移门禁将 `unmatched.html` 标记为产品取消页面，不再作为 Vue 对齐阻断项。
- 验证：
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面数为 5。
  - `node node_modules\vue-tsc\bin\vue-tsc.js --noEmit`：通过。
  - `node node_modules\vite\bin\vite.js build`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过；仅保留既有第三方 PURE 注释与大 chunk 警告。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：`42 passed, 1 warning`。
  - HTTP 验证：`/unmatched` 和 `/unmatched?embedded=1` 返回 `307`，`Location=/task-hall?workspace=exceptions`。
  - Browser 验证：登录本地演示账号后直接访问 `/unmatched`，最终落到 `/task-hall?workspace=exceptions`；顶部导航仅显示 `项目看板 / 任务领取 / 审阅工作台 / 施工采集`；无独立 `异常处理`、无 `缓存上传`、无 iframe；控制台无 error/warn。
  - Browser 验证：旧入口 `/app?page=unmatched` 同样最终落到 `/task-hall?workspace=exceptions`；无独立 `异常处理`、无 iframe；控制台无 error/warn。
- 状态：完成。

### 任务领取：定时刷新与跨页刷新对齐

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 静态基准：
  - `v2-api/app/static/claim_tasks.html`
  - 静态任务领取页会定时刷新任务列表，并监听跨页面数据刷新消息。
- 修改文件：
  - `v2-web/src/views/ClaimTasksView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - 任务领取页挂载后每 10 秒自动刷新任务列表。
  - 监听 `module-manager:data-refresh` 消息，收到后 180ms 防抖刷新。
  - 页面卸载时清理定时器和消息监听，避免重复刷新或内存残留。
  - 对齐矩阵中 `Scheduled refresh` 改为非阻断项。
- 验证：
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面数为 5，旧独立页面不作为 Vue 对齐目标。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：`42 passed, 1 warning`。
  - `.venv\Scripts\python.exe scripts\smoke-client-demo.py`：通过，旧 `/unmatched` 与 `/construction-cache` 均验证为取消页重定向。
  - `node node_modules\vue-tsc\bin\vue-tsc.js --noEmit`：通过；本机 shell 未直接暴露 `node`，实际使用 Codex bundled Node 执行。
  - `node node_modules\vite\bin\vite.js build`：通过；仅保留既有第三方 PURE 注释和 chunk 体积警告。
- 状态：完成。

### 登录页：团队标识、登录配置与 Enter 登录对齐

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 静态基准：
  - `v2-api/app/static/login.html`
  - 静态登录页包含账号、团队标识、密码；读取 `/auth/config` 展示演示账号或正式登录提示；点击预设账号可填充表单；密码框按 Enter 可登录；登录后写入会话并进入工作台。
- 修改文件：
  - `v2-web/src/views/LoginView.vue`
  - `v2-web/src/api/services.ts`
  - `v2-web/src/api/types.ts`
  - `v2-web/src/stores/auth.ts`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - Vue 登录页新增 `团队标识` 输入框，默认读取 `module_manager_team_id`，无值时使用 `default-team`。
  - 新增 `fetchAuthConfig()` 服务，读取 `/auth/config` 并在页面展示登录配置。
  - 当演示账号开启时，渲染管理员、审阅员、施工员预设账号；点击预设后填充账号、团队、密码。
  - 当演示账号关闭时，显示正式登录提示，不硬编码演示密码。
  - `auth.login()` 和底层 `services.login()` 增加显式 `teamId` 参数，登录请求头与请求体均携带当前团队标识。
  - 密码输入框增加显式 `Enter` 登录处理，避免 Element Plus 表单无法触发原生 submit。
  - 构建产物同步刷新到 `v2-api/app/static/vue/`，确保本地 FastAPI 预览使用最新 Vue 登录页。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/login?qa=login-parity`
  - 页面身份：标题 `模块更换项目管理器 V2.4.10`，DOM 包含 `登录工作台`。
  - 页面非空且无框架错误遮罩。
  - 控制台：无 `error` / `warn`。
  - DOM 验证：显示 `团队标识`、`登录配置`、`管理员 admin demo-team`、`审阅员 reviewer demo-team`、`施工员 constructor demo-team`。
  - 交互验证：点击 `审阅员 reviewer demo-team` 后，表单填充 `reviewer / demo-team / review123`。
  - 交互验证：在密码框按 Enter 后跳转到 `/project-board`，页面显示 `项目看板` 与 `审阅员 / 资料审阅员`。
  - 持久化验证：刷新 `/project-board` 后仍停留在项目看板，不返回登录页。
- 命令验证：
  - `node node_modules\vue-tsc\bin\vue-tsc.js --noEmit`：通过；实际使用 Codex bundled Node 执行。
  - `node node_modules\vite\bin\vite.js build`：通过；仅保留既有第三方 PURE 注释和 chunk 体积警告。
- 矩阵更新：
  - `Team id input` 改为非阻断。
  - `/auth/config display/preset accounts` 改为非阻断。
  - `Enter key login` 改为非阻断。
  - `Persist login state` 改为非阻断。
- 状态：完成。

### 应用壳与任务领取：角色导航和领取释放闭环验证

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 静态基准：
  - `v2-api/app/static/app_shell.html`
  - `v2-api/app/static/claim_tasks.html`
  - 用户补充规则：独立 `缓存上传` 和独立 `异常处理` 页面已取消，不得在顶部导航中恢复。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 本次说明：
  - 本轮未改 Vue 业务代码；对已实现的应用壳和任务领取基础功能做浏览器闭环验证，并据实更新矩阵阻断状态。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/login?qa=role-nav`
  - 管理员登录：`管理员 admin demo-team`，进入 `/project-board`，顶部导航显示 `项目看板 / 任务领取 / 审阅工作台 / 施工采集`，不显示 `缓存上传`、`异常处理`、`导入配置`。
  - 审阅员登录：`审阅员 reviewer demo-team`，进入 `/project-board`，顶部导航显示 `项目看板 / 任务领取 / 审阅工作台`，不显示 `施工采集`、`缓存上传`、`异常处理`。
  - 施工员登录：`施工员 constructor demo-team`，进入 `/construction`，顶部导航只显示 `施工采集`。
  - 三类账号验证期间控制台无 `error` / `warn`。
  - URL：`http://127.0.0.1:8000/claim-tasks?qa=claim-basic`
  - 审阅员任务领取页显示概览卡：`已领取任务 / 我的任务 / 改造总数 / 未审阅数 / 审阅进度`。
  - 审阅员任务领取页显示 1 个可见终端任务卡，包含终端、改造数量、已上传、未审阅和领取状态。
  - 可逆交互验证：点击当前终端 `领取` 后，状态变为 `我已领取`，按钮变为 `继续持有`；点击 `暂存释放` 后，状态回到 `可领取`，释放按钮恢复禁用。
- 矩阵更新：
  - `Unified navigation` 改为非阻断。
  - `Role-based page visibility` 改为非阻断。
  - `Claim overview` 改为非阻断。
  - `Terminal task list` 改为非阻断。
  - `Claim task` 改为非阻断。
  - `Release task` 改为非阻断。
- 状态：完成。

### 取消页残留入口：旧静态异常处理/缓存上传链接清理

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户规则：
  - 独立缓存上传页已合并至施工采集页，不再单独开页。
  - 独立异常处理页已合并至审阅工作台，不再单独开页。
  - 静态 HTML 隐藏或不可达功能默认视为取消，不应作为 Vue 补齐目标。
- 修改文件：
  - `v2-api/app/static/app_shell.html`
  - `v2-api/app/static/claim_tasks.html`
  - `v2-api/app/static/construction.html`
  - `v2-api/app/static/project_board.html`
  - `v2-api/app/static/sync_config.html`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - 移除旧静态壳中的 `缓存上传` 页面按钮，以及 `unmatched`、`construction-cache` 独立页面配置。
  - 移除旧静态任务领取、施工采集、项目看板、同步配置里的独立 `异常处理` 链接。
  - 移除旧静态施工采集里的独立 `缓存上传` 链接。
  - 保留后端旧地址重定向：`/unmatched` 仍进入审阅工作台异常模式，`/construction-cache` 仍进入施工采集，避免历史链接直接报错。
- 验证：
  - 源码检索：旧静态/Vue/矩阵中不存在可点击的 `/unmatched`、`/app?page=unmatched`、`/app?page=construction-cache`、可见 `异常处理` 或 `缓存上传` 导航入口。
  - Browser 验证：访问 `http://127.0.0.1:8000/unmatched?qa=cancel-exception`，最终落到 `/task-hall?workspace=exceptions`；顶部导航不显示独立 `异常处理` 或 `缓存上传`。
  - Browser 验证：访问 `http://127.0.0.1:8000/construction-cache?qa=cancel-cache`，最终落到 `/construction`；顶部导航不显示独立 `异常处理` 或 `缓存上传`。
  - Browser 控制台：两个验证路径均无 `error` / `warn`。
  - 对齐矩阵已标记独立缓存上传页和独立异常处理页为产品取消项，且注明旧静态入口已清理。
- 状态：完成。

### 独立异常处理页 iframe 残留删除

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户规则：
  - 异常处理页已经合并至审阅工作台，独立页面可以删除。
  - 静态 HTML 隐藏、无入口、不可达功能默认取消，不再作为 Vue 对齐目标。
- 修改文件：
  - `v2-api/app/static/task_hall.html`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - 删除静态审阅工作台内指向 `/unmatched?embedded=1` 的 iframe 残留。
  - 删除 `exceptionFrameSrc()`、`postExceptionRefresh()` 和 `exceptionFrameLoaded` 状态，避免审阅工作台再隐式加载独立异常页。
  - 将“异常导出”页签改为审阅工作台内置卡片，只保留 `导出异常表计 Excel` 和 `返回照片审阅`。
  - 保留 `/unmatched` 后端旧地址重定向到 `/task-hall?workspace=exceptions`，用于兼容历史链接，不再作为真实页面。
- 验证目标：
  - 源码中不再存在 `exceptionFrame`、`postExceptionRefresh`、`/unmatched?embedded`。
  - 独立异常处理页面不再被 iframe 或导航入口引用。
- 验证：
  - 源码检索：`exceptionFrame`、`exceptionFrameSrc`、`postExceptionRefresh`、`/unmatched?embedded` 均无业务源码匹配。
  - `python scripts\verify-static-pages.py`：通过。
  - `python scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面数 5，legacy bridge pages 0。
  - `pytest v2-api\tests\test_api.py -q`：`43 passed, 1 warning`。
  - `vue-tsc --noEmit`：通过，使用 Codex bundled Node。
  - `vite build`：通过，保留既有第三方 PURE 注释和 chunk 体积警告。
  - Browser/Chrome 验证：`/unmatched?qa=cancelled-exception` 最终落到 `/task-hall?workspace=exceptions`，页面无 iframe，显示取消说明，顶部导航无独立 `异常处理` 和 `缓存上传`。
  - Browser/Chrome 验证：`/construction-cache?qa=cancelled-cache` 最终落到 `/construction`，页面为施工采集，控制台无 `error`/`warn`。
- 状态：完成。

### P0 审阅工作台全项复核

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 静态基准：
  - `v2-api/app/static/task_hall.html`
- 复核范围：
  - 图片半图/灰图问题。
  - 已归档照片可重新选择并修改分类。
  - 重复分类标签时禁止整组归档。
  - 键盘上下切组自动滚动。
  - 未施工折叠和搜索规则对齐静态 HTML。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 本次说明：
  - 本轮未改 Vue 业务代码；按当前运行态重新抽检 P0 审阅工作台，避免只依赖历史记录判断。
  - Browser 插件可用，已通过内置浏览器验证；该环境无法直接写 `localStorage`，切换账号通过登录页 UI 完成。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/task-hall?qa=p0-review-admin`
  - 登录身份：管理员 `admin / demo-team`，进入已由 `admin` 领取的终端 `350000434929`。
  - 图片加载：主图以 `blob:` 加载，`naturalWidth=810`、`naturalHeight=1080`、`complete=true`，页面无图片加载失败提示。
  - 未施工折叠：默认全部视图显示 `22/87`，并提示 `已折叠 65 个未施工资料组`；筛选项显示 `全部 87 / 可审阅 0 / 异常 22 / 已归档 0 / 未施工 65`。
  - 键盘切组滚动：连续按 `ArrowDown` 12 次后，活动资料组从 `120000784940` 移到 `120000984569`，资料组列表 `scrollTop` 从 `0` 变为约 `1019`，活动卡片仍在可视区域。
  - 重复分类阻断：将同组两张照片都临时设为 `表箱整体改造前` 后点击 `归档当前资料组`，页面显示 `分类重复` 提示，未提交整组归档。
  - 手机宽度：设置 390px 视口，页面无横向溢出，`scrollWidth=clientWidth=375`；审阅页签、筛选、图片和操作按钮均可见。
  - 控制台：页面 `error` / `warn` 日志为空。
- 结论：
  - P0 审阅工作台本轮未发现新增阻断项。
  - 下一阶段可继续推进 P0 施工采集复核和缺口处理。
- 状态：完成。

### 独立异常处理页旧入口降级为普通审阅入口

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户规则：
  - 异常处理页已经合并至审阅工作台，独立页面可以删除。
  - 静态 HTML 隐藏的功能默认取消，不再作为 Vue 对齐目标。
- 修改文件：
  - `v2-api/app/main.py`
  - `v2-web/src/router/index.ts`
  - `v2-web/src/views/TaskHallView.vue`
  - `scripts/smoke-client-demo.py`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - `/unmatched` 旧地址不再进入 `task-hall?workspace=exceptions`，改为重定向到普通审阅工作台 `/task-hall`。
  - `/app?page=unmatched` 前端兼容入口改为 `/task-hall`，并清除旧 `page=unmatched` 查询参数。
  - `TaskHallView.vue` 不再读取 `workspace=exceptions` 或 `mode=exceptions` 作为初始模式；即使打开旧带参链接，也默认显示“照片审阅”。
  - 保留审阅工作台内部的“异常导出”页签和导出能力，不恢复独立异常处理页。
  - 烟测脚本同步更新旧 `/unmatched` 路由期望。
- 验证：
  - `.venv\Scripts\python.exe scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面数 5，legacy bridge pages 0。
  - `.venv\Scripts\python.exe scripts\smoke-client-demo.py`：通过，`/unmatched` 与 `/unmatched?embedded=1` 均按取消页跳转验证。
  - `.venv\Scripts\python.exe scripts\verify-static-pages.py`：通过。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过，保留既有第三方 PURE 注释和 chunk 体积警告。
  - `vue-tsc --noEmit`：通过，使用 Codex bundled Node。
  - Browser 验证：访问 `/unmatched?qa=cancelled-exception-page` 最终落到 `/task-hall`，显示“照片审阅”，不显示独立异常处理面板。
  - Browser 验证：访问 `/app?page=unmatched&qa=clean-query` 最终落到 `/task-hall`，旧 `page=unmatched` 参数已清除。
  - Browser 验证：访问 `/task-hall?workspace=exceptions&qa=cancelled-exception-page` 仍显示“照片审阅”，不会因旧参数进入异常模式。
  - Browser 控制台：验证路径无应用 `error` / `warn`。
- 状态：完成。

### 项目看板基础功能对齐复核

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 静态基准：
  - `v2-api/app/static/project_board.html`
- 复核范围：
  - 项目总览指标。
  - 项目进度。
  - 风险与审阅分布。
  - 安装人员资料组占比。
  - 终端任务进度表。
  - 系统状态。
  - 总清单导入、扫码表格异步导入入口。
  - 异常表计导出、任务明细导出。
- 修改文件：
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 本次说明：
  - 本轮未改 Vue 业务代码；按当前运行态对项目看板已实现功能做静态/Vue对齐证据补齐。
  - `导出终端包` 和前端 ZIP 图片下载仍保留为后续文件级 QA 项，未在本轮强行标记完成。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/project-board?qa=board-parity-audit`
  - 页面身份：标题 `模块更换项目管理器 V2.4.10`，顶部导航当前项为 `项目看板`。
  - 导入入口：页面显示 `导入总清单`、`导入扫码表格`，文件 input 分别接受 `.xlsx/.xls` 与 `.xlsx/.xls/.csv`。
  - 指标总览：页面显示总清单 `22358`、资料组 `22361`、已扫码组 `22`、已归档 `0`、异常组 `22`。
  - 项目进度：已扫码、已归档两条进度行正常渲染。
  - 风险指标：显示 `扫码未匹配 1`、`异常资料组 22`、`未施工未扫码 22339`、`缺照片 22`。
  - 安装人员占比：显示 `罗爱民 96% / 22`、`qa 4% / 1`。
  - 终端任务表：显示前 12 个终端，首行 `350000434929`，并带 `导出明细 / 已归档范围 / 导出终端包` 控件。
  - 系统状态：显示服务、磁盘使用、数据文件、最近备份字段。
  - 账号管理：显示账号表单和账号列表，包含管理员账号行。
  - 响应式：390px 视口无横向溢出，`scrollWidth=clientWidth=375`，导入入口、任务表和账号管理仍可见。
  - 控制台：页面 `error` / `warn` 日志为空。
- 导出交互验证：
  - 点击 `导出异常表计`，页面提示 `异常表计已导出`。
  - 点击首个 `导出明细`，页面提示 `任务明细已导出`。
  - Browser 环境未捕获 Blob 下载事件，但 UI 和后端响应链路已验证；终端 ZIP 文件内容验证仍保留为后续 QA。
- 矩阵更新：
  - 项目总览指标、项目进度、风险指标、安装人员占比、终端任务表、系统状态、总清单导入入口、扫码异步导入入口、任务明细导出、异常表计导出改为非阻断。
  - `Export single-terminal delivery` 与 `Frontend image ZIP packaging` 仍保持阻断，等待文件级导出包验证。
- 状态：完成。

### 同步配置页静态功能对齐

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 静态基准：
  - `v2-api/app/static/sync_config.html`
- 用户规则：
  - 后台同步已取消，不恢复爬虫或 token/API 同步。
  - 正式流程改为表格导入，入口放在项目看板。
  - 静态 HTML 隐藏或不可达功能默认取消。
- 修改文件：
  - `v2-web/src/views/SyncConfigView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - 修复 Vue 同步配置页中文乱码和损坏的标题标签。
  - 页面明确显示“后台同步已停用”，仅作为历史说明保留。
  - 补齐静态基准里的表格导入流程：导出表格、项目看板导入、领取并分类。
  - 主按钮跳转 `/project-board`，用于导入总清单和扫码表格。
  - 次按钮跳转 `/claim-tasks`，与静态基准“查看终端任务”保持一致。
- 矩阵更新：
  - `Backend sync disabled notice` 改为非阻断。
  - `Spreadsheet import explanation` 改为非阻断。
  - `Link to project board import` 改为非阻断。
  - `Link to task view` 改为非阻断。
- 验证：
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过，保留既有第三方 PURE 注释和 chunk 体积警告。
  - `vue-tsc --noEmit`：通过，使用 Codex bundled Node。
  - `.venv\Scripts\python.exe scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面数 5，legacy bridge pages 0。
  - `.venv\Scripts\python.exe scripts\verify-static-pages.py`：通过。
  - Browser 验证：访问 `/sync-config?qa=sync-config-parity`，页面标题为 `模块更换项目管理器 V2.4.10`，显示 `后台同步已停用`、`导出表格`、`项目看板导入`、`领取并分类`、`去项目看板导入表格`、`查看终端任务`。
  - Browser 验证：点击 `去项目看板导入表格` 后跳转 `/project-board`。
  - Browser 验证：点击 `查看终端任务` 后跳转 `/claim-tasks`。
  - Browser 移动端验证：390px 视口下 `scrollWidth=clientWidth=375`，无横向溢出，页面无应用 `error` / `warn`。
- 状态：完成。

### 应用壳跨页刷新通知对齐

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 静态基准：
  - `v2-api/app/static/app_shell.html`
  - 静态壳通过 `module-manager:data-mutated` 汇总各页面写操作，再广播 `module-manager:data-refresh`，用于跨页面刷新任务、审阅、施工和看板数据。
- 修改文件：
  - `v2-web/src/api/services.ts`
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/views/ProjectBoardView.vue`
  - `v2-web/src/views/TaskHallView.vue`
  - `v2-web/src/views/ConstructionView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - `services.ts` 在非 GET/HEAD 的 JSON API 写操作和表单写操作成功后，统一发出 `module-manager:data-mutated`。
  - `AppLayout.vue` 监听 `module-manager:data-mutated`，写入 `module_manager_refresh_event` 与 `module_manager_refresh_version`，并向当前页面广播 `module-manager:data-refresh`。
  - `AppLayout.vue` 同时监听跨标签 `storage` 事件，同团队数据变化会转发为当前页刷新事件。
  - `ProjectBoardView.vue` 监听刷新事件后重新加载看板和账号数据；扫码导入任务完成时额外发出 `scan-import-complete` 变更事件。
  - `TaskHallView.vue` 监听刷新事件后复用已有 `externalRefresh()`，在非忙碌状态刷新任务和资料组。
  - `ConstructionView.vue` 监听刷新事件后重新加载施工任务。
  - `ClaimTasksView.vue` 已有刷新监听，本轮不重复修改。
- 验证：
  - `vue-tsc --noEmit`：通过，使用 Codex bundled Node。
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过，保留既有第三方 PURE 注释和 chunk 体积警告。
  - `.venv\Scripts\python.exe scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面数 5，legacy bridge pages 0。
  - `.venv\Scripts\python.exe scripts\verify-static-pages.py`：通过。
  - `.venv\Scripts\python.exe scripts\smoke-client-demo.py`：通过。
- 验证边界：
  - Browser 插件当前 evaluate 上下文不暴露完整 `window.addEventListener/localStorage`，无法在本轮用脚本直接注入并捕获刷新事件。
  - 本项按源码链路、类型检查、构建和烟测作为证据；后续普通浏览器人工 QA 可进一步验证跨标签刷新体感。
- 状态：完成。

### 应用壳全局导入与终端包导出任务对齐

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 静态基准：
  - `v2-api/app/static/app_shell.html`
  - 静态壳负责长任务：扫码表格异步导入、单终端交付包导出；切换页面时任务不应停止。
- 修改文件：
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/views/ProjectBoardView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - `AppLayout.vue` 新增全局任务条，显示任务标题、详情和进度条。
  - `AppLayout.vue` 监听 `module-manager:start-terminal-export`，调用 `exportTerminalDeliveryPackage()` 执行终端包导出。
  - `AppLayout.vue` 监听 `module-manager:start-scan-import`，调用 `startScanImportJob()` 启动异步导入，并轮询 `fetchScanImportJob()` 更新进度。
  - 扫码导入完成后，AppLayout 广播 `scan-import-complete` 刷新事件，驱动各页面更新。
  - `ProjectBoardView.vue` 的 `导出终端包` 不再由页面组件独占执行，改为提交给全局任务条；切换页面后导出生命周期仍在 AppLayout。
  - `ProjectBoardView.vue` 的 `导入扫码表格` 不再由页面组件独占轮询，改为提交给全局任务条。
  - 清理项目看板旧的导入轮询残留变量。
- Browser 验证：
  - URL：`http://127.0.0.1:8000/project-board?qa=shell-job-fast`
  - 默认 `已归档` 范围点击 `导出终端包` 后，全局任务条出现并显示 `导出失败 / 当前终端还没有已归档完成的资料。`，说明项目看板已把任务提交到 AppLayout。
  - URL：`http://127.0.0.1:8000/project-board?qa=shell-zip-all`
  - 将首个终端导出范围切换为 `全部`，点击 `导出终端包` 后，全局任务条显示 `导出完成 / 压缩包已生成，已下载 2 张图片`。
  - 验证期间页面无应用 `error` / `warn`。
- 验证边界：
  - Browser 插件可验证前端导出函数进入成功分支，但不能读取操作系统下载目录中的 ZIP 文件内容；最终文件打开检查保留为普通浏览器发布烟测项，不再阻断 Vue 功能对齐。
- 状态：完成。

### 测试与计划文档同步：取消页旧入口规则

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户规则：
  - 独立异常处理页已合并至审阅工作台，旧 `/unmatched` 入口只跳普通审阅工作台。
  - 旧 `workspace=exceptions` 或 `page=unmatched` 不得再强制进入异常模式。
- 修改文件：
  - `v2-api/tests/test_api.py`
  - `docs/V2_4X_VUE_FULL_PARITY_EXECUTION_PLAN.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - 修正 API 测试中 `/unmatched` 与 `/unmatched?embedded=1` 的期望跳转，从 `/task-hall?workspace=exceptions` 改为 `/task-hall`。
  - 修正 Vue 全量对齐计划文档中的 legacy `/unmatched` 说明，避免后续按旧异常模式回归。
- 验证：
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：`43 passed, 1 warning`。
- 状态：完成。

### 审阅工作台内置异常页签删除

- 时间：2026-06-21
- 版本目标：`V2.4.x`
- 用户规则：
  - 异常处理页已经合并至审阅工作台。
  - 独立异常处理页和审阅工作台内的异常页签/页面式分区都不再保留。
  - 静态 HTML 隐藏或不可达功能默认取消，不再作为 Vue 对齐目标。
- 修改文件：
  - `v2-web/src/views/TaskHallView.vue`
  - `docs/V2_4X_STATIC_TO_VUE_PARITY_MATRIX.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- 改动内容：
  - 删除 `TaskHallView.vue` 内部 `workbenchMode` 状态和 `照片审阅 / 异常导出` 页签。
  - 删除 `异常处理工作台` 页面式分区和相关 CSS。
  - 保留审阅资料组区域内的异常类型、异常说明、补图、回退未施工、转异常工单、恢复待审、导出异常表计等动作。
  - 更新对齐矩阵：异常处理只能作为审阅工作台内动作存在，不能再作为独立页面或页签恢复。
- 静态检查：
  - `rg "workbenchMode|异常处理工作台|review-workbench-tab|exception-workspace-v24" v2-web/src/views/TaskHallView.vue`：无结果。
- 自动化验证：
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`：通过；保留既有第三方 PURE 注释和 chunk 体积警告。
  - `.venv\Scripts\python.exe scripts\verify_vue_migration_gate.py --strict-native`：通过，注册页面数 5，legacy bridge pages 0。
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`：`43 passed, 1 warning`。
  - `.venv\Scripts\python.exe scripts\smoke-client-demo.py`：通过。
  - `.venv\Scripts\python.exe scripts\verify-static-pages.py`：通过。
- Browser 验证：
  - 访问 `/unmatched?qa=exception-page-removed` 最终落到 `/task-hall`。
  - 访问 `/task-hall?workspace=exceptions&qa=exception-query-ignored` 仍显示照片审阅工作台，不出现 `异常处理工作台`、`独立异常处理页已取消` 或 `异常导出` 页签。
  - 顶部导航不显示 `异常处理`。
  - 审阅操作区保留 1 个 `导出异常表计` 按钮。
  - 控制台 `error` / `warn` 日志为空。
- 状态：完成。
### Release Governance: V2.3.1 production baseline and V2.4.10 local version alignment

- Date: 2026-06-21
- Version target: `V2.4.10`
- User rule:
  - Register current production server as the `V2.3.1` rollback baseline before the next deployment.
  - Treat current local version as `V2.4.10`.
  - From this point onward, every update must be committed to Git and versioned.
- Changed files:
  - `v2-api/pyproject.toml`
  - `AGENTS.md`
  - `v2-api/app/static/app_shell.html`
  - `v2-api/app/static/task_hall.html`
  - `.gitignore`
  - `docs/V2_CHANGE_WORKLOG.md`
- Changes:
  - Aligned backend package metadata with app/runtime version `2.4.10`.
  - Updated required-reading version discipline to `V2.4.10` and next patch example `V2.4.11`.
  - Updated compatibility static version labels from `V2.4.0` to `V2.4.10`.
  - Ignored local runtime state paths, including `v2-api/local-test-state.json`, to prevent test data from entering Git.
- Local gates:
  - `vue-tsc --noEmit`: passed.
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`: passed, with existing Rollup PURE/chunk warnings.
  - `.venv\Scripts\python.exe scripts\verify_vue_migration_gate.py --strict-native`: passed.
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`: `43 passed, 1 warning`.
  - `.venv\Scripts\python.exe scripts\smoke-client-demo.py`: passed.
- Production baseline registration:
  - Release snapshot: `/opt/module-manager-v2/releases/v2.3.1-production-baseline-20260621_204310`
  - Runtime backup: `/opt/module-manager-v2/backups/runtime/20260621_204310_v2.3.1_baseline`
  - PostgreSQL backup: `/opt/module-manager-v2/backups/runtime/20260621_204310_v2.3.1_baseline/postgres.dump`
  - Registration file: `PRODUCTION_BASELINE_REGISTRATION.md` in both release and backup folders.
  - Service check: `module-manager-v2.service=active`, `nginx=active`, `/health=ok`, `/login GET=200`, `https://www.sgcc.online/login=200`.
- Git baseline:
  - Commit message: `chore: register v2.4.10 release baseline`
  - Release tag: `v2.4.10`
  - Next version after this baseline: `V2.4.11` for the next small patch, or `V2.5.0` for the next major workflow/UI change.
- Status: Completed.

### Project Board production patch: V2.4.11

- Date: 2026-06-21
- Version target: `V2.4.11`
- User request:
  - Fix clipped version label in the top-left product mark.
  - Show all terminal tasks on the project board, not only the first 12.
  - Add sortable upload rate / review rate / archive / unreviewed / total archive count columns.
  - Treat account `name` as the canonical installer/KPI identity and show last login IP/device.
  - Add installer daily workload popup and CSV export for KPI review.
  - Add version to system status and explain the meaning of `data file`.
- Changed files:
  - `v2-web/src/views/ProjectBoardView.vue`
  - `v2-web/src/api/services.ts`
  - `v2-web/src/api/types.ts`
  - `v2-web/src/styles/base.css`
  - `v2-web/src/styles/main.css`
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/views/LoginView.vue`
  - `v2-web/src/components/AppLayout.vue`
  - `v2-web/index.html`
  - `v2-web/package.json`
  - `v2-api/app/api/routes/auth.py`
  - `v2-api/app/api/routes/local_test.py`
  - `v2-api/app/services/account_store.py`
  - `v2-api/app/services/local_simulation.py`
  - `v2-api/app/services/state_repository.py`
  - `v2-api/app/services/ops_status.py`
  - `v2-api/app/main.py`
  - `v2-api/pyproject.toml`
  - `v2-api/tests/test_api.py`
  - `AGENTS.md`
- Changes:
  - Bumped runtime/product metadata from `V2.4.10` to `V2.4.11`.
  - Enlarged the version badge min width so `V2.4.11` renders fully.
  - Rebuilt the Vue project board view with clean Chinese labels in the touched surface.
  - Removed the 12-terminal slice and added table-level sorting for full terminal data.
  - Added upload rate to the task model and terminal progress table.
  - Added login IP and User-Agent recording for account rows.
  - Added `/local-test/installers/{installer}/daily-workload` for JSON/PostgreSQL backends.
  - Added installer KPI modal and CSV export from the project board.
  - Added system status version row and a note explaining that data file size means the business state snapshot, not photo storage.
- Validation:
  - `vue-tsc --noEmit`: passed.
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`: passed, with existing Rollup PURE/chunk warnings.
  - `python scripts\verify_vue_migration_gate.py --strict-native`: passed.
  - `pytest v2-api\tests\test_api.py -q`: `43 passed, 1 warning`.
  - `pytest v2-api\tests\test_state_repository.py v2-api\tests\test_postgres_migration.py -q`: `9 passed`.
  - `python scripts\smoke-client-demo.py`: passed.
  - Browser QA on local `http://127.0.0.1:8000/project-board`: passed after explicit demo admin login.
    - Confirmed full `V2.4.11` badge.
    - Confirmed full terminal table text, upload-rate column, upload-rate sort button, login IP/device columns, system data-file explanation, installer KPI modal and KPI CSV action.

### Construction mobile scanner patch: V2.4.12

- Date: 2026-06-21
- Version target: `V2.4.12`
- User request:
  - Fix the mobile construction collection page where tapping scan did not open/respond with the camera.
  - Production deployment will be handled by the project engineer thread that has SSH key permission.
- Changed files:
  - `v2-web/src/views/ConstructionView.vue`
  - `v2-web/package.json`
  - `v2-api/pyproject.toml`
  - `v2-api/app/main.py`
  - `v2-api/app/services/ops_status.py`
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/components/AppLayout.vue`
  - `v2-web/src/views/LoginView.vue`
  - `v2-api/app/static/app_shell.html`
  - `v2-api/app/static/task_hall.html`
  - Vue build output under `v2-api/app/static/vue/**`
  - `v2-api/tests/test_api.py`
  - `AGENTS.md`
  - `PROJECT_KNOWLEDGE.md`
  - `BUG_HISTORY.md`
  - `FIX_NOTES.md`
- Changes:
  - Bumped app/product metadata from `V2.4.11` to `V2.4.12`.
  - Added `await nextTick()` before starting the live scanner so the camera target DOM is mounted before QuaggaJS/BarcodeDetector binds to it.
  - Changed live-scan failure behavior to keep the scanner panel open and ask the user to tap `拍照识别` or `手动输入`, avoiding mobile browser blocking of programmatic camera/file-picker activation.
- Validation:
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`: passed, with existing Rollup PURE/chunk warnings.
  - `python scripts\verify_vue_migration_gate.py --strict-native`: passed.
  - `pytest v2-api\tests\test_api.py -q`: `43 passed, 1 warning`.
  - Desktop automation cannot fully validate real phone camera permission, autofocus, or barcode recognition; production mobile QA remains required after deployment.
- Deployment handoff:
  - SSH deployment is assigned to the project engineer thread with key access.
  - Suggested command after pulling this commit on the server: rebuild Vue if needed, restart `module-manager-v2.service`, reload Nginx if config changed, then verify `/health`, `/login`, and `/construction`.
  - Production deployment result from project engineer thread:
    - Strategy: new release directory + `current` symlink switch, not in-place overwrite.
    - Production `.env`, `data/`, and uploads were not overwritten.
    - Alembic migration was not executed.
    - Backup: `/opt/module-manager-v2/backups/runtime/20260621_224811_before_v2.4.12`.
    - Release: `/opt/module-manager-v2/releases/v2.4.12-20260621_224816`.
    - Service: active.
    - Server-local checks: `/health` OK, `/login` 200, `/project-board` 200, `/construction` 200, `/openapi.json` 404, `https://www.sgcc.online/login` 200.
    - Version files confirmed as `2.4.12`: `v2-api/app/main.py`, `v2-web/package.json`, `v2-api/app/static/vue/index.html`.
    - External HTTPS curl from engineer machine reset while server-local HTTPS was OK; HTTP IP external GET returned 200.
    - Real phone scanner QA remains pending.

### Construction mobile scanner and cache filter follow-up: V2.4.14

- Date: 2026-06-21
- Version target: `V2.4.14`
- Coordination:
  - `V2.4.13` is still owned by the project engineer thread for `ClaimTasksView.vue`.
  - This patch is limited to `ConstructionView.vue` and documentation.
  - Do not include unrelated dirty files in this patch.
- User request:
  - Camera should successfully open from the construction collection scan button.
  - Cached construction card layout should not overlap.
  - Exception work orders should not appear in the cached tab.
  - Upload buttons should only appear in the cached tab.
- Changed files:
  - `v2-web/src/views/ConstructionView.vue`
  - `BUG_HISTORY.md`
  - `FIX_NOTES.md`
  - `docs/V2_CHANGE_WORKLOG.md`
  - `docs/AGENT_COORDINATION.md`
- Changes:
  - Added scanner startup timeout protection for QuaggaJS loading, QuaggaJS initialization, and camera preview startup.
  - Falls back from QuaggaJS to native `getUserMedia` preview so the camera view can still open when real-time barcode decoding is unavailable.
  - Keeps exception work orders in the exception tab even when they have local drafts.
  - Counts and uploads only normal cached construction drafts from the cached tab.
  - Hides global/current upload controls outside the cached tab.
  - Adjusted mobile drawer action layout to avoid overlapping cached group photo slots.
- Validation:
  - `vue-tsc -p tsconfig.json --noEmit`: passed with bundled Node.
  - `vite build`: passed with existing Rollup PURE/chunk warnings.
  - User reported the phone camera can now open.
- Deployment handoff:
  - Patch deployment should wait for or coordinate with V2.4.13 because that thread owns current dirty version metadata and `ClaimTasksView.vue`.
  - Rebuild Vue after combining clean sources, then deploy changed source/build files only; do not overwrite production `.env`, `data/`, uploads, or run Alembic for this patch.

### Production patch deployment: V2.4.14

- Date: 2026-06-21
- Commit: `d18c516`
- Tag: `v2.4.14`
- Deployment strategy:
  - Patch sync to the existing production `current` release directory.
  - Replaced only changed source/runtime files and `v2-api/app/static/vue/**`.
  - Did not overwrite production `.env`, `data/`, uploads.
  - Did not run Alembic because this patch has no database schema change.
- Backup:
  - `/opt/module-manager-v2/backups/runtime/20260621_234534_before_v2.4.14_patch`
  - Includes selected current files, static Vue bundle, `.env`, `data`, uploads if present, and PostgreSQL dump.
- Production validation:
  - `module-manager-v2.service`: active.
  - `/health`: OK.
  - `http://127.0.0.1:8000/login`: 200.
  - `http://127.0.0.1:8000/project-board`: 200.
  - `http://127.0.0.1:8000/claim-tasks`: 200.
  - `http://127.0.0.1:8000/construction`: 200.
  - `https://www.sgcc.online/login`: 200.
  - Production version files confirm `2.4.14`.

### Claim task page workflow cleanup: V2.4.13

- Date: 2026-06-21
- Version target: `V2.4.13`
- Owner: project engineer thread
- Scope:
  - `v2-web/src/views/ClaimTasksView.vue`
  - `v2-web/src/styles/main.css`
  - version metadata and Vue build output
- User request:
  - Remove obsolete construction open/close buttons from the task claiming page.
  - Reviewers should only see terminals that still have unreviewed scanned photos.
  - Administrators should see all terminals and get a construction assignment action on each card.
  - Improve card hierarchy so the page does not read like a dense Excel printout.
  - Deploy as a patch, not as a full replacement release.
- Changes:
  - Reviewer task visibility now filters to `hasScanInfo && unreviewedCount > 0`.
  - Admin task visibility remains all terminals.
  - Replaced construction open/close action with `assignConstructionTask()` prompt action.
  - Reworked claim task cards to emphasize unreviewed count and review progress first.
  - Kept renovation, uploaded, archived counts as secondary metrics.
  - Fixed overly broad card `span` CSS that made Element Plus primary button text unreadable.
  - Bumped runtime metadata to `V2.4.13`.
- Validation:
  - `vue-tsc --noEmit`: passed with bundled Node.
  - `vite build`: passed with bundled Node; existing Rollup PURE/chunk warnings only.
  - `python scripts\verify_vue_migration_gate.py --strict-native`: passed.
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`: `43 passed, 1 warning`.
  - Headless Chrome screenshot with injected admin login confirmed:
    - `V2.4.13` visible.
    - `开放施工` / `关闭施工` no longer visible.
    - `指派施工` / `改派施工` visible for admin cards.
    - `领取` button text is readable.
- Deployment:
  - Must use patch sync only.
  - Do not overwrite production `.env`, `data/`, uploads, or run Alembic.
  - This patch was folded into `V2.4.14` because the build output includes all Vue pages and the BUG fix thread had already handed off a construction page patch.

### V2.4.15 review/thumbnail/KPI hotfix

- Date: 2026-06-22
- Owner: BUG fix thread
- Scope:
  - Preserve collector/module payload when review returns a group to construction exception work order.
  - Add review thumbnail fallback when backend thumbnail proxy fails.
  - Calculate installer daily workload from scan/source created time or latest construction upload time instead of storage/import time.
- Validation:
  - Backend compile passed.
  - `pytest v2-api\tests\test_api.py -q`: 43 passed, 1 warning.
  - Vue typecheck passed.
  - Vite build passed with existing warnings.
  - Vue strict-native gate passed.
- Coordination:
  - Current worktree also contains V2.5 project-engineer feature changes in unmatched/exception assignment files.
- User approved folding V2.4.15 into the V2.5.0 release, so this hotfix is no longer published as a separate tag.

### V2.5.0 unmatched and exception task workflow

- Date: 2026-06-22
- Owner: project engineer thread
- Scope:
  - Unmatched address task cards.
  - Project-outside-construction trace and export.
  - Replacement-meter rematch workflow.
  - Exception group assignment as constructor field task.
- Changed files:
  - `v2-api/app/services/local_simulation.py`
  - `v2-api/app/services/state_repository.py`
  - `v2-api/app/api/routes/local_test.py`
  - `v2-api/app/api/routes/exports.py`
  - `v2-web/src/api/services.ts`
  - `v2-web/src/api/types.ts`
  - `v2-web/src/views/TaskHallView.vue`
  - `v2-api/app/static/vue/**`
- Behavior:
  - Review workspace now shows field task cards for unmatched records and exception orders.
  - Unmatched records can be edited, rematched by new meter number, rematched by old meter number for replacement-meter cases, marked as project-outside construction, assigned/unassigned to constructors, exported, and deleted by admins.
  - Exception orders can be assigned/unassigned to constructors. Assignment also assigns the corresponding terminal construction task when possible.
  - JSON and PostgreSQL backends support the workflow without Alembic migration.
- Validation:
  - Backend Python compile passed.
  - Vue production build passed with existing warnings.
- Release:
  - User approved publishing V2.4.15 + V2.5.0 together as the combined `V2.5.0` release.
  - Must be committed/tagged as `v2.5.0` and deployed by patch sync.

### V2.5.0 combined release decision

- Date: 2026-06-22
- Decision:
  - Fold BUG-thread V2.4.15 fixes and project-engineer V2.5.0 workflow features into one release.
  - Do not create a standalone `v2.4.15` production tag.
  - Use `v2.5.0` as the commit/tag/deployment version.
- Included hotfixes:
  - Review exception work orders retain collector/module payload.
  - Review thumbnails fall back when the thumbnail proxy fails.
  - Installer daily workload uses scan/source time or latest construction upload time.
- Included features:
  - Unmatched address task cards.
  - Project-outside construction trace and export.
  - Replacement-meter rematch workflow.
  - Exception group task cards and constructor assignment.
- Deployment rule:
  - Use patch sync.
  - Preserve production `.env`, `data/`, and uploads.
  - Do not run Alembic because this combined release stores extension metadata in existing JSON/JSONB fields.

### Production patch deployment: V2.5.0

- Date: 2026-06-22
- Commit: `eeb0fa9`
- Tag: `v2.5.0`
- Deployment mode:
  - Patch sync to the current production release directory.
  - Did not replace production `.env`, `data/`, or uploads.
  - Did not run Alembic migration.
- Backup:
  - `/opt/module-manager-v2/backups/runtime/20260622_014459_before_v2.5.0_patch`
  - Included current release archive, `.env`, `data`, uploads archives, and PostgreSQL dump.
- Production validation:
  - `systemctl is-active module-manager-v2.service`: `active`
  - `http://127.0.0.1:8000/health`: 200
  - `http://127.0.0.1:8000/login`: 200, `V2.5.0`
  - `http://127.0.0.1:8000/project-board`: 200, `V2.5.0`
  - `http://127.0.0.1:8000/task-hall`: 200, `V2.5.0`
  - `http://127.0.0.1:8000/construction`: 200, `V2.5.0`
  - `http://127.0.0.1:8000/openapi.json`: 404
  - `http://106.14.122.43/login`: 200, `V2.5.0`
  - `http://106.14.122.43/project-board`: 200, `V2.5.0`
  - Server-side `http://www.sgcc.online/login`: 200, `V2.5.0`
  - Server-side `https://www.sgcc.online/login`: 200, `V2.5.0`
  - `nginx -t`: successful
- Note:
  - Local machine HTTPS request to `https://www.sgcc.online/login` returned `000`, but server-side domain HTTPS validation succeeded and Nginx listens on 443.

### V2.5.1 permanent field-task cards

- Date: 2026-06-22
- Owner: project engineer thread
- Scope:
  - Review task column.
  - Construction task picker.
  - Compact button/tag text alignment.
- Changed files:
  - `v2-web/src/views/TaskHallView.vue`
  - `v2-web/src/views/ConstructionView.vue`
  - `v2-web/src/styles/element-plus.css`
  - `v2-web/src/styles/main.css`
  - version metadata files
- Behavior:
  - Review workspace now has two always-visible task cards: `异常任务` and `未匹配任务`.
  - Clicking a review field-task card switches the middle panel away from terminal material groups and into the selected operation list.
  - Construction collection task picker now also has `异常任务` and `未匹配任务` cards.
  - Construction exception cards can enter the related terminal exception workflow when bound to a terminal.
  - Construction unmatched cards expose the field-confirmation queue and enter the related terminal when possible.
  - Element Plus button/tag contents are explicitly centered to avoid off-center text in compact pills.
- Validation:
  - Frontend typecheck passed with bundled Node.
  - Vue production build passed with existing Rollup PURE/chunk-size warnings.
  - Vue migration gate passed.
  - Backend API tests passed: `43 passed, 1 warning`.
  - Browser QA confirmed both cards on `/task-hall` and `/construction`, plus centered button/tag display.
- Release note:
  - This is a frontend-only patch. No API path change, no database migration, and no production data overwrite required.
- Production deployment:
  - Published to production by patch sync on 2026-06-22.
  - Backup path: `/opt/module-manager-v2/backups/runtime/20260622_075536_before_v2.5.1_patch`.
  - Preserved production `.env`, `data`, uploads; no Alembic migration.
  - Verified `/health`, `/login`, `/project-board`, `/task-hall`, `/construction`, `/claim-tasks`, `https://www.sgcc.online/login`, and `/openapi.json` 404.

### V2.5.2 construction mobile field-task card click-through hotfix

- Date: 2026-06-22
- Owner: BUG fix thread
- Scope:
  - Construction mobile task picker.
  - Permanent exception/unmatched field-task card click-through.
- Changed files:
  - `v2-web/src/views/ConstructionView.vue`
  - version metadata files
  - maintenance documentation
- Behavior:
  - Field-task card lookup now matches by task id first and terminal second.
  - Exception and unmatched field-task list cards are fully clickable, not only the small action button.
  - If a permanent field-task entry has exactly one actionable card, tapping the permanent entry enters that task directly.
- Release note:
  - Frontend-only hotfix.
  - No API path change.
  - No database schema change.
  - No Alembic migration required.
- Validation:
  - `node vue-tsc --noEmit`: passed using bundled Node.
  - `node vite build`: passed with existing Rollup PURE/chunk-size warnings.
  - `python scripts\verify_vue_migration_gate.py --strict-native`: passed.
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`: `43 passed, 1 warning`.
- Production deployment:
  - Published to production by project engineer patch sync.
  - Code commit: `8ac42ba`; tag: `v2.5.2`.
  - Backup path: `/opt/module-manager-v2/backups/runtime/20260622_085116_before_v2.5.2_patch`.
  - Preserved production `.env`, `data`, uploads; no Alembic migration.
  - Verified `/health`, `/login`, `/project-board`, `/construction`, `/task-hall`, `/claim-tasks`, `https://www.sgcc.online/login`, and `/openapi.json` 404.

### V2.5.3 project board installer KPI exception drilldown

- Date: 2026-06-22
- Owner: project engineer thread
- Scope:
  - Project board installer KPI daily workload popup.
  - JSON and PostgreSQL state repository workload payloads.
- Changed files:
  - `v2-api/app/services/local_simulation.py`
  - `v2-api/app/services/state_repository.py`
  - `v2-web/src/api/types.ts`
  - `v2-web/src/api/services.ts`
  - `v2-web/src/views/ProjectBoardView.vue`
  - version metadata files
  - maintenance documentation
- Behavior:
  - Installer daily workload rows now carry `exception_groups` details.
  - The daily exception count in the installer workload modal is clickable when greater than zero.
  - Clicking the count opens abnormal group details for that installer/date, including meter number, terminal, address, reason, and photo count.
- Release note:
  - No API path change.
  - No database schema change.
  - No Alembic migration required.
- Validation:
  - Frontend typecheck passed using bundled Node.
- Vue production build passed with existing Rollup PURE/chunk-size warnings.
- Vue migration gate passed.
- Backend API tests passed: `43 passed, 1 warning`.
- Browser QA opened `/project-board?qa=v253-installer-exception` and confirmed visible `V2.5.3`; local data is empty, so installer drilldown payload was verified with an in-memory backend sample.

### V2.5.4 construction upload installer name hotfix

- Date: 2026-06-22
- Owner: BUG fix thread
- Scope:
  - Construction upload attribution.
  - Installer KPI source names.
- Changed files:
  - `v2-api/app/api/routes/local_test.py`
  - `v2-api/app/services/local_simulation.py`
  - `v2-api/app/services/state_repository.py`
  - `v2-api/tests/test_api.py`
  - `scripts/verify-static-pages.py`
  - version metadata files
  - maintenance documentation
- Behavior:
  - Construction upload authorization continues to use `actor` username.
  - Uploaded construction photos now store the resolved account display name as `creator`.
  - If no display name can be resolved, behavior falls back to the username.
  - Static-page verifier no longer requires Node when a page has no inline scripts, so mojibake checks run first for no-script pages.
- Release note:
  - Backend attribution hotfix.
  - No API path change.
  - No database schema change.
  - No Alembic migration required.
- Validation:
  - `python -m py_compile v2-api/app/api/routes/local_test.py v2-api/app/services/local_simulation.py v2-api/app/services/state_repository.py`: passed.
  - `node vue-tsc --noEmit`: passed using bundled Node.
  - `node vite build`: passed with existing Rollup PURE/chunk-size warnings.
  - `python scripts\verify_vue_migration_gate.py --strict-native`: passed.
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`: `43 passed, 1 warning`.
- Production deployment:
  - Published to production by project engineer patch sync.
  - Code commit: `f532eb1`; tag: `v2.5.4`.
  - Backup path: `/opt/module-manager-v2/backups/runtime/20260622_105337_before_v2.5.4_patch`.
  - Synced 38 patch files/build artifacts; recorded list at `v2.5.4-synced-files.txt` inside the backup directory.
  - Preserved production `.env`, `data`, uploads; no Alembic migration.
  - Verified `/health`, `/login`, `/project-board`, `/construction`, `https://www.sgcc.online/login`, and `/openapi.json` 404.

### V2.5.5 historical construction creator backfill

- Date: 2026-06-22
- Owner: BUG fix thread
- Scope:
  - Historical construction-upload installer name repair.
- Changed files:
  - `v2-api/scripts/backfill_construction_creator_names.py`
  - `v2-api/tests/test_backfill_construction_creator_names.py`
  - version metadata files
  - maintenance documentation
- Behavior:
  - Adds a dry-run-first script that maps constructor usernames to configured user display names.
  - Only construction-upload photos are eligible for rewrite.
  - Imported scan photos and unknown creators are left untouched.
  - PostgreSQL is updated by default; JSON state can also be updated when `--state` is provided.
  - Script writes only when `--apply` is explicitly supplied.
- Release note:
  - Data backfill utility.
  - No API path change.
  - No database schema change.
  - No Alembic migration required.
- Validation:
  - `python -m py_compile v2-api/scripts/backfill_construction_creator_names.py`: passed.
  - `pytest v2-api/tests/test_backfill_construction_creator_names.py -q`: `1 passed`.
  - `node vue-tsc --noEmit`: passed using bundled Node.
  - `node vite build`: passed with existing Rollup PURE/chunk-size warnings.
  - `python scripts\verify_vue_migration_gate.py --strict-native`: passed.
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py v2-api\tests\test_backfill_construction_creator_names.py -q`: `44 passed, 1 warning`.
- Production deployment:
  - Published to production by project engineer patch sync.
  - Code commit: `f0b0edb`; tag: `v2.5.5`.
  - Backup path: `/opt/module-manager-v2/backups/runtime/20260622_114149_before_v2.5.5_patch`.
  - Preserved production `.env`, `data`, uploads; no Alembic migration.
  - Server virtualenv used for runtime scripts: `/opt/module-manager-v2/venv/bin/python`.
  - Dry-run matched only constructor username `xa` -> `樊哲浩`.
  - Dry-run report: PostgreSQL `matched_creator_username=211`, JSON compatibility state `matched_creator_username=54`.
  - Apply report: PostgreSQL `updated=211`, JSON compatibility state `updated=54`; JSON pre-backfill copy saved at `/opt/module-manager-v2/backups/runtime/20260622_114149_before_v2.5.5_patch/local_state.pre-creator-name-backfill.20260622_034158.json`.
  - Verified service active and `/health`, `/login`, `/project-board`, `/construction`, `https://www.sgcc.online/login` returned OK.

### V2.5.6 constructor assignment cap changed from 1 to 5

- Date: 2026-06-22
- Owner: Project engineer thread
- Goal:
  - Change the construction assignment rule from one active terminal task per constructor to at most five active terminal tasks per constructor.
  - Keep web and WeChat mini-program behavior aligned.
- Files changed:
  - `v2-api/app/services/local_simulation.py`
  - `v2-api/app/services/state_repository.py`
  - `v2-api/alembic/versions/0004_allow_five_construction_tasks.py`
  - `v2-api/tests/test_api.py`
  - `v2-miniprogram/miniprogram/pages/tasks/tasks.js`
  - `v2-miniprogram/miniprogram/pages/tasks/tasks.wxml`
  - `v2-miniprogram/miniprogram/utils/api.js`
  - `v2-api/app/main.py`
  - `v2-api/app/services/ops_status.py`
  - `v2-api/pyproject.toml`
  - `v2-web/package.json`
  - `v2-web/index.html`
  - `v2-web/src/components/AppLayout.vue`
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/views/LoginView.vue`
  - `v2-api/app/static/app_shell.html`
  - `v2-api/app/static/task_hall.html`
  - `v2-api/app/static/vue/**`
  - `PROJECT_KNOWLEDGE.md`
  - `AGENTS.md`
  - `FIX_NOTES.md`
  - `BUG_HISTORY.md`
  - `docs/AGENT_COORDINATION.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- Behavior:
  - Normal construction terminal assignment now counts active assignments and rejects only when the constructor already has 5 active terminal tasks.
  - Unmatched field-task assignment and exception field-task assignment use the same 5-task capacity guard.
  - Mini-program task list now keeps up to 5 assigned terminals visible and allows opening any of them.
  - Error text now says the account already has 5 active terminals instead of one active terminal.
- Database note:
  - Added Alembic migration `20260622_0004` to drop the old partial unique index `uq_tasks_team_construction_claimed_by`.
  - Production must run `alembic upgrade head`; otherwise PostgreSQL can still reject the second assignment even when code allows it.
  - Downgrade can fail if more than one active terminal already exists for a constructor.
- Validation:
  - `python -m py_compile v2-api/app/services/local_simulation.py v2-api/app/services/state_repository.py v2-api/app/main.py v2-api/app/services/ops_status.py`: passed.
  - `.venv\Scripts\python.exe -m py_compile v2-api/alembic/versions/0004_allow_five_construction_tasks.py`: passed.
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py::test_constructor_can_keep_up_to_five_assigned_terminals -q`: passed.
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`: `43 passed, 1 warning`.
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`: passed with existing Rollup PURE/chunk-size warnings.
  - `.venv\Scripts\python.exe scripts\verify_vue_migration_gate.py --strict-native`: passed.
  - Bundled Node `--check` for `v2-miniprogram/miniprogram/pages/tasks/tasks.js` and `v2-miniprogram/miniprogram/utils/api.js`: passed.
- Status:
  - Local implementation complete.
  - Requires production backup plus Alembic migration before deployment.

### V2.5.7 claim task terminal/address search

- Date: 2026-06-22
- Owner: Project engineer thread
- Goal:
  - Add a search box on the task claiming page so users can locate terminal cards by terminal number or installation address.
  - Keep the current reviewer/admin visibility rules unchanged.
- Files changed:
  - `v2-api/app/services/local_simulation.py`
  - `v2-api/app/services/state_repository.py`
  - `v2-api/tests/test_api.py`
  - `v2-web/src/api/types.ts`
  - `v2-web/src/api/services.ts`
  - `v2-web/src/views/ClaimTasksView.vue`
  - `v2-web/src/styles/main.css`
  - `v2-api/app/main.py`
  - `v2-api/app/services/ops_status.py`
  - `v2-api/pyproject.toml`
  - `v2-web/package.json`
  - `v2-web/index.html`
  - `v2-web/src/components/AppLayout.vue`
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/views/LoginView.vue`
  - `v2-api/app/static/app_shell.html`
  - `v2-api/app/static/task_hall.html`
  - `PROJECT_KNOWLEDGE.md`
  - `AGENTS.md`
  - `FIX_NOTES.md`
  - `BUG_HISTORY.md`
  - `docs/AGENT_COORDINATION.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- Behavior:
  - Task payloads now include `address` and `address_search_text`.
  - JSON state tasks get the address search fields refreshed on task-list reads, so existing data can be searched immediately.
  - PostgreSQL task stats include a full address aggregation for search without displaying long address text in cards.
  - The claim task page filters the currently visible task set by terminal, id, name, first address, or full address search index.
  - Search is responsive and stacks cleanly on narrow screens.
- Database note:
  - No Alembic migration required.
- Validation:
  - `python -m py_compile v2-api/app/services/local_simulation.py v2-api/app/services/state_repository.py v2-api/app/main.py v2-api/app/services/ops_status.py`: passed.
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`: `43 passed, 1 warning`.
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`: passed with existing Rollup PURE/chunk-size warnings.
  - `.venv\Scripts\python.exe scripts\verify_vue_migration_gate.py --strict-native`: passed.
  - Bundled Node `vue-tsc --noEmit`: passed.
  - Browser QA on `http://127.0.0.1:8000/claim-tasks`: V2.5.7 page loaded, search input visible, terminal search filtered 135 cards down to the matching terminal card.
  - Status:
    - Released to production by patch sync.
    - Commit: `0306f34`; tag: `v2.6.0`.
    - Backup path: `/opt/module-manager-v2/backups/runtime/20260622_184919_before_v2.6.0_patch`.
    - Production `.env`, `data`, and uploads preserved.
    - No Alembic migration.
    - Synced 22 changed source/docs/version files plus `v2-api/app/static/vue/`.
    - `module-manager-v2.service`: active.
    - Production GET checks passed for `/health`, `/login`, `/project-board`, `/task-hall`, `/construction`, `http://www.sgcc.online/login`, and `https://www.sgcc.online/login`.
    - `/openapi.json`: 404.

### V2.5.8 installer KPI effective work time

- Date: 2026-06-22
- Owner: Project engineer thread
- Goal:
  - Infer installer daily start/end time from scan time and construction upload time.
  - Calculate effective work duration for KPI assessment while excluding long idle gaps.
  - Add a click-through bar chart that shows hourly effective work distribution.
- Files changed:
  - `v2-api/app/services/local_simulation.py`
  - `v2-api/app/services/state_repository.py`
  - `v2-api/tests/test_api.py`
  - `v2-web/src/api/types.ts`
  - `v2-web/src/api/services.ts`
  - `v2-web/src/views/ProjectBoardView.vue`
  - `v2-api/app/main.py`
  - `v2-api/app/services/ops_status.py`
  - `v2-api/pyproject.toml`
  - `v2-web/package.json`
  - `v2-web/index.html`
  - `v2-web/src/components/AppLayout.vue`
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/views/LoginView.vue`
  - `v2-api/app/static/app_shell.html`
  - `v2-api/app/static/task_hall.html`
  - `PROJECT_KNOWLEDGE.md`
  - `AGENTS.md`
  - `FIX_NOTES.md`
  - `BUG_HISTORY.md`
  - `docs/AGENT_COORDINATION.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- Behavior:
  - Installer daily workload payload now includes `start_time`, `end_time`, `work_duration_minutes`, `work_duration_label`, `work_span_minutes`, `work_span_label`, `break_threshold_minutes`, `timepoint_count`, and `hourly_segments`.
  - Effective work duration sums only adjacent scan/upload intervals that are no more than 60 minutes apart.
  - Longer gaps are treated as breaks and excluded from effective work duration.
  - The project-board installer KPI dialog shows start/end/effective duration and opens a 24-hour bar chart by clicking the duration.
  - KPI CSV export includes start/end, effective duration, attendance span, and effective time point count.
- Database note:
  - No Alembic migration required.
- Validation:
  - `python -m py_compile v2-api/app/services/local_simulation.py v2-api/app/services/state_repository.py v2-api/app/main.py v2-api/app/services/ops_status.py`: passed.
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py::test_installer_daily_workload_includes_work_time_segments -q`: passed.
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`: `44 passed, 1 warning`.
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`: passed with existing Rollup PURE/chunk-size warnings.
  - `.venv\Scripts\python.exe scripts\verify_vue_migration_gate.py --strict-native`: passed.
- Status:
  - Released to production by patch sync.
  - Backup path: `/opt/module-manager-v2/backups/runtime/20260622_174553_before_v2.5.8_patch`.
  - Production `.env`, `data`, uploads preserved; no Alembic migration.
  - Production GET checks passed for `/login`, `/project-board`, `/task-hall`, `/construction`, `http://106.14.122.43/login`, `http://www.sgcc.online/login`, and `https://www.sgcc.online/login`.

### V2.6.0 installer KPI 2-hour efficiency model

- Date: 2026-06-22
- Owner: Project engineer thread
- Goal:
  - Change work-time chart dimension from 1 hour to 2 hours.
  - Add completion trend and per-effective-hour output.
  - Make each period clickable to inspect address-level evidence.
  - Add an explainable address difficulty weight model for KPI fairness.
- Files changed:
  - `v2-api/app/services/local_simulation.py`
  - `v2-api/app/services/state_repository.py`
  - `v2-api/tests/test_api.py`
  - `v2-web/src/api/types.ts`
  - `v2-web/src/api/services.ts`
  - `v2-web/src/views/ProjectBoardView.vue`
  - `v2-api/app/main.py`
  - `v2-api/app/services/ops_status.py`
  - `v2-api/pyproject.toml`
  - `v2-web/package.json`
  - `v2-web/index.html`
  - `v2-web/src/components/AppLayout.vue`
  - `v2-web/src/layouts/AppLayout.vue`
  - `v2-web/src/views/LoginView.vue`
  - `v2-api/app/static/app_shell.html`
  - `v2-api/app/static/task_hall.html`
  - `AGENTS.md`
  - `PROJECT_KNOWLEDGE.md`
  - `BUG_HISTORY.md`
  - `FIX_NOTES.md`
  - `docs/AGENT_COORDINATION.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- Behavior:
  - Installer daily workload now includes `completion_count`, `completion_per_effective_hour`, `weighted_completion`, `weighted_completion_per_effective_hour`, and `two_hour_segments`.
  - Two-hour segments include effective minutes, completion count, weighted completion, per-hour efficiency, weighted efficiency, and address drilldown rows.
  - Address drilldown rows include meter, terminal, address, completed time, photo count, cluster size, difficulty label, weight, and reasons.
  - Difficulty weight rule is transparent: concentrated same building/area is easier; missing room number, scattered address, and charging pile/parking-space work are harder; clustered charging-pile areas recover part of the search cost.
  - The project-board KPI dialog adds a completion line chart and 2-hour bar grid. Clicking a period opens the address list.
  - KPI CSV export includes completion and weighted efficiency columns.
- Database note:
  - No Alembic migration required.
- Validation:
  - `python -m py_compile v2-api/app/services/local_simulation.py v2-api/app/services/state_repository.py v2-api/app/main.py v2-api/app/services/ops_status.py`: passed.
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py::test_installer_daily_workload_includes_work_time_segments -q`: passed.
  - `.venv\Scripts\python.exe -m pytest v2-api\tests\test_api.py -q`: `44 passed, 1 warning`.
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`: passed with existing Rollup PURE/chunk-size warnings.
  - `.venv\Scripts\python.exe scripts\verify_vue_migration_gate.py --strict-native`: passed.
- Status:
  - Released to production by patch sync.
  - Commit: `0306f34`; tag: `v2.6.0`.
  - Backup path: `/opt/module-manager-v2/backups/runtime/20260622_184919_before_v2.6.0_patch`.
  - Production `.env`, `data`, and uploads preserved.
  - No Alembic migration.
  - Production GET checks passed for `/health`, `/login`, `/project-board`, `/task-hall`, `/construction`, `http://www.sgcc.online/login`, and `https://www.sgcc.online/login`.

### V2.6.1 installer KPI PostgreSQL field hotfix

- Date: 2026-06-22
- Owner: BUG fix thread
- Goal:
  - Fix personnel KPI popup errors on PostgreSQL-backed production data.
- Files changed:
  - `v2-api/app/services/state_repository.py`
  - `v2-api/tests/test_state_repository.py`
  - version metadata files
  - `BUG_HISTORY.md`
  - `FIX_NOTES.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- Behavior:
  - PostgreSQL installer workload completion records now use `display_meter_no` and `installation_address`.
  - The daily KPI popup and segment address drilldown no longer access non-existent `MaterialGroup.meter_no` or `MaterialGroup.address` fields.
- Database note:
  - No Alembic migration required.
- Validation:
  - `pytest v2-api\tests\test_state_repository.py::test_postgres_installer_workload_uses_material_group_installation_address -q`: passed.
  - `python -m py_compile v2-api/app/services/state_repository.py v2-api/app/main.py v2-api/app/services/ops_status.py`: passed.
  - `pytest v2-api\tests\test_api.py -q`: `44 passed, 1 warning`.
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`: passed with existing Rollup PURE/chunk-size warnings.
  - `python scripts\verify_vue_migration_gate.py --strict-native`: passed.
  - Browser QA on `http://127.0.0.1:8000/project-board?qa=v261-kpi`: page title `V2.6.1`, installer KPI dialog opens, no console error/warn.
  - Status:
    - Released to production by project engineer patch sync.
    - Commit: `b3470d1`; tag: `v2.6.1`.
    - Backup path: `/opt/module-manager-v2/backups/runtime/20260622_192008_before_v2.6.1_patch`.
    - Production `.env`, `data`, and uploads preserved.
    - No Alembic migration.
    - Synced 18 changed source/docs/version files plus `v2-api/app/static/vue/`.
    - `module-manager-v2.service`: active.
    - Production GET checks passed for `/health`, `/login`, `/project-board`, `https://www.sgcc.online/login`; `/openapi.json` stayed 404.
    - Authenticated production KPI API check passed: installer `白红运` returned daily workload rows with 143 address drilldown records; sample address row used `display_meter_no` and `installation_address` values.

### V2.6.2 installer KPI chart visual refinement

- Date: 2026-06-22
- Owner: Project engineer thread
- Goal:
  - Remove the visually noisy completion line chart from the installer KPI work-time popup.
  - Rework the 2-hour chart toward an Apple Screen Time style view.
  - Preserve address drilldown and KPI values while making the popup calmer and easier to read.
- Files changed:
  - `v2-web/src/views/ProjectBoardView.vue`
  - version metadata files
  - `BUG_HISTORY.md`
  - `FIX_NOTES.md`
  - `docs/AGENT_COORDINATION.md`
  - `docs/V2_CHANGE_WORKLOG.md`
- Behavior:
  - The separate completion trend line is removed.
  - A single screen-time card now shows 2-hour bars.
  - Each bar displays completion count, effective work duration, and per-hour efficiency.
  - Clicking a period still opens the address list.
- Database note:
  - No Alembic migration required.
- Validation:
  - `python -m py_compile v2-api/app/main.py v2-api/app/services/ops_status.py`: passed.
  - `pytest v2-api\tests\test_api.py -q`: `44 passed, 1 warning`.
  - `powershell -ExecutionPolicy Bypass -File scripts\build-vue-shell.ps1`: passed with existing Rollup PURE/chunk-size warnings.
  - `python scripts\verify_vue_migration_gate.py --strict-native`: passed.
- Status:
  - Released to production by patch sync.
  - Commit: `f4d32c2`; tag: `v2.6.2`.
  - Backup path: `/opt/module-manager-v2/backups/runtime/20260622_202711_before_v2.6.2_patch`.
  - Production `.env`, `data`, uploads preserved; no Alembic migration.
  - Production checks passed for `/health`, `/login`, `/project-board`, `/task-hall`, `/construction`, and `https://www.sgcc.online/login`; `/openapi.json` returned `404`.
