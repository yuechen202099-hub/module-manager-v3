# V3.2.0 Production Release Record

- Status: reviewed, packaged, deployed, and verified in production
- Local Verification: passed
- Package: verified
- Production Deployment: deployed
- Production Reconciliation: completed
- Rollback target: `/opt/module-manager-v2/releases/v3.2.0-20260724_095503`

## 更新内容

- 数据中台统一审阅：管理员按资料组平铺复核，统一处理照片、扫码、字段、异常和回退。
- 驾驶舱下钻：汇总指标携带筛选条件跳转数据中台，保留日期与安装人员范围。
- 统一导出中心：终端交付、设备清单、业务清单、统计报表和导出任务收口到管理员入口。
- 审阅员角色下线：停止 reviewer-only 账号与旧导航，历史审阅人与审计记录继续保留。
- 明细分页支持 20/50/100：所有相关明细分页默认 20 条，可选 20/50/100 条。

## 基线与迁移

- 发布前生产基线：`V3.1.1`。
- 候选分支：`production/V3/3.2.0`。
- 迁移顺序：
  1. `0013_data_center_query_indexes`
  2. `0014_export_center_jobs`
- 生产迁移已在 `.env`、data、uploads、交付缓存与 PostgreSQL 备份全部验证后执行，当前数据库为 `20260724_0014 (head)`。

## 权限

- 数据中台审阅、导出中心、导出创建与下载仅允许 `admin`。
- `constructor` 继续使用施工采集与指派的异常处理，不获得审阅或导出权限。
- reviewer-only 账号停用，旧 token 不能继续访问受保护业务接口；历史审阅字段和审计记录不删除。

## 备份范围

- 当前 release 与回滚目标。
- 生产 `.env`。
- data 与 uploads。
- 交付缓存和导出缓存。
- PostgreSQL 全量 dump，并验证文件非空。

## rollback

应用回滚保留前向数据库结构，不覆盖 `.env`、data、uploads、缓存或 PostgreSQL：

```bash
ln -sfn /opt/module-manager-v2/releases/v3.2.0-20260724_095503 /opt/module-manager-v2/current
systemctl restart module-manager-v2.service
systemctl restart module-manager-v2-photo-barcode-maintenance.service
```

数据库整体恢复只允许使用已验证的 V3.2.0 发布前 PostgreSQL 备份，并另行批准。

## 健康与验收路由

- `GET /health`
- `GET /local-test/system/status`（管理员）
- `GET /login`
- `GET /project-board`
- `GET /global-search`
- `GET /exports`
- `GET /task-hall` 应重定向到 `/global-search`
- `GET /review/:groupId` 应重定向到数据中台审阅弹窗

## 聚焦门禁

```powershell
python scripts\verify_v3_2_0_role_routes.py
python scripts\verify_v3_2_0_data_center_ui.py
python scripts\verify_v3_2_0_dashboard_drilldown.py
python scripts\verify_v3_2_0_export_center_ui.py
python scripts\verify_v3_2_0_single_export_entry.py
python scripts\verify_v3_2_0_release.py
python scripts\test_verify_release_sop.py
```

## 完整验证

```powershell
cd v2-api
python -m pytest -q
cd ..\v2-web
npm run type-check
npm run build
cd ..
python scripts\verify_release_sop.py --version V3.2.0
```

完整回归结果为 `1782 passed, 13 skipped`。独立复审结果为 `Critical 0 / Important 0 / Minor 1`，该 Minor 项不阻断发布。发布包校验通过，共核对 167 个必需文件，未发现本地缓存、运行数据或敏感文件。

## 发布包与生产证据

| Evidence | Value |
| --- | --- |
| Artifact | `build/server-release/module-manager-v2-server-3.2.0.zip` |
| Size | 1621627 bytes |
| SHA256 | 9448EDDCA27A36F2DF606EC1BC04A3BED05930B3D4D718E2D10381EE7FAEE6DF |
| Source commit | `fe527eb84064096321e727abf9ccbdc981e10b7e` |
| Backup directory | /opt/module-manager-v2/backups/V3.2.0-pre-20260724_105349 |
| Release directory | /opt/module-manager-v2/releases/v3.2.0-20260724_105649 |
| Database migration head | `20260724_0014 (head)` |
| Public health check | https://www.sgcc.online/health passed |
| Full test result | 1782 passed, 13 skipped |
| Required files | 167 |
| Independent review | Critical 0 / Important 0 / Minor 1，结论可发布 |
| `/local-test/system/status` | 管理员鉴权请求通过；条码维护 `paused=false`、`verification_pending=0`、`delivery_cache_pending=0` |
| Vue title and entry asset | `3.2.0`；`assets/index-fvxsb4dd.js`；入口 SHA256 `74C4531D3472784EEA87A9A60D2B07AB8A0C3BF4DBFBE6A69C16B6B29BF8E314` |
| Worker and export queue | API、Nginx、条码 worker 和 timer 均为 active；管理员 `/exports` 返回 200 |

## 上线后复核

- `/health`、`/login`、`/project-board`、`/global-search`、`/construction`、`/claim-tasks`、`/task-hall` 和管理员 `/exports` 均返回 200。
- `/docs`、`/redoc`、`/openapi.json` 均返回 404。
- 备份目录中的数据库、data、uploads、旧 release 和 `SHA256SUMS` 均验证通过。
- 数据中台和导出中心默认每页 20 条，支持 20/50/100；非法 `page_size=21` 返回 422。
- 交付缓存状态为 `ready=1825`、`failed=4`，无待处理交付缓存。
- 发布后 API journal 错误数为 0。
- 服务器只保留最近 5 个 release：`v3.2.0-20260724_105649`、`v3.2.0-20260724_095503`、`v3.1.1-20260723_171150`、`v3.1.0-20260723_153935`、`v3.1.0-20260723_151707`。
