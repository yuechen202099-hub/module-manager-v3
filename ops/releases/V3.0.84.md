# V3.0.84 生产发布记录

## 发布状态

- 日期：2026-07-22
- 版本：`3.0.84`
- 类型：小更新 / 任务与审阅性能优化
- 生产分支：`production/V3/3.0.84`
- Status: reviewed, packaged, deployed, and verified in production
- 应用发布提交：`dfc40c68e94cc69f0cec27c318160626f75b8d37`

## 更新内容

- 任务领取页改用服务器任务快照，不再串行请求任务状态和完整任务列表。
- 审阅工作台改为服务端搜索、状态统计和分页，每页最多 20 条。
- 后台刷新只更新轻量任务快照和当前审阅页，不再每分钟拉取全部资料组。
- 资料组详情只在打开时加载，自动预热仅限首个可审阅资料组的前 4 张缩略图。

## 本地验证

- 完整测试：`1130 passed, 5 skipped`
- 前端状态测试、源码门禁与正式构建：状态测试 `5 passed`，领取完成状态与审阅分页门禁通过，`vue-tsc` 与 Vite 正式构建通过
- 发布器与 SOP 专项测试：`481 passed`
- 客户端 smoke、严格 Vue 页面门禁、管理员更新说明与发布 SOP 验证通过
- 本地生产快照性能：热快照 `14.430ms`，审阅首屏 `43.338ms`，返回 `20` 条，真实 `60` 秒窗口构建次数 `1`，采样期间缓存实例未重启且计数单调递增
- 发布包源码绑定：ZIP 必须包含 `SOURCE_COMMIT`，并在发布前与独立复审通过的完整 Git commit 一致
- 独立代码复审：全分支初审与首次复审发现的 Important 均已关闭；最终结果 `Critical=0`、`Important=0`、`RELEASE_GATE: PASS`，复审代码提交 `3894aae`

## 发布包

| 项目 | 结果 |
| --- | --- |
| 文件 | `build/server-release/module-manager-v2-server-3.0.84.zip` |
| 生成时间 | `2026-07-22 03:30:34 +08:00` |
| 大小 | `1514683 bytes (1.44 MiB)` |
| SHA256 | EBE0708C28055073890B6FEECF632B2E9BF0D817D78CC2968DC553B66A385751 |
| 服务器文件 | `/tmp/module-manager-v2-server-3.0.84.zip` |
| 验包 | 通过；132 个必需文件齐全，`SOURCE_COMMIT` 与应用发布提交一致；额外注入未跟踪文件的临时包被门禁拒绝 |

## 生产部署

| 项目 | 结果 |
| --- | --- |
| Backup directory | /opt/module-manager-v2/backups/V3.0.84-pre-20260722_033427 |
| 备份内容 | `.env`、数据库 dump/schema、data、uploads、当前 release 和 `SHA256SUMS` |
| 备份校验 | `SHA256SUMS` 全部通过；数据库 dump 的 `pg_restore -l` 返回 190 行；`.env` 哈希一致；data/uploads 归档可读取 |
| Release directory | /opt/module-manager-v2/releases/v3.0.84-20260722_033636 |
| 回滚 release | `/opt/module-manager-v2/releases/v3.0.83-20260721_214617` |
| 本地与服务器哈希 | 一致 |
| Public health check | https://www.sgcc.online/health passed |

## 线上验收

- `/health`、`/login`、`/project-board`、`/claim-tasks`、`/task-hall`、`/construction`、`/global-search` 返回 `200`；`/docs`、`/redoc`、`/openapi.json` 返回 `404`。
- 系统状态版本为 `3.0.84`；登录令牌在北京时间当天结束边界 `00:00` 失效；项目汇总的照片和资料组条码准确率字段完整。
- 任务快照返回 135 条，`00000000` 占位终端为 0；审阅首屏返回 20 条，服务端分页、全局搜索和状态统计数据正常。
- 全局搜索返回真实资料组；资料组详情返回 4 张照片，缩略图请求为 `image/jpeg` 且 HTTP `200`。
- 线上 60 秒性能采样：首个任务快照 `24.821ms`，热任务快照 `21.947ms`，审阅首屏 `41.938ms`，缓存构建次数 `1`，均满足发布阈值。
- 领取、释放、施工指派、审阅、异常、回退、重新扫码和人工确认未在真实生产数据上制造验收写入；对应回归由本版本完整测试和独立复审覆盖。
- 应用与 Nginx 均为 `active`；采样时应用 CPU `1.8%`、系统负载 `0.00/0.03/0.02`，部署后应用 warning/error 与 5xx 均为 0，Nginx 最近 2000 条请求 5xx 为 0，错误日志为空。
- 版本目录清理先 dry-run，确认仅删除 V3.0.79 后执行；服务器现保留 V3.0.80 至 V3.0.84 共五个 release。

## 回滚

- 应用回滚目标为 `/opt/module-manager-v2/releases/v3.0.83-20260721_214617`。
- 回滚不覆盖 `.env`、data、uploads 或数据库；V3.0.84 未增加数据库迁移。
