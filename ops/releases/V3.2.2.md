# V3.2.2 Production Release Record

- Status: deployed
- Local Verification: passed
- Package: passed
- Production Deployment: passed
- Production Reconciliation: passed
- Rollback target: V3.2.1
- 发布状态：已上线，待完成 release 留存清理和 GitHub 推送
- 生产分支：`production/V3/3.2.2`
- 源码提交：`5890a622b734c025be55da371287aae8c2d736e9`
- 当前 release：`/opt/module-manager-v2/releases/v3.2.2-20260729_033157`
- 回滚 release：`/opt/module-manager-v2/releases/v3.2.1-20260728_111144`
- 数据库版本：`20260724_0014 (head)`
- 生产域名：`https://www.sgcc.online`

## 更新内容

- 安装人员每日工作量 KPI 升级为 A1 通栏经营简报。
- 保留原 15 列每日明细、日期固定列、横向滚动、2 小时工时分段、地址与异常资料组下钻和页面内 KPI CSV。
- 保留日、周、月范围和数据中台“查看原始资料”次级入口。
- KPI 公式、API、人工导入口径、PostgreSQL/JSON 业务数据、OSS、uploads 和缓存写入规则均不变。

## 构建与发布包

- 构建命令：`powershell -ExecutionPolicy Bypass -File .\scripts\build-client-release.ps1 -Version 3.2.2 -PerformanceReport .\build\release-evidence\v3.2.2-task-review.json`
- 本地发布包：`build/server-release/module-manager-v2-server-3.2.2.zip`
- 文件大小：`1,650,752 bytes`
- 本地 SHA256：`F7B5E692F32FF3BCB752258281FBC8EC5673536BB6E97C538B452AD3BF521CB0`
- 服务器 SHA256：`F7B5E692F32FF3BCB752258281FBC8EC5673536BB6E97C538B452AD3BF521CB0`
- 发布包校验：通过；包含 173 个必需文件，源码提交匹配，无 `.env`、密钥、数据库、uploads、缓存或其他禁止文件。
- 性能证据：60 秒采样通过，证据中的 source commit 与最终提交一致。

## 本地验证

- 后端测试：`1263 passed, 13 skipped`
- 发布与 SOP 验证：`603 passed`
- Vue：`type-check` 和生产构建通过
- 独立审阅：`Ready`；Addressed `2`，Open `0`
- 最终发布包哈希在部署收尾时重新计算，仍与本记录一致。

## 备份与数据边界

- 上线前备份：`/opt/module-manager-v2/backups/V3.2.2-pre-20260729_032646`
- 备份包含 `.env`、current release、data、uploads、PostgreSQL dump 和 `SHA256SUMS`。
- 备份验签：通过。
- 未覆盖生产 `.env`、data 或 uploads。
- 未修改 OSS 生产对象。
- 未执行数据库迁移、downgrade 或生产数据修复；上线前后数据库均为 `20260724_0014 (head)`。

## 部署过程

1. 首次切换到 `/opt/module-manager-v2/releases/v3.2.2-20260729_033041` 后，systemd 已报告 active，但 Uvicorn 尚未完成端口监听，紧随其后的健康检查收到瞬时 `502`。
2. 部署脚本按预案自动将 current 回滚到 V3.2.1；未执行数据库回滚，未恢复或覆盖数据。
3. 确认根因是进程启动就绪竞态后，在切换脚本中加入 `127.0.0.1:8000` 端口就绪等待。
4. 第二次切换到 `/opt/module-manager-v2/releases/v3.2.2-20260729_033157` 成功并完成全部部署后检查。

该事件由发布脚本自动处置，未形成持续业务中断，不单独升级为 P0 数据事件。

## 生产验证

- `current`：`/opt/module-manager-v2/releases/v3.2.2-20260729_033157`
- `module-manager-v2.service`：`active`
- `nginx.service`：`active`
- `module-manager-v2-photo-barcode-maintenance.service`：`active`
- `module-manager-v2-photo-barcode-maintenance.timer`：`active`
- `module-manager-v2-photo-barcode-maintenance-enqueue.service`：`inactive`，符合按 timer 触发的一次性服务预期
- Uvicorn：监听 `127.0.0.1:8000`
- 本机 `/health`：`200`，`{"data":{"status":"ok"}}`
- 公网 `/health`：`200`，`{"data":{"status":"ok"}}`
- `production_health_check.py --expected-version 3.2.2`：通过
- 生产安全审计：通过

## 公网业务验收

- `/login`、`/project-board`、`/global-search`、`/exports`、`/construction` 均加载成功。
- 页面运行版本显示 `V3.2.2`。
- KPI A1 主层真实加载生产安装人员数据。
- 原 15 列全部存在；日期列固定在左侧；横向滚动生效。
- 1440、1366、1024 三种宽度下 KPI 弹窗可用。
- 2 小时工时分段、地址清单和异常明细均完成真实数据下钻。
- “导出 KPI CSV”成功下载，验收文件大小 `1,787 bytes`。
- “查看原始资料”成功进入带安装人员筛选条件的数据中台。
- 自动化验收共 47 项通过，无页面脚本错误、框架错误遮罩或控制台 error。
- 截图与验收 JSON 保存于本机 Codex 可视化证据目录，不纳入生产仓库。
- Codex 内置浏览器因本机 DNS 代理将域名解析到 `198.18.0.8` 而收到 `ERR_CONNECTION_CLOSED`；改用独立无痕 Playwright 并为本次进程显式解析到生产 IP，不修改系统 hosts。

## Release 留存

- 上线后服务器共有 7 个 release 目录。
- 清理前必须先运行：`bash /opt/module-manager-v2/current/scripts/cleanup_old_releases.sh /opt/module-manager-v2 5 --dry-run`
- 清理只允许删除 `$APP/releases` 下最旧的两个 V3.2.0 release；必须保留 current、V3.2.1 回滚 release、backups、data、uploads 和数据库 dump。
- 实际 dry-run 和清理摘要将在 SSH 维护通道恢复后补录。

## 回滚

- 回滚目标：`/opt/module-manager-v2/releases/v3.2.1-20260728_111144`
- 只切换 `/opt/module-manager-v2/current` 并重启应用。
- 回滚前停止照片条码维护 worker 和 timer；确认 V3.2.1 与当前前向数据库兼容后再恢复。
- 不执行 Alembic downgrade，不恢复数据库，不覆盖 `.env`、data 或 uploads。
