# V3.0.39 Production Release Record

## Summary

- Version: `V3.0.39`
- Release type: production bug fix, `+0.01`
- Branch: `production/v3.0.35`
- Runtime commit: `f146ca7 fix: release production v3.0.39 exception refresh`
- Production release directory: `/opt/module-manager-v2/releases/v3.0.39-20260627_033344`
- Previous release: `/opt/module-manager-v2/releases/v3.0.38-20260627_023937`

## User Issue

- 项目看板“异常与缺照/待派发”列表中仍出现“缺少模块资产编号”。
- 用户截图中的资料组已经有模块号，继续显示该异常会误导管理员和施工派发。

## Root Cause

- V3.0.38 修复了归档校验函数的模块号识别口径，但 PostgreSQL 的异常列表入口仍直接读取历史 `exception_note`。
- 生产库里部分历史记录 `status=rejected`、`exception_note=缺少模块资产编号`，但 `has_archive_blocker=false` 且模块号已存在。
- 前端 `exceptionReasonText()` 在 `exception_reasons` 为空时会回退展示 `exception_note`，因此待派发列表继续显示旧自动异常。

## Changed Scope

- PostgreSQL 异常列表读取前，会用数据库资料组字段和活跃照片字段重新校验归档异常。
- 新增 PostgreSQL 专用模块号索引，避免使用 JSON state 的模块号映射。
- 自动生成的过期归档异常备注会落库清理。
- 没有真实异常的历史 rejected 资料组会恢复为待审状态。
- 真实异常继续保留：缺少采集器信息、资料组照片不足、模块号重复。

## Verification

- `python -m pytest v2-api/tests/test_state_repository.py -k stale_missing_module_note -q`: `1 passed`
- `python -m pytest v2-api/tests/test_state_repository.py -k "manual_exception_note or stale_missing_module_note" -q`: `2 passed`
- `python -m pytest v2-api/tests/test_state_repository.py -q`: `18 passed`
- `python -m pytest v2-api/tests/test_local_simulation.py -k "archive_validation or duplicate_module or missing_required_scan_fields" -q`: `3 passed, 60 deselected, 1 warning`
- `python -m pytest v2-api/tests/test_api.py -k "exception_groups or project_board_page_is_available" -q`: `1 passed, 52 deselected, 1 warning`
- `python -m pytest v2-api/tests/test_api.py -k "system_status_requires_admin_and_reports_runtime_state or exception_groups or project_board_page_is_available" -q`: `2 passed, 51 deselected, 1 warning`
- `npm run build`: passed with existing Rollup PURE and chunk-size warnings.
- `node scripts/verify_admin_release_notes.js`: passed.
- `node scripts/verify_photo_barcode_accuracy.js`: passed.
- `python scripts/verify_vue_migration_gate.py --strict-native`: passed.
- `python scripts/verify_release_sop.py`: passed.
- `python scripts/verify-client-release.py build/server-release/module-manager-v2-server-3.0.39.zip`: passed.
- `git diff --check`: passed with CRLF warnings only.
- Subagent review: one blocking issue found; manual exception preservation was added and covered by regression test before deployment.

## Package

- Local package: `build/server-release/module-manager-v2-server-3.0.39.zip`
- SHA256: `73D4D6312218D6B99ED4F5B1DD0306807058802CCDE8F1594F4E11B46A9BEED7`
- Server package: `/tmp/module-manager-v2-server-3.0.39.zip`
- Server SHA256: `73D4D6312218D6B99ED4F5B1DD0306807058802CCDE8F1594F4E11B46A9BEED7`

## Production Backup

- Backup directory: `/opt/module-manager-v2/backups/V3.0.39-pre-20260627_033327`
- Included `.env`, current release archive, `data`, `uploads`, PostgreSQL custom dump, schema dump, and `SHA256SUMS`.

Two earlier backup attempts created directories whose names include a carriage return from the old CRLF shell script path handling. They also completed, but the valid clean backup for this release is `/opt/module-manager-v2/backups/V3.0.39-pre-20260627_033327`.

## Production Checks

- Current symlink: `/opt/module-manager-v2/releases/v3.0.39-20260627_033344`
- `module-manager-v2.service`: `active`
- `nginx`: `active`
- Local app `/health`: `200`
- Local nginx `/health`, `/project-board`, `/global-search`, `/construction`, `/login`: `200`
- Public `https://www.sgcc.online/health`, `/project-board`, `/global-search`, `/construction`, `/login`: `200` from the production server.
- Admin `/local-test/system/status`: version `3.0.39`
- `production_health_check.py --expected-version 3.0.39`: passed.

## Production Exception Refresh

After deployment, existing production exception rows were refreshed with the V3.0.39 PostgreSQL logic:

- Before refresh: `candidate_total=123`
- Before refresh stale module false positives: `缺少模块资产编号=72`
- After refresh: `candidate_total=51`
- Exception list stale module items after refresh: `0`
- Remaining reasons:
  - `missing_collector_photo`: `33`
  - `缺少采集器信息`: `33`
  - `资料组照片不足 4 张`: `16`
  - `模块号重复: 3130054532250008577372`: `2`

## Rollback

Rollback target:

```text
/opt/module-manager-v2/releases/v3.0.38-20260627_023937
```

Rollback command:

```bash
ln -sfn /opt/module-manager-v2/releases/v3.0.38-20260627_023937 /opt/module-manager-v2/current
systemctl restart module-manager-v2.service
curl -fsS http://127.0.0.1/health
```

Do not restore database or uploads without separate user approval.
