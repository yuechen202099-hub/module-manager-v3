# V3.2.16 Production Release Record

## Summary

- Status: attested
- Local Verification: passed
- Package: passed
- Production Deployment: passed
- Production Reconciliation: passed
- Rollback target: V3.2.15
- Candidate branch: `production/V3/3.2.16`
- Deployed production baseline: `V3.2.15`
- Candidate version: `V3.2.16`
- Database head: `20260824_0016 (head)`
- User request: remove the retired reviewer-claim dependency from review saving, hide completed unmatched history from the actionable list, and publish the fix.
- Priority: production bug fix from the attested V3.2.15 baseline.

## Scope

- PostgreSQL unmatched data-center rows expose only records whose status is `open`.
- JSON/local and PostgreSQL group review decisions no longer require a legacy reviewer claim.
- The acting administrator remains recorded in historical reviewer/audit fields.
- Login and stale-session labels do not present the removed reviewer role.
- Production backup checksum manifests use relocatable paths and include the protected `.env` file.
- V3.2.15 remains immutable and is the rollback boundary.
- No database migration, production business-data rewrite, camera request, or client-platform request is part of this release.
- V3.2.16 is deployed and verified in production.

## Local verification

- Backend full suite: `1723 passed, 49 skipped`; only the existing dependency deprecation warnings remain.
- Frontend state/view/component suites: Node `13 passed`; focused Vitest `100 passed`; shared component Vitest `21 passed`.
- Type check and Vite production build: passed; only existing dependency-annotation and chunk-size warnings remain.
- Source verifier tests: passed
- Generic client-package verifier tests: passed
- SOP verifier tests: passed
- Source phase: passed
- SOP source phase: passed
- Local verifier counts: V3.2.16 focused/source `97 passed`; generic package and SOP `715 passed`.
- Git diff check: passed
- Browser viewport: `1280x720 passed`

## Source and package

- Source commit: 6e286b33d6063d9c89da6b28013909bce81488d1
- Archive file: build/server-release/module-manager-v2-server-3.2.16.zip
- SHA256: 5265cce8397e785d4fdd0dc521f17e25e171b7ef4d7105ee45db8aab0004fc60
- Server SHA256: 5265cce8397e785d4fdd0dc521f17e25e171b7ef4d7105ee45db8aab0004fc60
- Build command: passed
- Archive verification result: passed

Archive verification found `283` required files and `467` ZIP members. Every member passed CRC validation, no duplicate or case-conflicting members were present, `SOURCE_COMMIT` equals `6e286b33d6063d9c89da6b28013909bce81488d1`, and the local and server package hashes are identical. The archive size is `2249170` bytes.

## Production deployment

- Backup directory: /opt/module-manager-v2/backups/v3.2.16-20260829T014034Z
- Backup verification: passed
- Release directory: /opt/module-manager-v2/releases/v3.2.16-20260829T020456Z
- Rollback directory: /opt/module-manager-v2/releases/v3.2.15-20260828T144635Z
- Uvicorn readiness: 127.0.0.1:8000 ready
- Local health: HTTP 200 version 3.2.16
- Public health: HTTP 200 version 3.2.16
- Authorization acceptance: passed
- Zero-write acceptance: passed
- Camera requests: 0
- Client-platform requests: 0
- Maintenance restoration: passed
- Soak health checks: passed
- Attestation: passed

Production evidence:

- The canonical backup retained `.env`, the complete V3.2.15 current release, shared data, uploads, and PostgreSQL custom and schema dumps. All seven relative SHA256 entries passed after relocation; the data archive listed `10234` members, the uploads archive listed `1` member, the current-release archive listed `563` members, and `pg_restore -l` listed `316` lines.
- The local and server package SHA256 values matched. ZIP CRC, generic package verification, and the V3.2.16 package contract passed before extraction.
- The final atomic switch started PID `6400` from `/opt/module-manager-v2/releases/v3.2.16-20260829T020456Z/v2-api`. Systemd and Nginx were active, and exactly one Uvicorn listener was bound to `127.0.0.1:8000`.
- Alembic remained `20260824_0016 (head)`. The production security audit passed; the maintenance service and timer were restored active with worker PID `8100`, while the enqueue service remained inactive.
- Public `/health`, `/login`, `/project-board`, `/review-workbench`, `/collector-inventory`, and `/construction` returned `200`; `/docs`, `/redoc`, and `/openapi.json` returned `404`. Unauthenticated data-center and collector-transfer API requests returned `401`.
- Authenticated read-only data-center acceptance returned `16` open unmatched rows. The deployed review functions no longer consult a reviewer claim, the acting administrator audit field remains in place, and the deployed unmatched query contains the open-status filter.
- No business mutation endpoint, camera operation, or client-platform request was used during acceptance. Login, system status, summary, and data-center queries were the only authenticated application requests.
- The in-app Browser production-domain request was closed by its network path. The exact V3.2.16 candidate bundle was therefore rendered locally at `1280x720`: `/review-workbench` redirected to `/login?redirect=/review-workbench`, the V3.2.16 login shell showed only administrator and constructor roles, and the browser captured no warning or error logs.
- The deployed Vue entry is `assets/index-CUfjXgNF.js`; the release file, local HTTP, and public HTTPS SHA256 values all equal `f28fafe4d3e2312b6b6516534bb612fd34da20df79b05dbf916181c3129190dc`.
- The first guarded switch was automatically returned to V3.2.15 because an ad hoc precheck incorrectly expected `/health` to carry the application version. Logs showed that V3.2.16 had started and served HTTP `200`; the corrected gate waited for the real listener and used `production_health_check.py` to validate version `3.2.16` through the authenticated system-status route before retaining the final switch.
- Three stability samples retained API PID `6400`, worker PID `8100`, one listener, local and public HTTP `200`, active API/maintenance/timer states, and inactive enqueue state. The sampled API and worker journals contained zero `ERROR`, `Traceback`, or `Exception` entries.
- Retention dry-run selected only `/opt/module-manager-v2/releases/v3.2.11-20260827T030718Z`; cleanup removed that release and retained V3.2.12 through V3.2.16, including the V3.2.15 rollback target. Backups, `.env`, shared data, uploads, database dumps, and OSS data were not deleted.

## Rollback

- Restore `/opt/module-manager-v2/current` to `/opt/module-manager-v2/releases/v3.2.15-20260828T144635Z`.
- Keep Alembic at `20260824_0016`; do not run a downgrade.
- Wait for `127.0.0.1:8000` before health and page checks.
