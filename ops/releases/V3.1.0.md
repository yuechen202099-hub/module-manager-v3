# V3.1.0 生产发布记录

## 发布状态

- 日期：2026-07-23
- 版本：`3.1.0`
- 类型：大更新 / 正式交付、资料核验与后台归档
- 生产分支：`production/V3/3.1.0`
- Status: reviewed, packaged, deployed, and verified in production
- 应用发布提交：`b48221e98b012002d94df3a652adce85d99dc74a`
- 回滚版本：`V3.0.84`

## 回滚边界

- `0006` 至 `0012` 的核验、审计、自动归档队列、交付缓存和批量失效索引迁移为生产不可逆迁移，禁止执行 `alembic downgrade`，所有 downgrade 入口会在任何 DDL 前失败。
- 应用回滚可切回 V3.0.84 release，但保留 V3.1.0 数据库结构与历史数据；如事故必须回退数据库，只能按单独批准的恢复方案使用已验证的升级前 PostgreSQL 备份整体恢复。

## 更新内容

- 正式交付生成设备清单双 Sheet，并在 ZIP 中按改造前表箱、采集器条码、模块与电能表、改造后表箱四类照片目录交付。
- 条码和二维码可作为机器核验依据；OCR 仅保留候选，必须人工确认后才可写入正式字段、通过核验或触发自动归档。
- 机器核验或人工确认通过后才进行事务性自动归档；审阅完成后仅向后台交付缓存队列入队，页面不回源原图或同步压缩。
- 审阅事务提交后异步缓存正式交付原图，失败状态持久化并可重试；重新扫码或证据变更会失效旧缓存与交付包。
- 外部原图下载统一执行生产白名单、单次 DNS 解析、公网 IP 固定连接、socket peer 核验和禁止重定向；HTTP Host 与 HTTPS SNI/证书校验继续使用原域名，阻断 DNS 重绑定访问内网。

## 本地验证

- Task 7 的 V3.1 行为验证器已通过迁移执行、四图资格正反例、OCR-only 拦截、默认暂停、21 任务仅处理 20 个且严格串行、批间一次 5 秒等待、双 Sheet 样本和占位值拒绝。
- 迁移验证确认 `0006` 至 `0012` 均在任何 DDL 前拒绝 downgrade；本候选不声明数据库往返可逆。
- 本地真实 HTTP 任务/审阅性能报告绑定应用发布提交：任务快照 `1.950ms`，审阅分页 `2.126ms`，60 秒内快照构建次数 `1`。
- 本地回填仅执行 `--preview`：无分类候选、冲突、跳过、可入队或不可扫描资料组；空样本预计导出错误为 `no_groups`。未执行 `--apply`。
- 缓存、存储清理、并发修复和批量失效原专项回归为 `33 passed`，外部图片安全下载专项为 `9 passed`；完整后端为 `1161 passed, 13 skipped`。跳过项均为仓库既有的外部环境条件用例，当前迁移头为 `20260723_0012`。
- 前端状态测试为 `17 passed`，仓库全部 `30` 个 JS 行为/源码门禁通过；`vue-tsc --noEmit`、Vite 正式构建、严格 Vue 路由、生产就绪、发布 SOP、保留策略与安全加固静态检查均通过。发布包与 SOP 验证器测试为 `525 passed`，最终 worker 专项为 `72 passed`。
- 独立审阅最终结论：`Critical=0`、`Important=0`、`Minor=0`、`Ready to release: yes`。

## 发布包

| 项目 | 结果 |
| --- | --- |
| 文件 | `build/server-release/module-manager-v2-server-3.1.0.zip` |
| 生成时间 | `2026-07-23 15:38:32 +08:00` |
| 大小 | `1704559 bytes (1.63 MiB)` |
| SHA256 | 2D939298EA2E8A072E3D6C9EB2AB3040DA2E0BA555473F3330A630A08A1B259A |
| 验包 | 通过；`SOURCE_COMMIT` 与应用发布提交一致，本地与服务器部署前哈希一致，服务器临时 ZIP 已删除 |

## 生产部署

| 项目 | 结果 |
| --- | --- |
| Backup directory | /opt/module-manager-v2/backups/V3.1.0-pre-20260723_150202 |
| 备份内容 | `.env`、当前 release、data、uploads、PostgreSQL dump/schema 和 `SHA256SUMS` |
| 备份校验 | `SHA256SUMS` 6 项全部通过；`pg_restore -l` 返回 190 行 |
| Release directory | /opt/module-manager-v2/releases/v3.1.0-20260723_153935 |
| 回滚 release | `/opt/module-manager-v2/releases/v3.0.84-20260722_033636` |
| 数据库迁移 | `20260723_0012 (head)` |
| 运行账号 | 主服务与后台 worker 均为 `modulemgr` |
| Public health check | https://www.sgcc.online/health passed |

## 线上验收

- `/health`、`/login`、`/project-board`、`/task-hall`、`/construction` 返回 `200`；`/docs`、`/redoc`、`/openapi.json` 返回 `404`。
- 主服务、Nginx、单进程后台 worker 和午夜定时器均为 `active`；worker 限制为每批 20 组、串行执行、批后休眠 5 秒、CPU 20%、内存 512MB，重启次数为 0。
- `modulemgr` 实际导入 Pillow 成功；部署后 worker 日志未发现 traceback、崩溃或 OOM 信号。
- 审阅完成会在事务提交后入队，不阻塞审阅请求；缓存失败持久化，证据变更会将旧缓存与交付包标记失效。
- 只读动态采样时队列为 `ready=324`、`pending=224`、`processing=1`、`failed=3`；缓存目录有 2284 个文件，约 357MB。失败项均为外部 HTTP 图片源在两次尝试后仍不可用，未输出签名 URL，不影响 worker 继续串行处理其余队列。
- 生产安全审计通过，PostgreSQL `select 1` 成功；未在真实生产数据上制造审阅、条码变更或归档验收写入，对应回归由自动化测试和独立审阅覆盖。
- 发布目录保留策略已执行，服务器仅保留最近 5 个 release。

## 回滚

- 应用回滚目标为 `/opt/module-manager-v2/releases/v3.0.84-20260722_033636`。
- 回滚不覆盖 `.env`、data 或 uploads；应用回滚保留 V3.1.0 数据库结构，数据库整体恢复只能使用已验证的升级前备份并经单独批准。
