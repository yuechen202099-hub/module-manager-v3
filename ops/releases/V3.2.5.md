# V3.2.5 Production Release Record

- Status: pending
- Local Verification: passed
- Package: passed
- Production Deployment: pending
- Production Reconciliation: pending
- Rollback target: V3.2.2
- Candidate branch: `production/V3/3.2.5`
- Deployed production baseline: `V3.2.2`
- Candidate version: `V3.2.5`
- Database head: `20260724_0014`; no Alembic migration

## Bounded repair

- V3.2.4 trial `v324-external-20260819T233526Z` stopped fail-closed with 10 declared_hash_mismatch; uploaded/reused/committed: 0 / 0 / 0.
- Read-only production classification found exact historical identities only: Formula A: 1999 and Formula B: 3.
- The V3.2.5 gate accepts only the two exact historical formulas in addition to the pre-existing empty, source-URL, and downloaded-content cases; every arbitrary 64-hex mismatch remains rejected.
- The locked identity revalidation covers every Formula A/B input before commit, including group and photo legacy IDs, source fingerprint/URL, image file ID, team, and image URL.
- The canonical downloaded-content SHA256 remains the OSS key/metadata and new database `Photo.sha256`; the old synthetic value is preserved in `pre_oss_sha256` for audit and rollback.
- Export retirement, OSS transfer primitives, local export behavior, and Alembic remain unchanged; there is no Alembic migration and head stays `20260724_0014`.

## V3.2.4 trial reconciliation

- Application rollback restored the V3.2.2 production baseline; production reconciled to V3.2.2 with zero persistent database/OSS effects.
- Trial counters were zero for upload, reuse, commit, database migration rows, and OSS mutation; the ten mismatches stopped before the OSS store boundary.
- V3.2.5 lifecycle remains pending for upload, deployment, and production execution. Local verification and package are passed; both production lifecycle fields remain pending until separately authorized phases complete.

## Fresh local evidence (passed)

- Final reviewed and packaged source commit: `ef4394c687610c2ba4f65423c4801f1138c7ad41`; tracked worktree and index were clean before and after local verification.
- `v2-api` and `v2-web` at the packaged source are byte-identical to source-review commit `fe9f605ea5f2dbd6f44fccf86ea8490366713800`. The recorded complete backend result remains source-bound: `1439 passed, 35 skipped, 87 warnings in 243.83s`.
- Fresh complete root `scripts` suite: `892 passed, 22 skipped, 1 warning in 131.63s`, exit `0`. The warning is the deliberate duplicate-ZIP-member rejection fixture.
- Fresh complete Phase A migration file: `68 passed, 7 skipped in 1.79s`, exit `0`; all seven skips explicitly require the opt-in `ROUND3_POSTGRES_TEST_URL` isolated PostgreSQL fixture and are not counted as passes.
- Active V3.2.5 release contract, release SOP, administrator release-note, and strict-native Vue gates all exited `0`; strict-native reported six registered pages and zero legacy bridges.
- Vue `type-check` and production build both exited `0`; build transformed `1670` modules. Only the existing VueUse annotation notices and `>500 kB` chunk advisory were emitted.
- Post-build static attestation contains exactly `27` assets; actual file set, tracked membership, size, and SHA256 all match. Entry `assets/index-vYjKWucj.js` has SHA256 `267d052ddff1493124dc55e42fd597b9e277b94f358f810f3432c517a9ff4b68`.
- After the build, active V3.2.5 and strict-native Vue gates passed again, HEAD remained the packaged source commit, and no tracked drift was produced.

## Source-bound performance evidence (passed)

- Path: `build/release-evidence/v3.2.5-task-review.json`
- Size: `1290 bytes`
- SHA256: `a0a0da122d0c7b292e586781051f5870ad3924303ee995488ed841dc04195035`
- Source commit: `ef4394c687610c2ba4f65423c4801f1138c7ad41`
- A unique JSON state was initialized through supported `POST /local-test/bootstrap`, HTTP `200`; refreshed and ordinary task snapshots each contained `135` tasks.
- The official demo seed selected task `67` and group `g-22362`; exactly one task was assigned to `reviewer`.
- The sampler ran the full `60 seconds`: `ok=true`, failures `0`, routes `2`, one unchanged cache instance, snapshot version/generation reused, and all thresholds passed.
- Task snapshot was `17.393 ms` against `300 ms`, with `135` items and `1710552` serialized bytes. Review groups were `4.777 ms` against `1500 ms`, with page count `20`, total `88`, and `22426` serialized bytes. Task builds were `1`, count `3 -> 4`.
- Active `verify_v3_1_release.py --expected-source-commit ef4394c687610c2ba4f65423c4801f1138c7ad41` passed all ten gates.
- Foreground Uvicorn shut down, port `127.0.0.1:8013` was released, and the exact unique temporary state was deleted. No user PostgreSQL, production endpoint, or OSS endpoint was accessed.

## Package evidence (passed)

- Path: `build/server-release/module-manager-v2-server-3.2.5.zip`
- Size: `1735393 bytes`
- SHA256: `f741aa8d58947c4cc3490d3dc72ea1b3ae99847180a20ee6e41153f2bd55c6b7`
- ZIP file members: `389`; uncompressed bytes: `6963471`.
- The package builder passed its branch guard, dependency check, source-bound performance verifier, active/compatibility release gates, complete client smoke, and Vue build.
- Official `verify-client-release.py --expected-source-commit ef4394c687610c2ba4f65423c4801f1138c7ad41`: exit `0`; required members `192`, missing `0`, exact `SOURCE_COMMIT`, all production safety notes present, and forbidden-local/cache scan clean.
- Independent member/metadata audit: exact duplicates `0`, case-insensitive collisions `0`, unsafe/absolute/traversal names `0`, symlinks `0`, bad CRC members `0`, encrypted members `0`, and unexplained untracked members `0`.
- Source audit classified `234` members as exact Git blobs, `153` as Windows-checkout CRLF-only differences, one generated `RELEASE_MANIFEST.md`, and one generated `SOURCE_COMMIT`; unexplained content differences `0`.
- Named V3.2.5 verifier/tests, Phase A migration/tests, release record, runtime/static assets, manifest, and SOP surfaces were all present. ZIP Vue assets were `27 / 27` with exact set, size, SHA256, entry reference, and entry hash.
- `.env`, credentials, private keys, data/uploads/runtime/cache/build outputs, reports/allowlists, embedded performance evidence, generated ZIP/XLSX/CSV outputs, database dumps/logs/bytecode, and legacy static HTML members were all `0`.
- Secret audit found no unexplained credential. Four Alibaba-shaped occurrences were one fixed Phase A redaction-test fixture whose assertions require removal from reports; all `105` generic key-shaped candidates were test/smoke fixtures or demo placeholders. Real-secret verdict: `CLEAN`.
- Independent read-only reviewer: Critical `0`, Important `0`, Minor `0`; verdict `PERMIT_CANDIDATE_ATTESTATION`.
- The local-only Phase B guard and its ignored test artifacts are not release source and are absent from the package.

## Local attestation

- Packaged source commit is `ef4394c687610c2ba4f65423c4801f1138c7ad41`. The documentation-only commit immediately following it in Git history is the candidate attestation; it is intentionally not self-referenced inside its own content.
- The only tracked path allowed between packaged source and attestation is `ops/releases/V3.2.5.md`. Performance evidence and the ZIP remain ignored local artifacts.

## Production operations (not executed)

- No SSH, production access, upload, deployment, service switch/restart, database or OSS operation, migration, export, release cleanup, tag, or push was performed.
- No production release changed. `Production Deployment` and `Production Reconciliation` remain `pending`; rollback target remains `V3.2.2`.
