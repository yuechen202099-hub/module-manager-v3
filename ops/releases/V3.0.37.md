# V3.0.37 Production Release Record

## Summary

- Version: `V3.0.37`
- Release type: small production feature, `+0.01`
- Branch: `production/v3.0.35`
- Commit: `212bbc9 feat: release production v3.0.37 photo barcode accuracy`
- Production release directory: `/opt/module-manager-v2/releases/v3.0.37-20260627_013909`
- Previous release: `/opt/module-manager-v2/releases/v3.0.36-20260626_234036`

## User Requirement

After review photo classification, scan barcode-bearing photos once, compare scanned values with meter, collector, or module fields, and show `图片准确率` in the dashboard.

Non-barcode photos must not reduce image accuracy.

## Changed Scope

- Added barcode photo check service.
- Added dashboard photo accuracy fields.
- Integrated checks into photo classification and bulk archive.
- Preserved existing scanned barcode metadata during bulk archive.
- Added tests and verification script.
- Pinned `zxing-cpp==3.0.0`.

## Package

- Local package: `build/server-release/module-manager-v2-server-3.0.37.zip`
- SHA256: `BBFDA2F53354006F067E03F0E83642510FADDB3FDBE0D8F8338A5AFB80F786E5`
- Server package: `/tmp/module-manager-v2-server-3.0.37.zip`
- Server SHA256 matched local hash.

## Verification

- Backend target tests: `136 passed, 1 warning`
- `node scripts/verify_photo_barcode_accuracy.js`: passed
- `node scripts/verify_admin_release_notes.js`: passed
- `node scripts/verify_admin_group_search_display.js`: passed
- `npm run build`: passed with existing Rollup PURE and chunk-size warnings
- `git diff --check`: passed with CRLF warnings only
- Release package verification: passed
- Vue static asset reference check: passed

## Subagent Review

Result: Ready with fixes.

Fixed before deployment:

- Non-barcode categories no longer call scanner.
- Bulk archive preserves existing barcode check metadata.
- Postgres dashboard accuracy filters to grouped active photos.
- `zxing-cpp` pinned to `3.0.0`.
- Vue hashed static assets were staged and tracked.

Rejected with reason:

- `delete_group_photo()` returning `reviewer=None` was kept because existing regression tests protect that contract.

## Production Backup

- Backup directory: `/opt/module-manager-v2/backups/v3.0.37-pre-20260627_013826`
- Included `.env`, current release archive, `data`, `uploads`, PostgreSQL custom dump, schema dump, and `SHA256SUMS`.

## Production Checks

- Current symlink: `/opt/module-manager-v2/releases/v3.0.37-20260627_013909`
- `module-manager-v2.service`: active
- `nginx`: active
- Local `/health`: `200`
- Public `/health`: `200`
- Public `/project-board`: `200`
- Project board title: `Module Manager V3.0.37`
- Static assets contain `图片准确率`: yes
- Admin `/local-test/system/status`: version `3.0.37`
- Admin `/local-test/summary`: photo accuracy fields present

## Rollback

Rollback target:

```text
/opt/module-manager-v2/releases/v3.0.36-20260626_234036
```

Rollback command:

```bash
ln -sfn /opt/module-manager-v2/releases/v3.0.36-20260626_234036 /opt/module-manager-v2/current
systemctl restart module-manager-v2.service
curl -fsS http://127.0.0.1/health
```

Do not restore database or uploads without separate user approval.

## Remaining Notes

- Root `RELEASE_MANIFEST.md` was historically stale before this SOP cleanup; generated package manifests remain authoritative per package.
- Documentation-only SOP cleanup after this release does not require app version bump.
