# V3.2.8 Production Release Record

## Summary

- Status: pending
- Local Verification: passed
- Package: pending
- Production Deployment: pending
- Production Reconciliation: pending
- Rollback target: V3.2.7
- Candidate branch: `production/V3/3.2.8`
- Deployed production baseline: `V3.2.7`
- Candidate version: `V3.2.8`
- Database head: `20260824_0016 (head)`

## Change scope

- Inventory scan and photo registration now use a bounded single-collector lookup.
- Run source projection now uses compact selected-column streaming.
- Collector inventory opens media without native `BarcodeDetector` and falls back to the pinned QuaggaJS scanner while retaining preview/manual entry on recognition failure.
- The client-platform relay is now a global terminal workbench at `/collector-workbench`; it no longer depends on project/batch selection or batch creation.
- The retired `/collector-batches` client entry redirects to `/collector-workbench`, and the obsolete `CollectorBatchManagementView.vue` is absent from source and release gates.
- Terminal snapshots distinguish `有实物`, `无实物`, and `已替换`; direct-photo guidance is explicit, while administrator-only replacement and rollback stay hidden from constructor accounts.
- The terminal picker keeps opaque terminal identity separate from its user-visible value: ordinary candidates show `终端号 · 地址`, while only duplicate-number candidates add the project name for disambiguation.
- project/team isolation, photo-first precedence, mobile no-batch behavior, and API contracts are unchanged.
- Schema remains `20260824_0016`; this hotfix adds no migration.

## Local verification

- Collector backend matrix: `165 passed, 12 skipped, 1 warning in 8.97s`.
- PostgreSQL skip audit: all 12 skips were explicitly gated by unset `COLLECTOR_TRANSFER_POSTGRES_TEST_URL`.
- Frontend collector-transfer Node suite: `12 passed`.
- Focused collector workbench review-fix suite: `16 passed` after a three-test RED proving the previous opaque-key exposure.
- Frontend collector-transfer Vitest suite: `51 passed`.
- Frontend component suite: `5 passed`.
- Frontend type-check: passed.
- Frontend production build: passed; only acknowledged third-party Rollup `#__PURE__` annotation and chunk-size warnings were emitted.
- Direct release-tool suite: `754 passed, 1 warning`.
- Original three stale-release-gate failures after correction: `3 passed`.
- Whole repository: `2622 passed, 69 skipped, 88 warnings in 447.70s`.

## Browser acceptance

- Local-only administrator desktop acceptance confirmed page identity, meaningful DOM, all three collector states, replacement/rollback controls, and zero camera/video controls on the workbench.
- Local-only constructor acceptance confirmed all three states and direct-photo guidance while replace, refresh, and rollback button counts were each zero.
- Constructor desktop and `390x844` responsive checks exercised terminal selection and previous/next navigation; Browser console `error` and `warn` logs remained empty.
- `/collector-batches` resolved to final URL `/collector-workbench`.
- `/collector-inventory` independently rendered a video preview element and `打开摄像头扫码`; physical camera permission was not requested or accepted, so real-device camera startup/teardown is not claimed.
- Review-fix component coverage confirms the initial and selected input values plus datalist option values are readable, duplicate-aware labels while the open request remains bound to the original opaque identity.
- Review-fix Browser recheck used a fresh authenticated Codex In-app Browser tab directly at `/collector-workbench`: desktop and `390x844` both rendered input value `TASK7-001 · 本地验收地址 88 号`, the datalist exposed only the same readable label, no opaque key was found, all three collector states remained in DOM, and console `error` / `warn` logs were empty.
- The prior encoded-key screenshots are invalidated as pre-fix evidence. Post-fix desktop and mobile screenshots were emitted in the controller Browser session but were not saved as repository artifacts; the temporary viewport override was reset.
- Fixture concern: constructor login briefly reached the out-of-scope legacy `/construction` page, whose local SQLite fixture emitted 500 responses for an unsupported `DISTINCT` aggregate form and a missing legacy task `67`. The collector workbench, redirect, and inventory acceptance requests were 200; no production system or data was used.

## Source and package

- Source commit: pending
- Archive file: `build/server-release/module-manager-v2-server-3.2.8.zip`
- SHA256: pending

## Production acceptance

- production acceptance is pending.
- All production steps remain pending: deployment, backup, database migration confirmation, exactly one guarded smoke, live `390x844` acceptance, reconciliation, maintenance restoration, and attestation.
- Live deployment evidence, reconciliation, and attestation remain explicitly pending.
- The immutable V3.2.7 package and release directory documented in `ops/releases/V3.2.7.md` remain the rollback evidence.
