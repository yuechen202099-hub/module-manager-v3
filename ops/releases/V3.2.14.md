# V3.2.14 Production Release Record

## Summary

- Status: attested
- Local Verification: passed
- Package: passed
- Production Deployment: passed
- Production Reconciliation: passed
- Rollback target: V3.2.13
- Candidate branch: `production/V3/3.2.14`
- Deployed production baseline: `V3.2.13`
- Candidate version: `V3.2.14`
- Database head: `20260824_0016 (head)`
- User request: complete the review/re-photo changes, reuse one collector photo from the corresponding material group for a present physical collector, and release the result to production.
- Priority: production release after the collector-inventory and construction scanner hotfixes.

## Scope

- Keep safe constructed meter re-photo sources available when unrelated rows are unconstructed or lack a source collector number or source collector photo.
- Hide completed meter and collector re-photo rows by default while preserving completion status, actor, timestamp, and audit history.
- Add an administrator-only manual collector demand quantity that allocates atomically from the current project's eligible pool, writes nothing on shortage, and reuses the existing audit and rollback path.
- Reuse one authorized source-group collector photo as read-only reference material for a present physical collector without creating a pool photo or changing allocation eligibility.
- Preserve V3.2.13 Android, inventory-scanner, classification-independent rephoto, and large-image regressions.
- Preserve V3.2.13 as the immutable production baseline and rollback boundary.
- No database migration, production business-data rewrite, camera request, or client-platform request is part of source preparation.
- V3.2.14 has not been deployed to production.

## Local verification

- Backend full suite: `1716 passed, 49 skipped`; the warnings are the existing Starlette/httpx and barcode-library deprecations.
- Collector frontend verification: Node `13 passed`; focused Vitest `95 passed`; component suite `21 passed`.
- Type check and Vite production build: passed; the build reported only the existing dependency annotation and chunk-size warnings.
- V3.2.14 source verifier: `58 passed`; generic package and release-SOP gate: `709 passed`.
- Source verifier tests: passed
- Generic client-package verifier tests: passed
- SOP verifier tests: passed
- Source phase: passed
- SOP source phase: passed
- Git diff check: passed
- Local Browser acceptance: passed at `http://127.0.0.1:8000/review-workbench` with an isolated SQLite database and local storage. The existing platform shell rendered V3.2.14, the page console was clean after authenticated reload, and no framework overlay appeared.
- Local Browser interaction: a present collector reused `/static/demo-assets/review-photo-3.svg` from its linked material group, opened in the existing large-image dialog, and created no pool photo. Adding manual demand quantity `1` allocated the only eligible project pool collector and refreshed authoritative state. Completing the present collector and meter hid each row immediately from the default workbench list.
- Local Browser isolation: the review/re-photo main surface contained `0` video elements, `0` file inputs, and no camera/scan/photo-capture buttons; the page explicitly stated that it does not connect to the client platform.

## Source and package

- Source commit: 7f8218371f3afdfc9b448535883deba25a35a8eb
- Archive file: build/server-release/module-manager-v2-server-3.2.14.zip
- SHA256: bdeef95730fbe7704be7878dcde0d5456a67c0be1c78c67b809ae82375376981
- Server SHA256: bdeef95730fbe7704be7878dcde0d5456a67c0be1c78c67b809ae82375376981
- Build command: passed
- Archive verification result: passed
- Archive size: `2198285` bytes.
- Archive verification: `270` required files and `457` ZIP members; all members streamed successfully, with zero case-folded path collisions and zero forbidden files.
The archive `SOURCE_COMMIT` equals `7f8218371f3afdfc9b448535883deba25a35a8eb`.

## Production deployment

- Backup directory: /opt/module-manager-v2/backups/v3.2.14-20260828T112839Z
- Backup verification: passed
- Release directory: /opt/module-manager-v2/releases/v3.2.14-20260828T113306Z
- Rollback directory: /opt/module-manager-v2/releases/v3.2.13-20260828T020636Z
- Uvicorn readiness: 127.0.0.1:8000 ready
- Local health: HTTP 200 version 3.2.14
- Public health: HTTP 200 version 3.2.14
- Public health check: https://www.sgcc.online/health HTTP 200 version 3.2.14
- Browser viewport: 1280x720 passed
- Authorization acceptance: passed
- Zero-write acceptance: passed
- Camera requests: 0
- Client-platform requests: 0
- Maintenance restoration: passed
- Soak health checks: passed
- Attestation: passed

Production evidence:

- The canonical backup retained `.env`, PostgreSQL custom and schema dumps, shared data and uploads, and the complete V3.2.13 rollback release. All seven SHA256 entries passed; `pg_restore -l` listed `316` entries and the data archive listed `10234` members.
- The local and server package hashes matched. The server ZIP passed integrity checks, contained `457` members, and exposed the frozen source commit before extraction.
- The first guarded switch correctly rolled back to V3.2.13 when the hardened release lacked the existing shared `v2-api/app/static/uploads` link. V3.2.13 recovered on a real `127.0.0.1:8000` listener, and the maintenance worker and timer returned to their captured active state.
- The release layout was repaired using the V3.2.13 deployment pattern: only a root-owned link to `/opt/module-manager-v2/uploads` was added. The deployment upload contract changed from its expected failure to pass, and the service account imported application version `3.2.14` before the second switch.
- The second atomic switch started PID `12264` from `/opt/module-manager-v2/releases/v3.2.14-20260828T113306Z/v2-api`; the listener, local health, Nginx, maintenance service, and maintenance timer all passed. The enqueue service remained inactive, matching its captured state.
- Production readiness, security audit, and `production_health_check.py --expected-version 3.2.14` passed. The official health script confirmed the authenticated system version, public-route contract, retired exports, hidden API documentation, and barcode-accuracy summary.
- Public HTTPS `/health`, `/login`, `/project-board`, `/review-workbench`, `/collector-inventory`, `/construction`, and `/static/vendor/quagga.min.js` returned `200`; `/docs`, `/redoc`, and `/openapi.json` returned `404`. Unauthenticated local and public collector-workbench API requests returned `401`.
- The previously verified local `1280x720` exact-bundle viewport passed. Production browser navigation was blocked by the browser URL policy and was not bypassed; production delivery was instead bound to that exact bundle by matching the entry asset SHA256 across the release file, local HTTP, and public HTTPS.
- The deployed Vue entry is `assets/index-BAufJdkd.js`; file, local HTTP, and public HTTPS SHA256 all equal `f4b6ace1586b359676d1eb6b495f81225ad417e3eec138dd3ab0534d7b2e4cc4`.
- Alembic current and heads both remained `20260824_0016 (head)`. No migration, downgrade, production collector allocation, manual demand, completion, rollback mutation, camera request, or client-platform request was run during production acceptance.
- Three ten-second soak samples kept PID `12264`, one listener, version `3.2.14`, and RSS at `322616 KiB`; the current process contained zero `ERROR`, `Traceback`, or `Exception` journal lines.
- Retention dry-run named only `/opt/module-manager-v2/releases/v3.2.9-20260826T075447Z`; cleanup removed that old release and retained V3.2.14 through V3.2.10, including the V3.2.13 rollback target. Backups, `.env`, shared data, uploads, OSS, and the incoming package were not deleted.
- Final read-only review reported Critical `0`, Important `0`, Minor `2`, with release allowed. The remaining minor risks are the service-internal float quantity edge outside the strict public API and transient cross-terminal button disabling during another terminal request; neither can misallocate production data through the public route.

## Rollback

- Restore `/opt/module-manager-v2/current` to `/opt/module-manager-v2/releases/v3.2.13-20260828T020636Z`.
- Wait for a real Uvicorn listener on `127.0.0.1:8000`, then verify local and public health report `3.2.13`.
- Keep Alembic at `20260824_0016`; do not run a downgrade.
