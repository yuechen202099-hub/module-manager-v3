# V3.2.3 Production Release Record

- Status: pending
- Local Verification: passed
- Package: passed
- Production Deployment: pending
- Production Reconciliation: pending
- Rollback target: V3.2.2
- 候选分支：`production/V3/3.2.3`
- 当前生产基线：`V3.2.2`
- 候选版本：`V3.2.3`
- 数据库版本：`20260724_0014 (head)`；no Alembic migration

## 候选变更

- 导出中心和全部旧浏览器业务导出入口统一退休，Nginx 与应用层对固定路径返回 `410 Gone`。
- 浏览器 KPI CSV 和服务器交付包/最终交付制品任务停止生成；现有导出表、历史行、缓存和 OSS 对象保持不变。
- 有效 `external_url` 照片由独立单 worker 工具迁移到内容地址 OSS，发布过程不自动执行迁移。
- 服务器只读生成 header-first manifest，本机直接从 OSS 下载并组装 files/ZIP/CSV/XLSX。

## 发布边界

- 本记录是候选状态；生产部署和生产对账证据保持 pending，由后续发布任务填写。
- 应用回滚目标为 V3.2.2；回滚应用时保留 Nginx 导出退休拦截。
- Alembic head 必须保持 `20260724_0014`，不新增 migration，不删除表、历史行、任务、缓存或 OSS 对象。
- 发布包不得包含 `.env`、PEM/key、数据库 dump、data、uploads、交付缓存、allowlist、迁移报告或本机导出产物。

## 本地门禁（已通过）

- 最终 reviewed source commit：`ef2b3697dfffcbe449b451943948a61c3bdf71b5`。
- 独立审查累计 Addressed `7 + 1`、最终 Open `0`；package-blocker scoped re-review 审阅 HEAD 为 `ef2b3697dfffcbe449b451943948a61c3bdf71b5`。
- focused backend：`574 passed, 15 skipped, 87 warnings`；15 个 skip 均为 Windows 平台能力差异（2 个 POSIX ownership、9 个 descriptor-relative link/unlink、4 个 open-file pathname replacement），无 PostgreSQL/env/selection skip。
- focused scripts（Nginx patcher、本机 OSS exporter、V3.2.3 verifier）：`178 passed, 0 skipped`。
- complete backend：`1435 passed, 15 skipped, 87 warnings`；19 个隔离真实 PostgreSQL opt-in cases 全部激活（Task 6 为 8、migration 为 7、Round 3 为 2、Round 4 为 2）。
- complete scripts：`796 passed, 0 skipped, 1 warning`；warning 为故意构造 duplicate ZIP member 的拒绝测试。
- 8 个 static release gates 全部退出 `0`；Vue type-check/build 通过，build 转换 `1670` modules。
- build 后 release verifier 与 strict Vue gate 再次通过；27 个 generated assets 的 size/SHA256 全部一致，entry `assets/index-CHHY9zoG.js` SHA256 为 `acf06168f380c0870f54e6f18f7bda8db7e0a4a494be7d8fc0993c0cb1f9f056`，export chunk/forbidden marker 均为 0 matches。
- `production/V3/3.2.2...HEAD` 中 44 个现存 Python 路径全部通过 `py_compile`；`git diff --check` 与 source-tree clean gate 通过。

## Source-bound 性能证据（已通过）

- 路径：`build/release-evidence/v3.2.3-task-review.json`
- 大小：`1290 bytes`
- SHA256：`57119ed9afe549025e0b7b7940c5a6e1115c6e1933b9f4858452a6c372bc9fc3`
- source commit：`ef2b3697dfffcbe449b451943948a61c3bdf71b5`
- sampler：`60 seconds`，`ok = true`，failures `0`。
- measurements：task snapshot `17.578 ms`，review groups `4.842 ms`，review group count `20`（total `88`），task builds in 60 seconds `1`。
- real route data：task snapshot items `135`，review page items `20`；snapshot version/generation 均复用。
- `verify_v3_1_release.py --expected-source-commit ef2b3697dfffcbe449b451943948a61c3bdf71b5`：10 个 gate 全部通过。

## 发布包证据（已通过）

- 路径：`build/server-release/module-manager-v2-server-3.2.3.zip`
- 大小：`1704663 bytes`
- SHA256：`7d727aa3858a72404eb135e23d60106fe109c71dbf7ccac201c6cf22c1366e2d`
- `verify-client-release.py --expected-source-commit ef2b3697dfffcbe449b451943948a61c3bdf71b5`：通过。
- required members：`184`；forbidden local/cache/secret/runtime members：`0`。
- ZIP 内 `SOURCE_COMMIT`、performance evidence source commit 与最终 reviewed source commit 均为 `ef2b3697dfffcbe449b451943948a61c3bdf71b5`。
- 性能 JSON 与 ZIP 均保留在 ignored `build/` 下，不进入 Git commit。

## 本地 attestation

- 本记录之后紧接的 documentation-only commit 即 V3.2.3 候选 attestation；其精确 SHA 从 Git history 与 SDD ledger 解析，不在该 commit 内自引用，也不为记录自身 SHA 创建第二个文档提交。
- packaged source commit 为 `ef2b3697dfffcbe449b451943948a61c3bdf71b5`；attestation commit 与它之间唯一允许的 tracked path 是 `ops/releases/V3.2.3.md`。

## 生产操作（未授权且未执行）

- 未访问生产服务器、真实 PostgreSQL 或真实 OSS。
- Task 1 已在更早阶段安装生产 Nginx `410 Gone` 导出退休拦截；本次 Task 10 未执行 SSH、应用部署、数据库或 OSS 操作。
- 已在本机生成并独立验证候选发布包，但未上传或部署。
- 未切换 release、重启服务、运行外链迁移或清理历史 release。
