# V3.2.3 Production Release Record

- Status: pending
- Local Verification: passed
- Package: pending
- Production Deployment: pending
- Production Reconciliation: pending
- Rollback target: V3.2.2
- 候选分支：`production/V3/3.2.3`
- 当前生产基线：`V3.2.2`
- 候选版本：`V3.2.3`
- 数据库版本：`20260724_0014 (head)`；no Alembic migration

## 候选变更

- 导出中心和全部旧浏览器业务导出入口统一退休，Nginx 与应用层对固定路径返回 `410 Gone`。
- 浏览器 KPI CSV 和服务器交付包/最终交付制品任务停止生成；现有导出表、历史行、缓存和 OSS 对象保持不变。
- 有效 `external_url` 照片由独立单 worker 工具迁移到内容地址 OSS，发布过程不自动执行迁移。
- 服务器只读生成 header-first manifest，本机直接从 OSS 下载并组装 files/ZIP/CSV/XLSX。

## 发布边界

- 本记录是候选状态；生产部署和生产对账证据保持 pending，由后续发布任务填写。
- 应用回滚目标为 V3.2.2；回滚应用时保留 Nginx 导出退休拦截。
- Alembic head 必须保持 `20260724_0014`，不新增 migration，不删除表、历史行、任务、缓存或 OSS 对象。
- 发布包不得包含 `.env`、PEM/key、数据库 dump、data、uploads、交付缓存、allowlist、迁移报告或本机导出产物。

## 本地门禁（已通过）

- V3.2.3 release verifier、package verifier和 SOP verifier 已通过。
- 相关后端、退休生命周期与发布合同测试已通过。
- Vue type-check、production build 和 generated static asset attestation 已通过。
- `py_compile`、`git diff --check` 和 explicit-path membership audit 已通过。
- 以上仅证明 Local Verification；正式 Package、Production Deployment 和 Production Reconciliation 仍为 pending。

## 生产操作（未授权且未执行）

- 未访问生产服务器、真实 PostgreSQL 或真实 OSS。
- 未生成或上传正式发布包。
- 未切换 release、重启服务、运行外链迁移或清理历史 release。
