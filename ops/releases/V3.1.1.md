# V3.1.1 Production Release Record

- Status: reviewed, packaged, deployed, and verified in production
- Local Verification: passed
- Package: verified
- Production Deployment: completed
- Production Reconciliation: completed
- Rollback target: V3.1.0

## 更新内容

- 修复后台条码核验 worker 将数据库 UUID 传给只接受业务资料组 ID 的回写接口，导致生产队列连续失败的问题。
- 修复自动归档 claim/process 的同类身份错误：数据库 UUID 仅用于租约与失败回写，业务 `legacy_id` 用于正式归档。
- 预览统计与实际入队共用资料组身份归一化逻辑，统一识别施工填报字段、顶层字段和照片字段中的采集器号、模块号。
- 空白字符串不再遮蔽有效兜底值，占位值仍由资格判定拒绝；worker 查询资料组增加 `team_id` 约束。
- 保留审阅完成后异步创建原图与交付包缓存的 V3.1.0 行为，不在审阅请求中同步下载原图。

## 本地验证

- 完整测试：`1656 passed, 13 skipped`。
- 条码 worker、核验资格、状态仓库和 V3.1 专项：`407 passed`。
- 发布与 API 门禁：`726 passed`。
- 任务快照 `2.260ms`，审阅分页 `23.959ms`，60 秒内快照构建 1 次；报告绑定提交 `f0181977ed734844382c9837b53b1b0e87618ae0`。
- 前端 `vue-tsc --noEmit` 与 Vite 正式构建通过；25 个 JS 源码门禁、严格 Vue 路由、生产样例、发布 SOP、保留策略和 `git diff --check` 通过。
- 最终独立审阅：`Critical=0`、`Important=0`、`Minor=0`、`Ready to release=yes`。

## 发布包

| 项目 | 结果 |
| --- | --- |
| 文件 | `build/server-release/module-manager-v2-server-3.1.1.zip` |
| 生成时间 | `2026-07-23 17:07:29 +08:00` |
| 大小 | `1709215 bytes` |
| SHA256 | 02F642ECDB4CE0BBCA7FCE3EA28AF5C190E4D9C3863D9A7E61F7CF59B9BF7DE8 |
| 验包 | 通过；144 个必需文件，源码提交绑定正确，无本地缓存或敏感文件 |

## 生产部署

| 项目 | 结果 |
| --- | --- |
| Backup directory | /opt/module-manager-v2/backups/V3.1.1-pre-20260723_170933 |
| 备份校验 | `SHA256SUMS` 全部通过；PostgreSQL dump 清单 219 行 |
| Release directory | /opt/module-manager-v2/releases/v3.1.1-20260723_171150 |
| 回滚 release | `/opt/module-manager-v2/releases/v3.1.0-20260723_153935` |
| 数据库迁移 | `20260723_0012` |
| Public health check | https://www.sgcc.online/health passed |

## 部署过程

- 发布前队列保持 `paused=true`，状态为 `failed=42`、`pending=2382`、`processing=0`；未删除队列、资料组、照片或 OSS 对象。
- 首次切换时，候选目录缺少现网 `v2-api/app/static/uploads -> /opt/module-manager-v2/uploads` 运行期链接，主服务因无权创建目录而启动失败；发布脚本自动切回 V3.1.0，公网健康恢复为 200。
- 补齐共享 uploads 链接并在生产环境完成应用导入预检后再次切换，V3.1.1 主服务与低负载 worker 均启动成功。
- 全量入队通过受限 systemd oneshot 执行，42 个旧失败项全部重置为待处理；随后由 `codex-release-v3.1.1` 留痕解除维护暂停。
- worker 保持单进程、每批最多 20 组、串行、批后等待 5 秒、CPU 20%、内存 512MB；午夜定时器继续生效。
- 发布目录保留策略先 dry-run 后执行，服务器仅保留最近 5 个 release；临时上传 ZIP 已删除。

## 线上验收

- `/health`、`/login`、`/project-board`、`/task-hall`、`/construction`、`/claim-tasks` 内外网均返回 `200`；`/docs`、`/redoc`、`/openapi.json` 返回 `404`。
- 登录页标题和生产静态资源显示 `V3.1.1`，入口资源为 `assets/index-DEYqhiHQ.js`。
- 数据库 `select 1` 成功，迁移头保持 `20260723_0012`。
- 恢复后低频采样已完成 15 组：`passed=12`、`partial=3`、`failed=0`；自动归档 8 组，交付缓存 `ready` 从 557 增至 562。
- 最终主服务与 worker 的 `NRestarts=0`，worker 日志无 warning，服务器负载采样为 `0.16 / 0.27 / 0.19`。
- 未在真实生产资料组上制造审阅、条码修改或归档测试写入；审阅完成后自动缓存由回归测试、独立审阅、现有交付缓存队列和线上只读状态共同验证。

## 回滚

- 应用回滚目标为 `/opt/module-manager-v2/releases/v3.1.0-20260723_153935`。
- 回滚不覆盖 `.env`、data、uploads、PostgreSQL 或 OSS；数据库整体恢复仅允许使用已验证备份并经单独批准。
