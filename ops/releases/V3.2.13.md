# V3.2.13 Production Release Record

## Summary

- Status: attested
- Local Verification: passed
- Package: passed
- Production Deployment: passed
- Production Reconciliation: passed
- Rollback target: V3.2.12
- Candidate branch: `production/V3/3.2.13`
- Deployed production baseline: `V3.2.12`
- Candidate version: `V3.2.13`
- Database head: `20260824_0016 (head)`

## Scope

- Keep pending photo classification visible while allowing complete rephoto material to be used immediately.
- Keep hard source anomalies, missing or duplicate required photos, identity mismatches, and collector conflicts blocking rephoto.
- Exclude review-only status, barcode confirmation, and manual-classification confirmation from the rephoto source revision while retaining photo, identifier, and category evidence changes.
- Add a reusable large-image lightbox for classification, meter rephoto, and collector rephoto photos.
- Fetch the protected original classification photo on demand and retain the preview as a fallback when the original cannot be loaded.
- Preserve V3.2.12 as the immutable production baseline and rollback boundary.
- No database migration and no production business-data rewrite.

## Local verification

- Backend full suite: `1700 passed, 49 skipped`; the reported warnings are existing Starlette/httpx and barcode-library deprecations.
- Frontend component suite: `21 passed`.
- Collector state tests: `13 passed`; focused collector/review Vitest suite: `87 passed`.
- Type check and Vite production build: passed; the build reported only dependency annotation and existing chunk-size warnings.
- V3.2.13, release-SOP, generic client-package, and V3.1 compatibility contract suite: `751 passed`.
- Release archive contains `260` required files and `452` ZIP members; ZIP integrity, source-commit binding, static asset size, and SHA256 checks passed.

## Source and package

- Source commit: `7d9af463a52513dd0e7c3f3850bf6d07eba03d34`
- Archive file: `build/server-release/module-manager-v2-server-3.2.13.zip`
- SHA256: `40e437a6da9e533e8faadc0af3b8c8232a0faf454365069a31792968f2dba4bc`
- Server SHA256: `40e437a6da9e533e8faadc0af3b8c8232a0faf454365069a31792968f2dba4bc`

## Production deployment

- Backup directory: `/opt/module-manager-v2/backups/V3.2.13-pre-20260828_100309`
- Backup verification: passed
- Release directory: `/opt/module-manager-v2/releases/v3.2.13-20260828T020636Z`
- Rollback directory: `/opt/module-manager-v2/releases/v3.2.12-20260827T160539Z`
- Uvicorn readiness: `127.0.0.1:8000 ready`
- Local health: passed; `production_health_check.py --expected-version 3.2.13` confirmed the authenticated system version, public route contract, retired exports, hidden API documentation, and barcode-accuracy summary.
- Public health: passed; HTTPS `/health`, `/login`, `/project-board`, `/review-workbench`, `/collector-inventory`, `/construction`, and `/static/vendor/quagga.min.js` returned `200`.
- Browser viewport: production SPA shell passed at `1280x720`; `/review-workbench` redirected unauthenticated access to `/login?redirect=/review-workbench`, displayed `V3.2.13`, loaded `assets/index-Dms37Iv7.js`, and produced no browser warnings or errors.
- Authorization acceptance: passed
- Zero-write acceptance: passed
- Camera requests: 0
- Client-platform requests: 0
- Maintenance restoration: passed
- Soak health checks: passed
- Attestation: passed

Production evidence:

- All six backup artifacts matched `SHA256SUMS`; the PostgreSQL dump listed `316` restore entries, the data archive listed `10234` members, and the V3.2.12 current-release record fixed the rollback target before cutover.
- The server package hash matched the local package, passed ZIP integrity checks, contained `452` members, and its `SOURCE_COMMIT` matched the frozen source commit.
- The atomic switch moved `current` from V3.2.12 to V3.2.13. A rollback trap was armed for the immutable V3.2.12 release and was not needed after the successful readiness check.
- Alembic current and heads both reported `20260824_0016 (head)`; no migration, downgrade, repair, terminal replacement, collector renumbering, camera request, or customer-platform request was run during deployment acceptance.
- The production security audit passed. Administrator read-only acceptance and the official health script completed without production business-data writes.
- The deployed `version.json` reports `3.2.13` and entry `assets/index-Dms37Iv7.js`; the entry SHA256 `be0e8e78e49cfc22b783077a0da71e51d6607d60c8bac1528e5d9c3a77587a4c` matched the release file and both local and public HTTP responses.
- Three ten-second soak samples kept PID `81917`, one listener, version `3.2.13`, and stable RSS at `342764 KiB`; post-start error journal lines remained `0`.
- Photo barcode maintenance service, timer, and enqueue service remained inactive, matching the captured pre-deployment state.
- Release retention dry-run identified only four old directories under `/opt/module-manager-v2/releases`; cleanup kept V3.2.13, V3.2.12, V3.2.11, V3.2.10, and V3.2.9 and deleted the four listed V3.2.8/V3.2.7/V3.2.6 directories. Backups, `.env`, `data`, and `uploads` were not touched.

## Rollback

- Restore `/opt/module-manager-v2/current` to `/opt/module-manager-v2/releases/v3.2.12-20260827T160539Z` and restart `module-manager-v2.service`.
- Wait for a real Uvicorn listener on `127.0.0.1:8000`, then verify local and public health report `3.2.12`.
- Keep Alembic at `20260824_0016`; do not run a downgrade.
