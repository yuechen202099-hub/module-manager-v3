# V3.2.11 Production Release Record

## Summary

- Status: attested
- Local Verification: passed
- Package: passed
- Production Deployment: passed
- Production Reconciliation: passed
- Rollback target: V3.2.10
- Candidate branch: `production/V3/3.2.11`
- Deployed production baseline: `V3.2.10`
- Candidate version: `V3.2.11`
- Database head: `20260824_0016 (head)`

## Scope

- Preserve review access for a uniquely matched terminal even when re-photo actions remain blocked.
- Treat multiple legitimate per-meter installation addresses as valid; only a meter group with no authoritative address is blocked by `address_missing`.
- Move collector inventory camera scanning into a dedicated closable dialog while retaining the construction Quagga-first scanner and native fallback.
- Keep legitimate multi-address terminals in the default review search and auto-open a searched terminal only when the complete result set is unique.
- Restore public access to the construction Quagga runtime by making each immutable release root traversable by Nginx; probe `/static/vendor/quagga.min.js` during every production health check.
- Route Android construction scanning through the proven ZXing live-camera fallback when Quagga cannot start, before attempting the browser-native BarcodeDetector path.
- No database migration and no production business-data rewrite.

## Source and package

- Source commit: `2fd8c2cbe377d1316e3f97f6d7f83a53b4e08a3d`
- Archive file: `build/server-release/module-manager-v2-server-3.2.11.zip`
- SHA256: `65d2cdf07000f3ac914c54c083469a8fae19203793c384f6210f45bc580f5cd9`
- Server SHA256: `65d2cdf07000f3ac914c54c083469a8fae19203793c384f6210f45bc580f5cd9`

## Local verification

- Backend focused matrix: multi-address and terminal filtering `3 passed`; Android/release focused gates `22 passed`; source and SOP gates passed
- Backend full suite: `1656 passed, 48 skipped, 0 failed` in `262.99s`
- Release verifier suite: `710 passed, 0 failed`
- Frontend tests: Node state `13 passed`; Vitest `70 passed`; `0 failed`
- Type check: passed
- Production build: passed; `1918` modules transformed
- Browser acceptance: Android construction scanning passed on the field device; public construction, review workbench and scanner runtime routes returned `200`
- Browser viewport: field Android browser passed; public desktop SPA shell and authenticated API acceptance passed

## Production deployment

- Backup directory: `C:\Users\Administrator\Documents\module-manager-production-backups\20260827-105619-V3.2.11-fast`; same-day full backup retained at `C:\Users\Administrator\Documents\module-manager-production-backups\20260827-022950`
- Backup verification: passed
- Backup evidence: compact backup matched all `7` SHA256 entries and its PostgreSQL dump listed `301` restore entries
- Release directory: `/opt/module-manager-v2/releases/v3.2.11-20260827T030718Z`
- Rollback directory: `/opt/module-manager-v2/releases/v3.2.10-20260826T195524Z`
- Uvicorn readiness: `127.0.0.1:8000 ready`
- Local health: passed; authenticated health gate reported `3.2.11`, and Uvicorn PID `64031` served `/health`
- Public health: passed; HTTPS `/health`, `/construction`, and `/static/vendor/quagga.min.js` returned `200`
- Authorization acceptance: passed
- Zero-write acceptance: passed
- Camera requests: `0`
- Client-platform requests: `0`
- Maintenance restoration: passed
- Maintenance state: the service, timer, and enqueue service remained in their captured inactive state
- Soak health checks: passed
- Attestation: passed

Production evidence:

- The uploaded archive matched the local SHA256, passed ZIP integrity checks, contained `248` required files, and its `SOURCE_COMMIT` matched the frozen source commit.
- The new release root is `0751 root:modulemgr`; its shared uploads link resolves to `/opt/module-manager-v2/uploads`, allowing the `modulemgr` service account to import the application without writing inside the immutable release.
- Alembic current and heads both reported `20260824_0016 (head)`; no migration, downgrade, repair, collector allocation, terminal open, replacement, refresh, completion, or rollback was run.
- Production contains the Android scanner source markers and the built `ConstructionView-BZGM3dOR.js` ZXing path; `/static/vendor/quagga.min.js` returned `200` with `156608` bytes.
- Administrator read-only project and terminal requests returned `200` with `135` terminal candidates. Constructor requests to collector projects, review-workbench terminals, and system status all returned `403`.
- All `34` PostgreSQL table row counts matched before and after the read-only authorization acceptance. Three expected operational audit rows were created earlier by the authenticated production health checks; no business-table row count changed.
- Three soak samples kept PID `64031`, one real listener, version `3.2.11`, health `200`, disk usage `72%`, and post-start error count `0`; RSS samples were `410812416`, `454774784`, and `451072000` bytes.
- Field confirmation reported that Android construction scanning is working. Automated acceptance made no camera request and sent no request to the client platform.

## Rollback

- Restore `/opt/module-manager-v2/current` to `/opt/module-manager-v2/releases/v3.2.10-20260826T195524Z` and restart `module-manager-v2.service`.
- Wait for a real Uvicorn listener on `127.0.0.1:8000`, then verify local and public health.
- Keep Alembic at `20260824_0016`; do not run a downgrade.
