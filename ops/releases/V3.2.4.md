# V3.2.4 Production Release Record

- Status: pending
- Local Verification: passed
- Package: passed
- Production Deployment: pending
- Production Reconciliation: pending
- Rollback target: V3.2.2
- 候选分支：`production/V3/3.2.4`
- 当前生产基线：`V3.2.2`
- 候选版本：`V3.2.4`
- 数据库版本：`20260724_0014 (head)`；no Alembic migration

## 修复范围

- 修复 Python 3.12 标准库 `urllib.request.HTTPSHandler` 不再提供 `_check_hostname` 时，固定 DNS 的 HTTPS 下载处理器在进入网络边界前异常退出的问题。
- `_PinnedHTTPSHandler.https_open()` 继续把继承的 SSL context 传给标准库 `do_open()`，保留 DNS 固定、禁止重定向、私网地址拒绝、证书和 SNI 校验。
- 不修改迁移数据模型、OSS key、导出退休路径或 Alembic；数据库 head 保持 `20260724_0014`。

## V3.2.3 生产试运行失败与安全回退证据

- 失败迁移 ID：`v323-external-20260819T142410Z`。
- 精确异常：`AttributeError: '_PinnedHTTPSHandler' object has no attribute '_check_hostname'`。
- 数据库回滚结果：`zero-write`；数据库专用回滚完成且变更数为 0。
- Application rollback: V3.2.2 restored
- 未修改任何照片行或 OSS 对象；永久 Nginx `410 Gone` 导出退休拦截保持生效。
- V3.2.3 已作为不可变候选在生产暴露，本修复不得复用其版本、release 或发布包身份。

## 候选状态

- 本记录仅声明 V3.2.4 本地候选身份；本地验证、source-bound 性能证据、发布包构建与独立验包已通过。
- 发布包只保留在 ignored `build/` 下；未上传、未部署、未运行生产迁移、未修改生产 PostgreSQL、OSS、`.env`、data 或 uploads。
- V3.2.3 的服务器 release 和既有 ZIP 保持不可变；任何部署或重新试运行仍需独立授权与完整生产门禁。

## 本地验证（已通过）

- 最终 reviewed source commit：`3b13a57c66fbcd71c18928906e612342f9a90c99`；tracked worktree 在验证前后均 clean。
- complete backend：`1416 passed, 35 skipped, 87 warnings in 242.32s`，exit `0`。
- complete root scripts：`806 passed, 10 skipped, 1 warning in 59.76s`，exit `0`；10 个 skip 仅为已裁定的历史 V3.2.3 current-tree-only cases，无其他 skip 或 failure。
- 11 个 active gate 全部 exit `0`：static pages、5 个 V3.2.0 compatibility gates、V3.2.1 installer KPI restore、V3.2.4 release contract、release SOP、admin release notes 与 strict-native Vue migration gate。
- Vue `type-check` 与 production build 均 exit `0`，转换 `1670` modules；仅有 2 条依赖注释提示和既有的 `>500 kB` chunk advisory。
- Vue asset manifest 共 `27` 个文件，实际成员、size、SHA256 与 entry SHA256 全部一致；entry 为 `assets/index-BnLqfT2O.js`，SHA256 为 `ba8fc311fed14a100e96c6e9d025c9c8a763d364f5e277ae03fc4bc0e02bf22e`。
- build 后 V3.2.4 release contract 与 strict-native Vue gate 再次通过，HEAD 未变化且未产生 tracked drift。

## Source-bound 性能证据（已通过）

- 路径：`build/release-evidence/v3.2.4-task-review.json`
- 大小：`1290 bytes`
- SHA256：`ebef5c23852ac1c0696857dd6d554655f22cdebab49177b50e1649da903c13de`
- source commit：`3b13a57c66fbcd71c18928906e612342f9a90c99`
- 隔离 JSON state 通过受支持的 `POST /local-test/bootstrap` 初始化，HTTP `200`；强制刷新和随后普通 task snapshot 均为本次实际 `135` 项。
- 官方 seed 选中 task `67`、group `g-22362`，`reviewer` task count 为 `1`；未访问用户 PostgreSQL、生产或 OSS。
- sampler 完整运行 `60 seconds`，`ok = true`、failures `0`、routes `2`；task snapshot `31.782 ms`，review groups `4.883 ms`，review group count `20`（total `88`），task builds `1`。
- `verify_v3_1_release.py --expected-source-commit 3b13a57c66fbcd71c18928906e612342f9a90c99` 的 10 个 gate 全部通过；隔离服务已停止、端口 `8013` 已释放、临时 state 已删除。

## 发布包证据（已通过）

- 路径：`build/server-release/module-manager-v2-server-3.2.4.zip`
- 大小：`1715273 bytes`
- SHA256：`21ff3d8e5ed02e5c7cec6ee26bc84f808cb53cbd1b553956ef7e83618644abba`
- `verify-client-release.py --expected-source-commit 3b13a57c66fbcd71c18928906e612342f9a90c99`：通过；required members `189`，missing `0`。
- 独立成员审计：ZIP file members `386`，uncompressed bytes `6880889`；exact/case-insensitive duplicates、unsafe/absolute/traversal paths、symlinks、untracked non-Vue members 均为 `0`。
- forbidden local/cache/runtime/data/uploads/export-output/report/allowlist/build members 为 `0`；性能报告按合同不进入 ZIP，report members 为 `0`。
- 高置信度真实 secret 命中为 `0`；唯一 key-shaped 文本来自迁移脚本的固定脱敏回归测试夹具，不是凭据，且独立复核无 Critical/Important finding。
- ZIP 内 `SOURCE_COMMIT` 与外部性能报告 `source_commit` 均等于最终 reviewed source commit `3b13a57c66fbcd71c18928906e612342f9a90c99`。

## 本地 attestation

- 本记录之后紧接的 documentation-only commit 即 V3.2.4 候选 attestation；其精确 SHA 从 Git history 与忽略的验证报告解析，不在该 commit 内自引用，也不为记录自身 SHA 创建第二个文档提交。
- packaged source commit 为 `3b13a57c66fbcd71c18928906e612342f9a90c99`；attestation commit 与它之间唯一允许的 tracked path 是 `ops/releases/V3.2.4.md`。

## 生产操作（未执行）

- 本次未执行 SSH、生产访问、应用部署、数据库或 OSS 操作，也未上传发布包或 push 分支。
- 未切换 release、重启服务、运行外链迁移或清理历史 release；`Production Deployment` 与 `Production Reconciliation` 保持 `pending`。
