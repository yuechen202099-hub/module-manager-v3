# V3.2.9 Production Release Record

## Summary

- Status: deployed; automated acceptance passed; real-phone camera confirmation pending
- Priority: P0 construction inventory scanner hotfix
- Candidate branch: `production/V3/3.2.9`
- Candidate version: `V3.2.9`
- Deployed production baseline: `V3.2.8`
- Rollback target: `/opt/module-manager-v2/releases/v3.2.8-20260826T000859Z`
- Database head: unchanged; no migration

## User requirement

- Fix the collector inventory page whose camera opens but does not scan.
- Reuse the field-construction QuaggaJS scanner behavior.
- Release this repair first without including the unfinished review/re-photography workbench.

## Change scope

- Start the pinned QuaggaJS construction scanner before requesting a native camera stream.
- Use `BarcodeDetector` or native preview only after QuaggaJS is unavailable or fails cleanly.
- Remove a partially registered detection callback and stop QuaggaJS before opening the native fallback.
- Preserve manual entry, current-project isolation, duplicate/in-flight guards, same-number reuse, photo decisions, and pool rules.
- No API contract, database schema, production data, upload, OSS, or environment change.

## TDD evidence

- Root cause: the inventory page acquired `getUserMedia` before QuaggaJS, so mobile browsers could leave QuaggaJS unable to acquire the same camera even though the preview was visible.
- RED: the Quagga-first regression failed against the old native-first implementation.
- RED: `stops a partially started construction scanner before native fallback opens the camera` failed because `offDetected` was never called.
- GREEN: focused scanner cleanup regression passed after retaining startup ownership until callback registration and scanner start both finish.

## Local verification

- Collector-transfer Node suite: `12 passed, 0 failed`.
- Collector-transfer Vitest suite: `53 passed, 0 failed`, including `CollectorInventoryView.spec.ts` `30 passed`.
- Component suite: `5 passed, 0 failed`.
- Backend runtime-version checks: `2 passed, 1 third-party deprecation warning`.
- Vue type check: passed.
- Vue production build: passed; only acknowledged third-party Rollup annotation and chunk-size warnings were emitted.
- Version consistency: frontend source, built runtime, API runtime, package metadata, and static title all report `3.2.9`.
- Static asset integrity: all 10 `index.html` references exist; built `version.json` records 32 assets with their size and SHA-256.
- Package integrity: 569 entries, 0 forbidden entries, embedded source commit matched, Quagga-first and partial-start cleanup markers present.
- Scope isolation: unfinished V3.2.10 review/re-photography commit `9b6440e` is not an ancestor of the packaged source.

## Independent review

- Initial review: no Critical; one Important finding for cleanup after `onDetected()`/`start()` failure.
- Resolution: focused RED/GREEN coverage added and startup cleanup corrected.
- Final scoped rereview: no Critical and no code Important findings; code and static assets are ready subject to normal backup, hash, listener, page, and rollback deployment gates.

## Source and package

- Source commit: `f02f5e7229918f5d8e4b7412dc18c5405cc50049`
- Package: `build/server-release/module-manager-v2-server-3.2.9.zip`
- Package size: `2413096 bytes`
- Local SHA256: `9c8aa454f59933fcd7b91e2d5bdc76d619e58a82598f018d87f75ffd69d76e14`
- Server SHA256: `9c8aa454f59933fcd7b91e2d5bdc76d619e58a82598f018d87f75ffd69d76e14` (matches local)
- Package method: emergency source-bound Git archive from the clean committed candidate, with `SOURCE_COMMIT` embedded; no `.env`, data, uploads, database, key material, `v2-api/uv.lock`, or unfinished V3.2.10 feature code.

## Production deployment

- Backup: `/opt/module-manager-v2/backups/V3.2.9-pre-20260826_155122`; all six `SHA256SUMS` entries passed, including current release, PostgreSQL dump/schema, data, and uploads.
- Release directory: `/opt/module-manager-v2/releases/v3.2.9-20260826T075447Z`
- Rollback directory: `/opt/module-manager-v2/releases/v3.2.8-20260826T000859Z`
- Uvicorn listener `127.0.0.1:8000`: active, PID `46104`.
- Alembic: `20260824_0016 (head)`; no migration was run.
- Local health and authenticated production health script: passed; system status reports `3.2.9`.
- Public `/health`, `/project-board`, `/collector-inventory`, `/static/vendor/quagga.min.js`, and `/static/vue/version.json`: HTTP 200; public runtime version is `3.2.9`.
- Production security audit: passed.
- Deployed static manifest: 32 assets verified by size and SHA-256; public and deployed Quagga SHA-256 both equal `ae6c469103c5d427625a9a4c41175bd15420a14aa5579ea57dc1571d42346f4d`.
- Browser acceptance through a read-only SSH forward to the exact production Uvicorn process: desktop and `390x844` login shell rendered V3.2.9 without framework overlay or console warning/error; empty-login interaction showed the expected validation alert. Direct in-app Browser access to the public domain returned a browser-side connection-closed error although command-line public HTTPS checks passed.
- Real-phone acceptance: pending field confirmation that a physical collector barcode is recognized after camera permission; desktop automation cannot supply a real rear camera.
- Rollback readiness: previous immutable V3.2.8 release retained; the deployment trap was armed to restore it on switch failure. No rollback was required.
- Service soak: main service and Nginx active, listener PID stable, post-deploy error journal count `0`.
- Background photo-barcode maintenance service/timer remain inactive; this scanner-only hotfix did not broaden authority to resume unrelated maintenance.

## Retention cleanup

- Before backup, explicitly removed old V3.2.0 through V3.2.4 full backups after validating every resolved path remained under `/opt/module-manager-v2/backups`; retained V3.2.5, V3.2.6, runtime backups, data, uploads, and the new V3.2.9 backup.
- Removed the never-activated failed preparation directory `/opt/module-manager-v2/releases/v3.2.9-20260826T075408Z`.
- Release cleanup dry-run and apply retained exactly five releases: V3.2.9, V3.2.8, V3.2.7, and two V3.2.6 directories.
- Filesystem usage changed from 97% before cleanup to 72% after the new full backup and deployment.
