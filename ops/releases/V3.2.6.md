# V3.2.6 Production Release Record

- Status: deployed
- Local Verification: passed
- Package: passed
- Production Deployment: passed
- Production Reconciliation: passed
- Rollback target: V3.2.5
- Candidate branch: `production/V3/3.2.6`
- Deployed production baseline: `V3.2.5`
- Candidate version: `V3.2.6`
- Database head: `20260823_0015 (head)`

## User request

- 发布采集器盘点、一次性替换池和甲方翻拍工作台到生产。
- 取消新采集器 Excel/照片批量盘点导入。
- 保留系统原有总清单、施工资料等导入能力，以及项目快照建批、手机逐个扫码/补拍、管理员随机分配和桌面翻拍。

## Change scope

- 手机扫码只判断同号复用、是否补拍和是否入池，不录入甲方平台。
- 同一实物采集器和同一有效照片禁止重复使用。
- 新装项固定为“电表和模块”与“改造完成”两张照片；拆除项每个去重采集器一张照片。
- 已退役的 `POST /collector-transfer/runs/{run_id}/inventory/import` 必须返回 `404`，手机页面不得显示批量导入入口。
- 原 `material_groups`、`photos` 及系统原有清单/施工资料导入链路保持不变。

## Source and package

- 源码提交：`a6339ee5a15c76068ab9ed08f42c595458f48093`
- 本地包：`build/server-release/module-manager-v2-server-3.2.6.zip`
- 包大小：`1865760 bytes`；成员数 `421`；重复/大小写冲突/路径穿越/加密/CRC 错误均为 `0`。
- 本地 SHA256：`1b111b166001a392628a489b63c1310493fa369b763663e4e2882daf6fff5310`
- 服务器 SHA256：`1b111b166001a392628a489b63c1310493fa369b763663e4e2882daf6fff5310`
- 部署目录中的 `SOURCE_COMMIT` 为 `a6339ee5a15c76068ab9ed08f42c595458f48093`，与发布包一致。

## Production deployment

- 上线前备份：`/opt/module-manager-v2/backups/V3.2.6-pre-20260824_125115`
- 当前 release：`/opt/module-manager-v2/releases/v3.2.6-20260824_130101`
- 生产回滚 release：`/opt/module-manager-v2/releases/v3.2.5-20260823_030829`
- 数据库版本：`20260823_0015 (head)`
- 本机 `/health`：`200`
- 公网 `/health`：`200`
- `production_health_check.py --expected-version 3.2.6`：通过；核心页面状态、退役导出 `410`、生产 API 文档 `404`、版本和汇总字段均通过。
- `audit_production_security.py`：通过。

首次切换因新 release 目录为 `root:root 750`，服务以 `200/CHDIR` 失败；部署脚本自动回滚到 V3.2.5 并恢复健康。修正 release 目录可进入权限后再次切换成功，等待 `127.0.0.1:8000` 实际监听后才执行验收。失败目录 `/opt/module-manager-v2/releases/v3.2.6-20260824_125844` 保留用于审计，未执行 release 清理。

## Live rollout evidence

| Evidence | Value |
| --- | --- |
| SHA256 | 1B111B166001A392628A489B63C1310493FA369B763663E4E2882DAF6FFF5310 |
| Source commit | `a6339ee5a15c76068ab9ed08f42c595458f48093` |
| Backup directory | /opt/module-manager-v2/backups/V3.2.6-pre-20260824_125115 |
| Release directory | /opt/module-manager-v2/releases/v3.2.6-20260824_130101 |
| Public health check | https://www.sgcc.online/health passed (HTTP 200) |
| Database head | `20260823_0015 (head)` |

## Production acceptance

- `/health`、`/project-board`、`/collector-batches`、`/collector-inventory`、`/collector-workbench` 在应用本机和公网均返回 `200`。
- 管理员认证后 `GET /collector-transfer/projects` 返回 `200`（1 个项目），`GET /collector-transfer/runs` 返回 `200`（0 个现存批次）。因为生产暂无采集器批次，本次未创建验收批次，也未写入或上传现场数据。
- `POST /collector-transfer/runs/release-smoke/inventory/import` 返回 `404`。
- 生产资源 `CollectorInventoryView-Cc5pbeCN.js` 不含 `/inventory/import` 或“批量导入”。
- 内置浏览器通过 SSH 安全通道确认 V3.2.6 登录页非空、无框架错误覆盖层、控制台无相关 warning/error；生产路由在未认证时正确跳转正式登录页。登录后的手机盘点和桌面工作台视觉交互未执行，避免未经即时确认向浏览器传输生产凭据。
- 公网验证使用 `www.sgcc.online:443 -> 106.14.122.43` 固定解析保留 Host/SNI，规避工作站 Clash fake-DNS；未修改系统网络设置。
- 部署前本来就是停止状态的 worker/timer 继续保持 `inactive`，未擅自启用。

## Backup verification and authorized cleanup

- V3.2.6 备份中的 `current_release.tar.gz`、`current_release.txt`、`database.dump`、`database-schema.sql`、`data.tar.gz`、`uploads.tar.gz` 全部通过 `SHA256SUMS`。
- PostgreSQL dump 可列出，三个 tar.gz 归档可完整读取；`database.dump` 为 `20516650 bytes`，`data.tar.gz` 为 `2034591166 bytes`。
- 经用户授权删除被较新完整备份取代的重复旧备份 `/opt/module-manager-v2/backups/V3.2.0-pre-20260724_095217`，删除后复核确认目标不存在。
- 本次不执行旧 release 清理；current、回滚 release、失败审计 release、数据目录和 V3.2.6 完整备份均保留。

## Verification

- Backend tests: `1509 passed, 39 skipped, 87 warnings in 239.55s`。
- Frontend tests/type-check/build: component `5 passed`; collector-transfer `63 passed`; type-check passed; production build transformed `1684` modules。
- PostgreSQL migration upgrade/downgrade/upgrade: passed on disposable local PostgreSQL; final `20260823_0015 (head)`。
- Real PostgreSQL collector-transfer integration: `4 passed`。
- Gate/SOP/package verifier tests: `631 passed, 1 expected duplicate-ZIP fixture warning`。
- Source-bound performance evidence `build/release-evidence/v3.2.6-task-review.json` completed the full 60-second sample with zero failures; SHA256 `03cb5f60062196586e663a625788ab0e43a0c2922b43140c37dd1096d8538f68`。
- V3.2.6 专用 `verify_v3_2_6_release.py --phase attestation` 通过。通用 `verify_release_sop.py --version V3.2.6 --phase attestation` 仍错误地把非基线 candidate 强制判定为 `pending`；这是 attestation 阶段未传入通用 lifecycle 判断的既有验证器缺陷，本次遵守“只提交发布记录”边界，不修改发布包外的验证器源码，也不把真实部署状态回写为 pending。

## Remaining risks

- 磁盘仍为 `97%`，约余 `1.5 GB`；在明确新的保留策略和删除目标前，不扩大清理历史备份或 release。
- 手机摄像头扫码依赖浏览器 `BarcodeDetector`；不支持时使用手工输入或外接扫码枪回退。
- 生产当前没有采集器批次，真实业务数据上的完整“扫码 → 补拍 → 分配 → 翻拍完成”仍需首批受控业务验收。
- 如需在浏览器中完成登录后视觉验收，须在输入生产账号密码前取得即时确认，且不得创建批次或上传现场照片。
- 通用 SOP attestation 的候选生命周期判断需在后续工具修复中按 `--phase` 区分 source/pending 与 attestation/deployed，并增加相应回归测试。
